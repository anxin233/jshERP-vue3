# P0 测试证据（工程脚手架）

- 执行时间：2026-09-13
- 执行人：AI 助手（OpenCode）
- 依据：`26-移动端H5实施执行计划.md` 第 3 节 P0
- 结论：**P0 全部用例通过（5/5）**，lint 无错误

## 1. 环境

| 项目 | 值 |
|---|---|
| Node / npm | v22.22.3 / 10.9.8 |
| 后端 | http://localhost:9999/jshERP-boot（运行中） |
| MySQL / Redis | deploy/dev Docker（healthy） |
| H5 dev | http://localhost:3001（Vite 8.3.0） |
| 浏览器验证 | 无头 Chrome 152（CDP，390×844 移动视口） |

## 2. 任务完成情况

| 任务 | 内容 | 结果 |
|---|---|---|
| P0.1 | 创建工程与依赖安装（vant/pinia/axios/md5/dayjs/less/postcss 适配等） | ✅ 220 packages |
| P0.2 | `vite.config.js`：base / 端口 3001 / 代理 `/jshERP-boot` / Vant 自动导入 | ✅ |
| P0.3 | `.env.development`、`.env.production` | ✅ |
| P0.4 | 目录骨架（api/components/constants/layout/router/store/styles/utils/views） | ✅ |
| P0.5 | `styles/variables.less`、`styles/common.less`、px→vw 适配 | ✅ 构建 CSS 含 vw |
| P0.6 | 6 个路由页 + TabBar 布局（5 个 Tab） | ✅ |
| P0.7 | ESLint（flat config）+ `docs/` 证据目录 | ✅ lint 0 错误 |

## 3. 测试用例结果

| 用例 | 步骤 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| P0-T1 | `npm install` | 安装成功 | `added 220 packages in 6s` | ✅ |
| P0-T2 | `npm run dev` | 3001 可访问、无报错 | `HTTP 200`，Vite ready in 400ms | ✅ |
| P0-T3 | `curl http://localhost:3001/jshERP-boot/platformConfig/getPlatform/checkcodeFlag` | 返回 `1` | `1`（与后端直连一致） | ✅ |
| P0-T4 | 无头浏览器打开首页并依次点击 5 个 Tab | 路由/高亮正确、无控制台报错 | 初始 `/home`；依次 `/sale`→`/stock`→`/bills`→`/mine`→`/home`，高亮文字同步；`ERRORS []` | ✅ |
| P0-T5 | `npm run build` + `npm run preview` | 构建成功、`/m/` 可访问 | `built in 564ms`；资源前缀 `/m/assets/...`；`HTTP 200`；CSS 含 `vw` | ✅ |

## 4. 关键命令输出

```text
# P0-T2
VITE v8.3.0  ready in 400 ms
➜  Local:   http://localhost:3001/

# P0-T3
curl http://localhost:3001/jshERP-boot/platformConfig/getPlatform/checkcodeFlag  → 1

# P0-T5
dist/index.html: src="/m/assets/index-*.js" href="/m/assets/index-*.css"
vite preview:  http://localhost:4174/m/  → HTTP 200
```

## 5. 产物与备注

- 新工程：`jshERP-h5/`（与 `jshERP-web`、`jshERP-boot` 平级）
- H5 dev 服务当前仍在 3001 运行（供 P1 联调使用）；preview 服务已在测试后关闭
- 未改动 `jshERP-boot/` 与 `jshERP-web/`
