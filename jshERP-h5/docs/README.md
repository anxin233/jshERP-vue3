# jshERP-h5 文档目录

- `test-evidence/`：各阶段（P0~P8）测试执行证据，按阶段分子目录，如 `P0.md`、`P1/`。
- `bugs.md`：缺陷记录（模板见 `../../26-移动端H5实施执行计划.md` 第 5 节）。

约定：

1. 每完成一个阶段，新增/更新对应证据文件，写明执行时间、环境、用例结果、证据（命令输出、接口 JSON、截图路径）。
2. 同步更新 `../../26-移动端H5实施执行计划.md` 第 9 节"进度跟踪表"。
