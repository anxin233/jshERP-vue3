<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsh.erp.datasource.mappers.WorkOrderProjectMapperEx">

  <resultMap id="ProjectItemMap" type="com.jsh.erp.datasource.entities.WorkOrderProject">
    <id     column="id"           property="id" />
    <result column="order_id"     property="orderId" />
    <result column="project_id"   property="projectId" />
    <result column="project_name" property="projectName" />
    <result column="unit_price"   property="unitPrice" />
    <result column="quantity"     property="quantity" />
    <result column="discount_rate" property="discountRate" />
    <result column="amount"       property="amount" />
    <result column="worker_name"  property="workerName" />
    <result column="remark"       property="remark" />
    <result column="sort"         property="sort" />
  </resultMap>

  <select id="getByOrderId" resultMap="ProjectItemMap">
    select * from jsh_work_order_project
    where order_id = #{orderId}
    order by sort asc, id asc
  </select>

  <delete id="deleteByOrderId">
    delete from jsh_work_order_project where order_id = #{orderId}
  </delete>

  <delete id="deleteByOrderIds">
    delete from jsh_work_order_project
    where order_id in (
    <foreach collection="orderIds" item="id" separator=",">#{id}</foreach>
    )
  </delete>

  <insert id="batchInsert">
    insert into jsh_work_order_project
    (order_id, project_id, project_name, unit_price, quantity, discount_rate, amount, worker_name, remark, sort)
    values
    <foreach collection="list" item="item" separator=",">
      (#{item.orderId}, #{item.projectId}, #{item.projectName}, #{item.unitPrice},
       #{item.quantity}, #{item.discountRate}, #{item.amount}, #{item.workerName},
       #{item.remark}, #{item.sort})
    </foreach>
  </insert>

</mapper>
