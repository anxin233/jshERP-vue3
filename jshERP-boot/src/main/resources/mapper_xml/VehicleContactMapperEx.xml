<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsh.erp.datasource.mappers.VehicleContactMapperEx">

    <select id="getByVehicleId" resultMap="com.jsh.erp.datasource.mappers.VehicleContactMapper.BaseResultMap">
        select * from jsh_vehicle_contact
        where vehicle_id = #{vehicleId}
        and ifnull(delete_flag,'0') != '1'
        order by id asc
    </select>

    <delete id="deleteByVehicleId">
        delete from jsh_vehicle_contact where vehicle_id = #{vehicleId}
    </delete>

    <insert id="batchInsert">
        insert into jsh_vehicle_contact (vehicle_id, contact_name, contact_phone, tenant_id, create_time, delete_flag)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.vehicleId}, #{item.contactName}, #{item.contactPhone}, #{item.tenantId}, #{item.createTime}, '0')
        </foreach>
    </insert>

</mapper>
