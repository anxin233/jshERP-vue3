<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsh.erp.datasource.mappers.OptionGroupMapperEx">

    <resultMap id="BaseResultMap" type="com.jsh.erp.datasource.entities.OptionGroup">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="code"         jdbcType="VARCHAR"   property="code"/>
        <result column="name"         jdbcType="VARCHAR"   property="name"/>
        <result column="scope"        jdbcType="VARCHAR"   property="scope"/>
        <result column="tenant_id"    jdbcType="BIGINT"    property="tenantId"/>
        <result column="remark"       jdbcType="VARCHAR"   property="remark"/>
        <result column="enabled"      jdbcType="BIT"      property="enabled"/>
        <result column="create_time"  jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_time"  jdbcType="TIMESTAMP" property="updateTime"/>
        <result column="delete_flag"  jdbcType="CHAR"     property="deleteFlag"/>
    </resultMap>

    <!-- 管理界面：选项组列表，租户隔离（系统级 + 当前租户） -->
    <select id="listForManage" resultMap="BaseResultMap">
        select *
        from jsh_option_group
        where ifnull(delete_flag,'0') != '1'
          and (tenant_id is null or tenant_id = #{tenantId})
        <if test="code != null and code != ''">
          and (code = #{code} or code like concat('%', #{code}, '%'))
        </if>
        <if test="name != null and name != ''">
          and name like concat('%', #{name}, '%')
        </if>
        order by id asc
    </select>

</mapper>
