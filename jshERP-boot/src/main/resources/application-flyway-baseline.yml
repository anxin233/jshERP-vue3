# 老库首次纳入 Flyway 时启用一次：
#   java -jar jshERP.jar --spring.profiles.active=flyway-baseline
#
# FlywayConfig 在该 profile 下显式调用 baseline()。
# baseline 版本由 spring.flyway.baseline-version 绑定（默认与主配置一致为 8）。
spring:
  flyway:
    baseline-on-migrate: true
    baseline-version: 8
