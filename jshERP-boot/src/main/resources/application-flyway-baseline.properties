# Enable this profile once when an existing database is first enrolled in Flyway:
# --spring.profiles.active=flyway-baseline
#
# FlywayConfig explicitly calls baseline() in this profile. The baseline version
# is bound from spring.flyway.baseline-version by Spring Boot before baseline().
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=8
