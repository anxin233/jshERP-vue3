# 已有数据库首次纳入 Flyway 时启用一次：
# --spring.profiles.active=flyway-baseline
#
# 注意：Spring Boot 2.0 自带 FlywayProperties 不处理 baseline-on-migrate。
# 这里由 flyway-baseline profile 下的 FlywayConfig 显式调用 Flyway 5 的 baseline()。

spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=8
