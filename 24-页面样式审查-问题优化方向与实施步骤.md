# 24-页面样式审查：问题、优化方向与实施步骤

| 项 | 内容 |
| --- | --- |
| 文档性质 | 前端页面样式专项审查报告（供评审） |
| 审查分支 | `feature/vue3.0`（尖端提交以当时检出为准，约 `38e08e72`） |
| 审查范围 | `jshERP-web` 全局布局、列表搜索区、页签、侧栏、顶栏、表格/弹窗公共样式及典型业务页结构 |
| 技术栈基线 | Vue **3.5.35** + Ant Design Vue **4.2.6** + Vite **8** + Less |
| 文档日期 | 2026-07-19 |
| 关联文档 | `18`～`23` 号（图标/列表/表格/搜索样式排查）、`12`～`17` 号（AntD4/Vite 升级）、`jshERP-web/docs/ANTDV4_MIGRATION_STATUS.md` |

---

## 1. 审查结论摘要

### 1.1 总体判断

| 维度 | 结论 |
| --- | --- |
| 可用性 | **可正常使用**。Vue3 + AntD4 迁移后，列表搜索区、页签、图标映射等已做过针对性补丁，并非「完全没有样式」。 |
| 一致性 | **不足**。Header/Sider/内容区高度混用 49px 与 64px；light/dark 语义与视觉相反；主题色未统一走 Design Token。 |
| 可维护性 | **偏弱**。约 76 个列表页仍用旧式 `inline form + 栅格 + labelCol`；公共 CSS 用大量 `!important` 硬扛；内联 `style` 与 `float` 残留多。 |
| 优先级建议 | 先做「尺寸统一 + 主题语义」，再收敛搜索区结构，最后清理 float/`!important`/内联样式。 |

### 1.2 一句话结论

当前样式问题**主要不是缺美化**，而是：**布局尺寸与主题语义不一致**、**列表表单用 CSS 硬扛 AntD1 旧结构**、**float + 内联样式 + !important 技术债**。建议按本文第 4 节分阶段实施，优先低风险、高观感收益项。

### 1.3 量化观察（审查时统计）

| 指标 | 约数 | 说明 |
| --- | --- | --- |
| `layout="inline"` 列表/页面 | ~76 | 仍是 AntD1 时代列表搜索写法 |
| `table-page-search-wrapper` 使用页 | ~74 | 依赖 `GlobalLayout` 全局样式 |
| `style="..."` 内联（views） | ~950 | 间距/颜色/宽度散落 |
| `!important`（src 样式相关） | ~116 | 覆盖 AntD4 默认成本高 |
| `float: left` 相关文件 | 十余处 | 单据/财务弹窗、部分报表工具栏等 |
| `ant-form-item-control-wrapper` | 0 | 旧选择器已从公共样式中清除（好） |

---

## 2. 现状说明（已做得较好的部分）

以下内容**不建议推倒重来**，后续优化应在其基础上收敛，避免回归。

### 2.1 列表搜索公共样式（第一阶段已落地）

文件：`jshERP-web/src/components/page/GlobalLayout.vue`  
选择器：`.table-page-search-wrapper`

已具备：

- 适配 AntD4 表单项 DOM（`.ant-form-item-label` / `.ant-form-item-control` / `.ant-form-item-control-input`）
- 去掉对 `.ant-form-item-control-wrapper` 的依赖
- label 区固定宽度（约 82px）、控件区 `width: 100%`
- 查询按钮区 `.table-page-search-submitButtons` 使用 `inline-flex` 垂直居中
- 部分栅格断点下的 `md-*` 宽度强制规则

对应排查文档：`23-列表搜索框样式异常排查与解决方案.md` 第一阶段方向。

### 2.2 多页签 Tabs 定制

文件：`jshERP-web/src/components/layouts/TabLayout.vue`

已按 Ant Design Vue 4 的 tabs 结构做了 tab 高度、关闭按钮 hover 显示、active 态等覆盖，视觉接近原 Jeecg 多页签习惯。

### 2.3 侧栏图标映射

文件：`jshERP-web/src/components/legacy/legacy-icon-asn.js`、`LegacyIcon.vue`

业务图标名（如 `shopping-cart`、`money-collect`、`retweet`、`project` 等）已有映射；配合 `18` 号文档，侧栏「圆圈问号」问题应已明显缓解。后续缺图优先补映射，而不是改业务菜单结构。

### 2.4 列表页骨架统一

多数业务列表仍遵循：

```text
查询区（table-page-search-wrapper）
  → 操作按钮区（table-operator）
  → 表格（a-table）
```

结构清晰，适合抽公共组件，而不是逐页重做 UI。

---

## 3. 问题清单

### 3.1 P0：布局高度混用 49px / 64px

**现象风险：** 固定顶栏与内容区、侧栏 Logo、折叠态宽度计算容易出现「内容被挡」「顶栏与侧栏对不齐」「折叠后缝隙」等问题。

| 位置 | 当前高度/用途 |
| --- | --- |
| `GlobalHeader.vue` `@height` | **49px**（侧栏模式下顶栏实际高度） |
| `Logo.vue` | **49px** |
| `GlobalLayout.vue` 内容区 `paddingTop` | **49px**（`fixedHeader` 时） |
| `GlobalLayout.vue` 全局 `.header` 等 | 多处 **64px** |
| 侧栏 Logo / 部分 menu 行高 | **64px** |

**根因：** 迁移与历史补丁中未抽出统一的 layout token，Header 按 49 压缩，全局样式仍保留 Ant Design Pro 默认 64。

**影响范围：** 全站框架（所有登录后页面）。

---

### 3.2 P0：主题 light/dark 语义与视觉相反；主题色双轨

**问题 A — 命名与视觉相反**

- 默认配置：`defaultSettings.js` 中 `navTheme: 'light'`
- `GlobalHeader` 侧栏模式：默认 `.header` 为**主色底 + 浅色字**；`&.dark` 反而是**白底深色字**
- 与 Ant Design「dark = 深色底、light = 浅色底」惯例不一致，后续改主题、写样式极易写反

**问题 B — 图标/操作区颜色写死**

- `GlobalLayout` 中 `.user-wrapper .action .anticon { color: white }` 等写死颜色
- 在 light 顶栏或背景变化时，可能出现对比度不足或「白底白图标」类问题

**问题 C — 主题色未统一 Design Token**

| 来源 | 作用 |
| --- | --- |
| `defaultSettings.primaryColor` | 业务默认主色 |
| Vite `css.preprocessorOptions.less.modifyVars` | 编译期 Less 变量 |
| Ant Design Vue 4 `reset.css` | 组件默认 token |
| `a-config-provider` | **当前仅 locale，无 `theme.token`** |

结果：改主色/弱色可能只影响一部分 Less 自写样式，AntD4 组件本体不同步；「界面设置」动态换肤能力存疑。

**影响范围：** 顶栏、侧栏、按钮主色、链接色、页签 active 色。

---

### 3.3 P1：列表搜索区「三套布局叠加」，靠 CSS 硬扛

**典型结构（如商品列表、工单列表等）：**

```vue
<div class="table-page-search-wrapper">
  <a-form layout="inline">
    <a-row :gutter="24">
      <a-col :md="6" :sm="24">
        <a-form-item label="..." :labelCol="labelCol" :wrapperCol="wrapperCol">
          <a-input ... />
        </a-form-item>
      </a-col>
      <!-- 按钮常在 a-col 内，部分历史页仍有 span + float -->
    </a-row>
  </a-form>
</div>
```

**叠加关系：**

1. AntD `layout="inline"` 的默认行内规则  
2. `a-row` / `a-col` 栅格  
3. 各页 `labelCol` / `wrapperCol`（span 还不一致，如工单页按字段写不同 span）  
4. `GlobalLayout` 强制 label 约 82px、`form-item` 拉满宽度，并用大量 `!important`

**后果：**

- 公共样式与页面 `labelCol` **互相打架**
- 长标签（如「单据编号」「结算账户」）与短标签起点不统一的问题只能靠固定 px 掩盖
- 新增列表页容易再复制一套错误结构
- 文档 `23` 描述的第二、三阶段（规范按钮结构、收敛表单写法）**尚未系统性完成**

**影响范围：** 单据、财务、报表、系统、基础资料、项目、车辆、工单等几乎全部列表与选择弹窗。

---

### 3.4 P1：表格行高/单元格 padding 全局过松且不统一

**位置：** `jshERP-web/src/assets/less/common.less`

```less
.ant-table-tbody .ant-table-row td {
  padding-top: 15px;
  padding-bottom: 15px;
}
```

**问题：**

- 列表普遍 `size="middle"`，再叠加 15px 上下 padding，一屏可视行数偏少（ERP 场景影响效率）
- 部分选择弹窗（如 `JSelectMaterialModal`）又单独改为约 10px，**密度标准不统一**

---

### 3.5 P1：页签样式过度依赖 `!important`

**位置：** `TabLayout.vue` 中 `.tab-layout-tabs.ant-tabs`

对 tab 宽高、边框、背景、关闭按钮显示几乎全面 `!important` 覆盖。

**风险：** 与 AntD4 内部结构强耦合，升级小版本或改 `type` 时易回归；样式文件可读性差，难微调。

---

### 3.6 P2：侧栏宽度与滚动体验

| 项 | 现状 | 问题 |
| --- | --- | --- |
| 侧栏宽度 | 固定 **150px** | 中文业务菜单名 + 图标易挤；Ant 常见 200/208 |
| 菜单滚动条 | WebKit 下倾向隐藏 | 可滚动但缺少可视反馈 |
| 折叠宽度 | 与 content `paddingLeft` 联动写死 150/80 | 改宽度需多处同步，易漏 |

---

### 3.7 P2：弹窗遮罩与高度魔法数

**位置：** `common.less` 等

| 项 | 现状 | 问题 |
| --- | --- | --- |
| 遮罩 | `rgba(0,0,0,0.1)` | 过淡，主界面干扰大、层级感弱 |
| `.ant-modal-cust-warp` | `height: 90%`、`calc(100% - 110px/200px)` | 不同分辨率、全屏模式难对齐 |
| `.depot-mask` | `margin-top: 93px; margin-left: 154px` | 完全绑定旧布局像素，侧栏宽度一变即错 |

---

### 3.8 P2：float 与内联样式技术债

- 单据/财务 Modal 等仍存在 `style="float:left"` 排布工具条、扫码区、金额区
- `views` 内大量内联 style（颜色、间距、宽度）
- 工单列表等业务按钮用内联色值区分状态（可保留产品语义，但宜收成 class/token）

AntD4 表单项多为 flex 布局后，**float 更容易与表单项、栅格错位**。

---

### 3.9 P2：过渡动画类名不一致（小问题）

`TabLayout` 模板：

```vue
<transition name="page-toggle" mode="out-in">
```

样式区定义的是 `.page-transition-enter` 等，**动画实际不生效**。说明模板与样式不同步，属低优先级卫生问题。

---

### 3.10 P3：设计系统与长期债

| 问题 | 说明 |
| --- | --- |
| 无统一 Design Token 文件 | 主色、圆角、字号、间距散落在 `defaultSettings`、Vite less、组件 scoped |
| 登录页浏览器缩放提示 | 按分辨率建议缩放比例，偏桌面客户端思路；Web 端可弱化或可配置关闭 |
| `assets/less/index.less` 沉淀不足 | 列表通用能力多堆在 `GlobalLayout` 超大样式块中 |
| 移动端 | 有 Drawer 侧栏，但表格横向滚动、操作列触控未系统优化 |
| 部分模板格式 | 如工单 `bodyCell` 换行杂乱，不影响渲染但影响审查与后续改样式 |

---

## 4. 优化方向

### 4.1 原则

1. **先框架后页面**：先统一壳子（高度、主题、侧栏），再动 70+ 列表页。  
2. **少改业务逻辑**：本专项以样式与结构收敛为主，不改接口、权限、单据状态机。  
3. **一处定义、处处引用**：高度、主色、侧栏宽用变量/token，禁止魔法数复制。  
4. **兼容 AntD4**：新样式只认 AntD4 DOM 与 API；不再迁就 `control-wrapper` 等已删除结构。  
5. **可回退**：每阶段可独立合并、可截图对比回归。

### 4.2 方向对照表

| 方向 | 对应问题 | 目标状态 |
| --- | --- | --- |
| D1 布局 Token | 3.1 | 全站 Header/Sider/Logo/content 同一套高度与侧栏宽 |
| D2 主题语义 + ConfigProvider | 3.2 | light/dark 名实一致；主色走 token |
| D3 列表搜索组件化 | 3.3 | 单一搜索条约定，去掉三套布局叠加 |
| D4 表格密度 | 3.4 | 全局密度统一，服务 ERP 一屏多行 |
| D5 页签弱覆盖 | 3.5 | 减少 `!important`，保留产品观感 |
| D6 侧栏与滚动 | 3.6 | 宽度合理、滚动可见、折叠联动 |
| D7 弹窗规范 | 3.7 | 遮罩/高度/偏移可预期 |
| D8 去 float、收敛内联 | 3.8 | flex/工具类替代 |
| D9 卫生项 | 3.9、3.10 | 动画类名、文档与 token 沉淀 |

---

## 5. 实施步骤（供排期与验收）

### 阶段 0：准备与基线（0.5 人日）

| 步骤 | 动作 | 产出 |
| --- | --- | --- |
| 0.1 | 确认在 `feature/vue3.0` 上开样式专项分支（建议 `fix/ui-style-cleanup`） | 分支 |
| 0.2 | 固定截图基线：登录、首页、任一带多页签列表（如商品）、单据列表、工单列表、侧栏展开/折叠、一个大弹窗 | 基线图片或录屏 |
| 0.3 | 记录 Node 版本、`npm run serve` 可启动 | 环境说明 |
| 0.4 | 评审通过本文后再动代码（本步骤即审查入口） | 评审意见 |

**验收：** 评审确认优先级与是否砍掉某阶段；基线可对比。

---

### 阶段 1：统一布局高度与侧栏宽度变量（P0，约 1 人日）

**目标：** 消除 49/64 混用；侧栏宽度一处配置。

| 步骤 | 动作 | 建议落点 |
| --- | --- | --- |
| 1.1 | 新增 layout 变量（Less 或 CSS 变量二选一，推荐 CSS 变量便于运行时） | 如 `src/assets/less/layout-tokens.less` 或 `src/styles/tokens.css` |
| 1.2 | 建议取值（可评审调整）：Header/Logo **48px 或 49px 二选一**；Sider 展开宽 **180px 或 200px**；折叠宽 **80px** | 写入 tokens 并注释 |
| 1.3 | 修改 `GlobalHeader.vue`、`Logo.vue`、`SideMenu.vue`、`GlobalLayout.vue`（含 `paddingTop`、`paddingLeft`、drawer `width`）全部引用 tokens | 禁止再写裸 49/64/150 |
| 1.4 | 检查 `fixSiderbar` / `collapsed` 切换时内容区不跳动、不遮挡 | 手工 |

**验收标准：**

- [ ] 固定头模式下，列表首行不被顶栏遮挡  
- [ ] 侧栏展开/折叠，顶栏宽度与内容左边距同步  
- [ ] Logo 区高度与顶栏齐平  
- [ ] 与阶段 0 截图对比，无异常大空白或重叠  

**回退：** 还原 tokens 与上述 4 个布局文件即可。

---

### 阶段 2：主题语义与 ConfigProvider Token（P0，约 1～1.5 人日）

**目标：** light/dark 名实一致；主色统一。

| 步骤 | 动作 |
| --- | --- |
| 2.1 | 在 `App.vue` 的 `a-config-provider` 增加 `:theme`（至少 `token.colorPrimary` 与 `defaultSettings.primaryColor` 对齐） |
| 2.2 | 梳理 `GlobalHeader` / `GlobalLayout` 的 `.light` / `.dark`：使 **dark = 深底浅字，light = 浅底深字**（若产品坚持「主色顶栏」作为默认，应命名为独立 theme 名如 `primary`，避免继续滥用 dark） |
| 2.3 | 去掉顶栏图标 `color: white` 等写死规则，改为跟随主题 token 或父级 `color` 继承 |
| 2.4 | 评估「界面设置」换肤：能改则打通 token；不能一期完成则文档标明限制，避免用户以为已生效 |
| 2.5 | Vite Less `modifyVars` 与 token 主色保持同一来源（可从 `defaultSettings` 导出常量给两边用） |

**验收标准：**

- [ ] 默认主题下顶栏文字、图标、公司名对比度正常  
- [ ] 切换 light/dark（若产品仍提供）后侧栏/顶栏不出现反色错乱  
- [ ] 主按钮、链接、页签 active 与配置主色一致  

---

### 阶段 3：列表搜索区结构收敛（P1，约 2～3 人日，可拆 PR）

**目标：** 结束「三套布局叠加」；落实文档 `23` 的后续阶段。

#### 3.A 公共层（优先）

| 步骤 | 动作 |
| --- | --- |
| 3.A.1 | 抽出 `ListSearchBar`（或等价）组件：插槽放表单项 + 固定「查询/重置/展开」区 |
| 3.A.2 | 公共样式从「强制覆盖 inline」改为「组件根 class 下的明确规则」；逐步减少 `!important` |
| 3.A.3 | 统一 label 宽策略：固定 min-width（如 5～6 个汉字）或全部顶对齐，**禁止**页面再传冲突的 `labelCol` |

#### 3.B 页面迁移（分批）

| 批次 | 范围 | 说明 |
| --- | --- | --- |
| B1 | 系统/基础资料/项目/车辆/工单 | 页少、风险低，验证组件 |
| B2 | 商品 + 报表 | 搜索项较多 |
| B3 | 单据 + 财务列表 | 量最大，单独 PR |
| B4 | 选择弹窗（`JSelect*` 等） | 与列表同构部分一并收 |

**单页改造要点：**

1. 去掉无意义的 `layout="inline"` + 冲突 `labelCol/wrapperCol`（或仅保留组件约定）  
2. 按钮区禁止 `span + float + a-col` 混用  
3. 不改动 `queryParam` 字段与 `searchQuery` 逻辑  

**验收标准：**

- [ ] 抽样 5 个列表：标签与输入框同一基线；查询/重置与输入框垂直对齐  
- [ ] 展开/收起搜索不把表格顶乱  
- [ ] 窗口宽度 1280 / 1920 下无严重挤压或溢出  
- [ ] 查询、重置、回车查询行为与改造前一致  

---

### 阶段 4：表格密度与弹窗基础规范（P1～P2，约 1 人日）

| 步骤 | 动作 |
| --- | --- |
| 4.1 | 调整 `common.less` 全局 td 上下 padding（建议 **8px** 或删除全局覆盖、交给 `size="middle"`） |
| 4.2 | 统一选择弹窗内表格 padding，与列表一致 |
| 4.3 | 遮罩透明度调至约 **0.45**（可配置） |
| 4.4 | `.ant-modal-cust-warp` 改为 `max-height: calc(100vh - xx)` + body 滚动，减少 110/200 魔法数 |
| 4.5 | 审计 `.depot-mask`：改为相对布局容器定位，去掉 93/154 写死边距 |

**验收标准：**

- [ ] 同样分辨率下列表可视行数不少于改造前（宜略增）  
- [ ] 大弹窗在 1366 高度下可完整滚动操作，页脚按钮可见  
- [ ] 遮罩可明显区分模态层  

---

### 阶段 5：页签弱化覆盖 + 侧栏体验（P1～P2，约 1 人日）

| 步骤 | 动作 |
| --- | --- |
| 5.1 | 重写 `tab-layout-tabs`：保留产品需要的「可关闭卡片页签」观感，删除可省的 `!important` |
| 5.2 | 修正 transition：`name` 与 CSS 类前缀一致（`page-toggle` 或 `page-transition` 二选一） |
| 5.3 | 侧栏滚动条改为细条半透明，避免完全 `display:none` |
| 5.4 | 验证多页签右键关闭左/右/其他与样式无冲突 |

**验收标准：**

- [ ] 打开 8+ 页签可横向滚动，关闭按钮 hover 可点  
- [ ] 侧栏菜单过长时可感知滚动  
- [ ] 页签切换无白屏闪断异常（与 keep-alive 行为一致）  

---

### 阶段 6：去 float、收敛内联与状态色（P2，可持续多 PR）

| 步骤 | 动作 |
| --- | --- |
| 6.1 | 单据/财务 Modal 工具条改为 flex 工具类（如 `.bill-toolbar`） |
| 6.2 | 工单状态按钮等业务色抽为 class（如 `.wo-btn-repair`），颜色进 tokens |
| 6.3 | 新增代码规范：禁止新增 `float` 布局；内联 style 仅限动态计算值 |
| 6.4 | （可选）对高频列表做一轮内联 style → class 清理，不要求一次清完 ~950 处 |

**验收标准：**

- [ ] 扫码录入行、金额汇总行在 1280 宽下不换行错乱  
- [ ] 关键 Modal 打开/全屏与改造前功能一致  

---

### 阶段 7：文档与回归（0.5～1 人日）

| 步骤 | 动作 |
| --- | --- |
| 7.1 | 更新 `AGENTS.md` / 前端 README 中与 Vue2、`vue.config.js`、高度魔法数相关的过时描述（若仍有） |
| 7.2 | 在本文件或续篇 `24-...-实施报告.md` 记录实际取值、偏差与未做项 |
| 7.3 | 回归清单（最低集）：登录 → 首页 → 商品列表查询 → 销售出库列表 → 工单列表状态按钮 → 侧栏折叠 → 多页签关闭 → 任一大表单弹窗 → 上传/打印冒烟（若有） |
| 7.4 | 对照阶段 0 截图，归档差异说明 |

---

## 6. 建议排期与依赖

```text
阶段0 准备
  → 阶段1 布局 Token（P0，可独立上线）
  → 阶段2 主题 Token（P0，依赖阶段1 更稳，也可并行但需避免改同一文件冲突）
  → 阶段3 列表搜索组件化（P1，依赖阶段1 的宽度变量）
  → 阶段4 表格/弹窗（可与阶段3 部分并行）
  → 阶段5 页签/侧栏
  → 阶段6 float/内联（持续）
  → 阶段7 文档与回归
```

**建议首期交付（评审若同意「最小可用」）：仅阶段 1 + 2。**  
观感提升明显、改动面可控、不触及 70+ 业务列表逻辑。

---

## 7. 风险与约束

| 风险 | 说明 | 缓解 |
| --- | --- | --- |
| 全局高度一改，插件页/iframe 受影响 | 部分外嵌页按旧 64 假设 | 阶段 1 重点测 iframe 菜单 |
| 主题语义纠正导致「和旧版截图不一样」 | 产品可能习惯主色顶栏叫 light | 评审时锁定默认主题视觉，再决定命名 |
| 列表搜索组件化范围大 | 一次改 76 页易引入查询条件回归 | 分批 B1～B4，每批只回归查询/重置 |
| `!important` 减少后局部回退 | 历史页依赖覆盖 | 组件根 class 提高优先级，而不是全局打补丁 |
| 与后端无关但联调环境依赖 | 无菜单/无数据时看不出表格密度 | 用已有租户账号 + 演示数据 |

**明确不在本期范围（除非评审加码）：**

- 整体换 UI 框架（如改 Element Plus）  
- 全面 Composition API 重写  
- axios 等非样式依赖升级  
- 移动端 App 级响应式重做  

---

## 8. 审查检查表（请评审人勾选）

### 8.1 问题是否认同

- [ ] 同意 P0：高度 49/64 混用需优先处理  
- [ ] 同意 P0：主题 light/dark 语义与 ConfigProvider token 需处理  
- [ ] 同意 P1：列表搜索结构需组件化收敛（可分批）  
- [ ] 同意 P1：表格密度与页签 `!important` 需优化  
- [ ] 同意 P2/P3 项可列入后续迭代  

### 8.2 方案取舍（请批注）

- [ ] Header 高度选定：☐ 48px　☐ 49px　☐ 64px　☐ 其他：______  
- [ ] 侧栏展开宽度选定：☐ 150px 维持　☐ 180px　☐ 200px　☐ 其他：______  
- [ ] 默认顶栏视觉：☐ 主色底（当前习惯）　☐ 白底浅色（标准 light）　☐ 其他：______  
- [ ] 首期是否只做阶段 1+2：☐ 是　☐ 否（要包含阶段：______）  

### 8.3 实施授权

- [ ] 批准按本文阶段实施  
- [ ] 批准但需修改：______________________________  
- [ ] 驳回，原因：______________________________  

| 角色 | 姓名 | 日期 | 签字/备注 |
| --- | --- | --- | --- |
| 撰写 | AI 样式审查 | 2026-07-19 | 基于 `feature/vue3.0` 静态代码审查 |
| 前端评审 |  |  |  |
| 产品/业务评审 |  |  |  |

---

## 9. 关键文件索引

| 文件 | 与审查关系 |
| --- | --- |
| `jshERP-web/src/components/page/GlobalLayout.vue` | 壳子布局、搜索区全局样式、侧栏联动 |
| `jshERP-web/src/components/page/GlobalHeader.vue` | 顶栏高度与主题 class |
| `jshERP-web/src/components/layouts/TabLayout.vue` | 多页签样式与 transition |
| `jshERP-web/src/components/menu/SideMenu.vue` | 侧栏宽度、滚动条 |
| `jshERP-web/src/components/tools/Logo.vue` | Logo 高度与主题背景 |
| `jshERP-web/src/assets/less/common.less` | 表格 padding、弹窗、遮罩、拖拽列 |
| `jshERP-web/src/defaultSettings.js` | 默认主题、主色、layout 开关 |
| `jshERP-web/vite.config.js` | Less modifyVars |
| `jshERP-web/src/App.vue` | ConfigProvider（locale / 待补 theme） |
| `jshERP-web/src/components/legacy/legacy-icon-asn.js` | 菜单图标映射 |
| 典型列表 | `views/material/MaterialList.vue`、`views/workorder/WorkOrderList.vue` |
| 既有问题文档 | `18`～`23` 号 Markdown |

---

## 10. 附录：问题优先级速查

| 优先级 | 编号 | 标题 | 建议阶段 |
| --- | --- | --- | --- |
| P0 | 3.1 | 高度 49/64 混用 | 阶段 1 |
| P0 | 3.2 | 主题语义与双轨主色 | 阶段 2 |
| P1 | 3.3 | 列表搜索三套布局叠加 | 阶段 3 |
| P1 | 3.4 | 表格 padding 过松 | 阶段 4 |
| P1 | 3.5 | 页签过度 !important | 阶段 5 |
| P2 | 3.6 | 侧栏宽度与滚动 | 阶段 1/5 |
| P2 | 3.7 | 弹窗遮罩与魔法数 | 阶段 4 |
| P2 | 3.8 | float 与内联样式 | 阶段 6 |
| P2 | 3.9 | 过渡类名不一致 | 阶段 5 |
| P3 | 3.10 | Token 沉淀、登录缩放提示等 | 阶段 7 及以后 |

---

**文档结束。** 请按第 8 节检查表反馈；确认后可按第 5～6 节排期实施，实施完成后建议另写「实施报告」对照基线验收。
