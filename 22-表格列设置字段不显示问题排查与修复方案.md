# 表格列设置字段不显示问题排查与修复方案

## 1. 摘要

当前列设置弹层出现“只显示恢复默认，不显示正常字段项”的问题。结合现有代码判断，上一轮 `defColumns.title` 的主要错误已修复，本轮问题更可能出在列设置弹层的前端渲染链路。

核心原因集中在：

- `JEllipsis.vue` 在 Vue3 + Ant Design Vue 4 下用 `<a-tooltip>` 包裸文本，字段名可能没有稳定渲染出来。
- `columnSetting.js` 中 `action` 兜底标题仍是乱码。
- `initColumnsSetting()` 仍用 `columnsStr.indexOf(',') > -1` 判断缓存，用户只保留一列时会回退默认列。
- 列设置弹层结构分散在多个页面，缺少统一稳定的字段项容器。

## 2. 修复范围

本次只修复列设置字段显示和列选择缓存问题。

涉及范围：

- 单据模块列设置
- 商品列表列设置
- 报表模块列设置
- 通用列表列设置 mixin
- `JEllipsis` 显示组件
- `columnSetting` 工具方法

不修改：

- 后端接口
- 表格业务字段含义
- 查询条件
- 表格数据渲染逻辑
- 权限逻辑
- 导出逻辑

## 3. 实施步骤

### 步骤 1：修复 `JEllipsis.vue` 渲染结构

目标：确保列设置里的字段名能稳定显示。

建议修改：

```vue
<template>
  <a-tooltip placement="topLeft">
    <template #title>
      <span>{{ value }}</span>
    </template>
    <span class="j-ellipsis-text">{{ ellipsisText(value, length) }}</span>
  </a-tooltip>
</template>
```

说明：

- 不再让 Tooltip 默认插槽直接渲染裸文本。
- 用明确的 `span` 包住字段名。
- 保持 tooltip 仍显示完整字段名。

### 步骤 2：修复列标题兜底乱码

文件：

```text
jshERP-web/src/utils/columnSetting.js
```

将：

```js
action: '鎿嶄綔'
```

改为：

```js
action: '操作'
```

保留：

```js
rowIndex: '#'
```

目标：

- 避免缺少 `title` 的操作列在列设置里显示乱码。
- 保持 `title -> columnTitleMap -> DEFAULT_COLUMN_TITLE_MAP -> dataIndex` 的兜底顺序。

### 步骤 3：修复列设置缓存解析

当前问题：

```js
if (columnsStr && columnsStr.indexOf(',') > -1) {
  this.settingDataIndex = columnsStr.split(',')
} else {
  this.settingDataIndex = [...this.defDataIndex]
}
```

问题点：

- 如果用户只勾选一列，缓存值没有逗号。
- 刷新后会被误判为无缓存，导致恢复默认列。

建议统一改为：

```js
if (typeof columnsStr === 'string' && columnsStr.length > 0) {
  this.settingDataIndex = columnsStr.split(',').filter(Boolean)
} else {
  this.settingDataIndex = [...this.defDataIndex]
}
```

涉及入口：

```text
jshERP-web/src/mixins/JeecgListMixin.js
jshERP-web/src/views/bill/mixins/BillListMixin.js
jshERP-web/src/views/material/MaterialList.vue
```

### 步骤 4：保留非法缓存过滤

继续保留当前逻辑：

```js
sanitizeSettingDataIndex(
  this.settingDataIndex,
  this.defColumns,
  this.defDataIndex
)
```

目标：

- 旧缓存中存在已删除字段时自动过滤。
- 过滤后为空时回退默认列。
- 避免列设置历史数据导致页面列集合异常。

### 步骤 5：统一列设置字段项结构

当前页面中列设置结构大致为：

```vue
<a-checkbox-group>
  <a-row>
    <template v-for="(item,index) in columnSettingColumns">
      <a-col :span="8">
        <a-checkbox :value="item.dataIndex">
          <j-ellipsis :value="getColumnSettingTitle(item)" :length="10"></j-ellipsis>
        </a-checkbox>
      </a-col>
    </template>
  </a-row>
</a-checkbox-group>
```

建议保持结构不大改，只补充稳定 class：

```vue
<a-row class="column-setting-list" style="width: 500px">
  <template v-for="(item,index) in columnSettingColumns" :key="item.dataIndex || index">
    <a-col :span="8" class="column-setting-item">
      <a-checkbox :value="item.dataIndex">
        <j-ellipsis :value="getColumnSettingTitle(item)" :length="10"></j-ellipsis>
      </a-checkbox>
    </a-col>
  </template>
</a-row>
```

目标：

- 确保 `v-for` 有稳定 key。
- 确保字段项容器稳定。
- 不改变现有勾选和保存逻辑。

## 4. 验证方案

### 4.1 静态检查

执行：

```bash
rg -n "鎿嶴綔|请输入|请选择" jshERP-web/src/views jshERP-web/src/utils
```

预期：

- 不应再出现 `鎿嶴綔`
- 列标题中不应再出现 `请输入`、`请选择`

执行：

```bash
rg -n "indexOf\\(', '\\)|indexOf\\(',\\'\\)|indexOf\\(',\\)" jshERP-web/src/mixins jshERP-web/src/views/material
```

预期：

- 列设置缓存解析不再依赖逗号判断。

执行：

```bash
rg -n "getColumnSettingTitle\\(item\\)" jshERP-web/src/views
```

预期：

- 单据、报表、商品列设置继续使用 `getColumnSettingTitle(item)`。

### 4.2 构建验证

执行：

```bash
cd jshERP-web
npm run build
```

预期：

- 构建通过。
- 不新增 Vue 模板编译错误。
- 不新增 AntD4 运行时警告。

### 4.3 页面验证

重点验证页面：

- 零售出库
- 零售退货
- 销售订单
- 销售出库
- 销售退货
- 采购订单
- 采购入库
- 采购退货
- 采购申请
- 其他入库
- 其他出库
- 调拨出库
- 组装单
- 拆卸单
- 商品列表
- 库存报表
- 账户报表
- 销售报表
- 零售报表
- 客户欠款报表
- 供应商欠款报表

每个页面验证：

- 点击“列设置”后能看到正常中文字段名。
- 不再只显示“恢复默认”。
- 勾选/取消字段后表格列即时变化。
- 刷新页面后列设置仍保持。
- 只保留一列后刷新页面仍只显示一列。
- 点击“恢复默认”后恢复默认列。

## 5. 验收标准

完成后应满足：

- 所有列设置弹层字段名正常显示。
- 不出现空白字段项。
- 不出现乱码字段名。
- 不出现 `请输入...`、`请选择...` 作为字段名。
- 单列缓存能正确恢复。
- 废弃 `dataIndex` 缓存不会影响页面。
- `npm run build` 通过。
- 核心单据、商品、报表页面列设置交互正常。

## 6. 默认决策

- `action` 兜底标题统一为 `操作`。
- `rowIndex` 兜底标题统一为 `#`。
- 本次不重构列设置为独立组件，只做最小稳定修复。
- 本次不清空所有用户本地缓存，通过代码兼容旧缓存。
- 本次不修改后端和数据库。
