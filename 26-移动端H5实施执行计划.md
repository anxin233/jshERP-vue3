# 26 - 移动端 H5 实施执行计划（P0~P8 分步执行与测试指南）

> 文档状态：执行契约（后续 AI / 开发 / 测试必须按本文档实施与验收）
> 编写日期：2026-09-13
> 依据文档：[`25-移动端H5开发方案与实施步骤.md`](./25-移动端H5开发方案与实施步骤.md)
> 目标工程：`jshERP-h5/`（待创建）
> 后端：`jshERP-boot/`（**零改动**）；PC 前端：`jshERP-web/`（**零改动**）

---

## 0. 执行规则（强制，先读这一节）

### 0.1 对执行者的要求

1. **严格按 P0 → P8 顺序执行，不得跳步**；每一步必须先通过该步全部测试用例（标 ✅）才能进入下一步。
2. **只允许改动 `jshERP-h5/` 目录**。禁止修改 `jshERP-boot/` 与 `jshERP-web/`。
   - 若确需后端配合，先写入本文档第 11 节"后端改动申请"，经项目负责人确认后再实施。
3. 每一步完成后必须：
   - a. 执行该步全部测试用例，记录结果；
   - b. 更新本文档第 10 节"进度跟踪表"（状态/日期/证据）；
   - c. 按团队规范提交代码（分支见 0.3），提交信息格式见 0.4。
4. 测试用例的"预期结果"必须逐条核对，**不允许"大概没问题"**；失败用例记录到第 9 节缺陷模板并修复后重测。
5. 所有测试过程保留证据：命令输出、接口 JSON、截图、数据库查询结果，存放于 `jshERP-h5/docs/test-evidence/`（该目录随 H5 工程创建）。
6. AI 执行者禁止自行决定"简化实现"或"跳过测试"；范围变更须先更新文档并经确认。

### 0.2 术语与编号

- 步骤编号：`P0`~`P8`；任务编号：`P{步}.{序}`；测试用例编号：`P{步}-T{序}`。
- DoD：Definition of Done（完成定义），每步末尾列明。
- 业务单据缩写：零售出库 `retail_out`、零售退货 `retail_back`、销售出库 `sale_out`、销售退货 `sale_back`、采购入库 `purchase_in`、采购退货 `purchase_back`。

### 0.3 分支与目录约定

- 从当前分支（`feature/vue3.0`）切出工作分支：`feature/h5-mobile`（如团队已有约定，按团队约定）。
- H5 工程位于仓库根目录 `jshERP-h5/`，与 `jshERP-web/` 平级。
- 每阶段至少一次提交；跨阶段不混提交。

### 0.4 提交信息规范（与仓库风格一致，中文）

```text
移动端H5-P0: 初始化 Vue3+Vant 工程与基础配置
移动端H5-P1: 登录认证、请求封装与 Tab 框架
移动端H5-P2: 商品/往来单位/仓库等公共选择组件
移动端H5-P3: 零售出库与零售退货开单
移动端H5-P4: 销售出库与销售退货开单
移动端H5-P5: 采购入库与采购退货开单
移动端H5-P6: 单据中心列表、详情与审核
移动端H5-P7: 库存查询与首页看板
移动端H5-P8: 构建部署、公众号接入与真机回归
```

---

## 1. 环境准备（P0 前置，所有步骤共用）

### 1.1 依赖服务与启动命令

```bash
# 1) 本机数据库/缓存（Colima + Docker）
colima start
cd deploy/dev && docker compose up -d          # MySQL 127.0.0.1:3306 / Redis
# 凭据：MySQL root/jsherp_dev_root，Redis jsherp_dev_redis

# 2) 后端（新终端）
cd jshERP-boot && mvn spring-boot:run          # http://localhost:9999/jshERP-boot

# 3) PC 前端（对照验证用，新终端）
cd jshERP-web && npm install && npm run serve  # http://localhost:3000/erp/

# 4) 移动端 H5（P0 完成后，新终端）
cd jshERP-h5 && npm install && npm run dev     # http://localhost:3001
```

### 1.2 测试账号与登录凭证

| 项目 | 值 |
|---|---|
| 租户/登录名 | `admin` |
| 密码（明文） | `123456` |
| 密码（MD5，接口测试用） | `e10adc3949ba59abbe56e057f20f883e` |
| 验证码开关 | 当前 `checkcode_flag=1`（开启） |

### 1.3 自动化/接口测试时临时关闭验证码（仅测试环境）

```sql
-- 关闭（执行接口自动化前）
UPDATE jsh_erp.jsh_platform_config SET platform_value='0' WHERE platform_key='checkcode_flag';
-- 测试完成后必须恢复
UPDATE jsh_erp.jsh_platform_config SET platform_value='1' WHERE platform_key='checkcode_flag';
```

> 数据库访问：`docker exec -it jsherp-dev-mysql mysql --default-character-set=utf8mb4 -uroot -pjsherp_dev_root jsh_erp`

### 1.4 获取接口测试 token（供 curl 验证使用）

```bash
TOKEN=$(curl -s -X POST http://localhost:9999/jshERP-boot/user/login \
  -H 'Content-Type: application/json' \
  -d '{"loginName":"admin","password":"e10adc3949ba59abbe56e057f20f883e","code":"","uuid":""}' \
  | python3 -c "import json,sys;print(json.load(sys.stdin)['data']['token'])")
echo $TOKEN   # 形如 2d9..._0
# 后续请求带头： -H "X-Access-Token: $TOKEN"
```

### 1.5 测试基础数据（验收前准备，一次性）

| 数据 | 准备方式 | 关键值 |
|---|---|---|
| 仓库 | PC：基础资料 → 仓库信息 | `H5主仓库`（记下 depotId） |
| 商品 A | PC：商品管理 → 商品信息 | 名称 `H5测试商品A`，条码 `TEST0001`，单位 `个`，零售价 10、销售价 9、采购价 6 |
| 商品 A 期初库存 | 商品编辑 → 库存数量 | H5主仓库 期初 `100`，最低安全库存 5 |
| 商品 B（序列号） | 商品信息（可选） | 条码 `TEST0002`，开启序列号开关 |
| 会员 | PC：基础资料 → 会员信息 | `H5测试会员`（记下 id）；后续用"收预付款"充值 200 |
| 客户 | PC：基础资料 → 客户信息 | `H5测试客户` |
| 供应商 | PC：基础资料 → 供应商信息 | `H5测试供应商` |
| 结算账户 | PC：基础资料 → 结算账户 | `H5现金`（记下 id） |
| 经手人 | PC：基础资料 → 经手人管理 | `H5测试员` |

> 数据准备完成后，记录各 id 到 `jshERP-h5/docs/test-evidence/env.md`，全流程测试复用。

### 1.6 环境自检（每次开始测试前）

- [ ] `curl -s http://localhost:9999/jshERP-boot/platformConfig/getPlatform/checkcodeFlag` 返回 `1`
- [ ] PC 端 `http://localhost:3000/erp/` 能登录
- [ ] MySQL/Redis 容器状态 healthy

---

## 2. 总览：步骤、交付物与工期

| 步骤 | 名称 | 主要交付物 | 通过标准（摘要） | 工期 |
|---|---|---|---|---|
| P0 | 工程脚手架 | 可运行的 `jshERP-h5` 空框架 | 3001 可访问、代理通后端、可构建 | 0.5 天 |
| P1 | 认证与框架 | 登录闭环 + Tab 框架 + 请求封装 | 登录保持、失效跳登录、退出/改密可用 | 1~1.5 天 |
| P2 | 公共组件 | 6 个业务选择器 + 扫码输入 | 组件可用、价格分派正确 | 1.5 天 |
| P3 | 零售开单 | 零售出库/退货全流程 | 手机开单 PC 可见、库存/预付款正确 | 2~2.5 天 |
| P4 | 销售开单 | 销售出库/退货全流程 | 欠款/优惠/销售员口径与 PC 一致 | 1.5~2 天 |
| P5 | 采购开单 | 采购入库/退货全流程 | 付款/费用/应付口径与 PC 一致 | 1.5~2 天 |
| P6 | 单据中心 | 列表/详情/审核 | 手机审核 PC 同步、权限显隐正确 | 1.5 天 |
| P7 | 库存与看板 | 库存查询 + 首页统计 | 与 PC 报表/首页一致 | 1 天 |
| P8 | 部署与回归 | 可上线版本 + Nginx + 公众号入口 | 真机全流程闭环 | 1 天 |

> 每个步骤的详细任务、测试用例与"测试通过标准"见第 3 节。

---

## 3. 分步执行计划

### P0 工程脚手架（0.5 天）

**目标**：创建独立 H5 工程并可运行、可构建、能访问后端接口。

**前置**：环境准备第 1.1 节完成。

**任务清单**

- [ ] P0.1 创建工程：`npm create vite@latest jshERP-h5 -- --template vue`，安装依赖：
  `vue-router@4 pinia axios vant dayjs md5`，开发依赖 `less postcss-px-to-viewport-8-plugin unplugin-vue-components @vant/auto-import-resolver`
- [ ] P0.2 `vite.config.js`：`base` 读取 `VITE_APP_PUBLIC_PATH`（默认 `/m/`，开发用 `/`）；dev 端口 **3001**；代理 `/jshERP-boot → http://localhost:9999`；别名 `@`；Vant 组件按需自动导入
- [ ] P0.3 环境变量文件：`.env.development`（`VITE_APP_API_BASE=/jshERP-boot`）、`.env.production`（`VITE_APP_PUBLIC_PATH=/m/`、`VITE_APP_API_BASE=/jshERP-boot`）
- [ ] P0.4 目录骨架按 25 号文档第 4 节创建（router/store/api/utils/constants/components/views/styles）
- [ ] P0.5 样式基础：`styles/variables.less`（主题色 `#1890FF`）、`styles/common.less`（安全区、1px 边框、文本省略）、viewport 适配配置
- [ ] P0.6 路由与骨架页：`/login`、`/home`、`/sale`、`/stock`、`/bills`、`/mine` 六个空页面 + 底部 Tabbar（Vant Tabbar，路由联动）
- [ ] P0.7 代码规范：ESLint 基础配置（可参考 PC 工程，但只对新增文件生效）；`docs/test-evidence/` 目录与 `docs/README.md`

**测试用例**

| 用例 | 前置 | 步骤 | 预期结果 | 结果 |
|---|---|---|---|---|
| P0-T1 | 依赖服务已启动 | `cd jshERP-h5 && npm install` | 安装成功，无报错 | ☐ |
| P0-T2 | P0-T1 | `npm run dev` | 服务启动在 `http://localhost:3001`，浏览器控制台无报错 | ☐ |
| P0-T3 | P0-T2 | `curl -s http://localhost:3001/jshERP-boot/platformConfig/getPlatform/checkcodeFlag` | 返回 `1`（代理生效） | ☐ |
| P0-T4 | P0-T2 | 浏览器打开首页，依次点击 5 个 Tab | 路由切换正常，Tab 高亮正确，无白屏 | ☐ |
| P0-T5 | P0-T1 | `npm run build` | 构建成功生成 `dist/`；`npm run preview` 可访问 | ☐ |

**DoD**：P0-T1~T5 全部 ✅；无控制台报错；目录骨架与 25 号文档一致。

**交付物**：`jshERP-h5/` 可运行框架 + 首次提交。

---

### P1 认证与框架（1~1.5 天）

**目标**：完成登录/退出/改密、请求封装、路由守卫、按钮权限缓存、5 个 Tab 基础页。

**前置**：P0 通过；后端可访问。

**任务清单**

- [ ] P1.1 `api/request.js`：
  - `baseURL = import.meta.env.VITE_APP_API_BASE || '/jshERP-boot'`
  - 请求拦截：有 token 时加 `X-Access-Token`
  - 响应拦截：HTTP 200 返回 `response.data`；**HTTP 500 且响应文本为 `loginOut`** → 清登录态并跳 `/login?redirect=...`；其他错误 Toast
  - 业务统一处理：`code===200` 返回；`code!==200` 时 `Toast(data.message || '操作失败')`；`510` 特殊提示（报表）
- [ ] P1.2 `api/auth.js`：验证码 `GET /user/randomImage`、登录 `POST /user/login`、退出 `POST /user/logout`、修改密码（与 PC 端 `/user/updatePwd` 一致，执行前用 PC 抓包确认路径）
- [ ] P1.3 `store/user.js`（Pinia）：state `token/userId/userInfo/btnStrMap`；actions `login/logout/loadBtnPermissions`；持久化（`utils/storage.js`）
- [ ] P1.4 登录页 `views/login/index.vue`：账号/密码/MD5/验证码（读开关 `checkcodeFlag`，开启时显示并可点击刷新）、错误提示、登录后跳转 `redirect` 或 `/home`；记住登录名（可选）
- [ ] P1.5 路由守卫 `router/index.js`：无 token → `/login`；已登录访问 `/login` → `/home`；设置页面标题
- [ ] P1.6 `utils/permission.js` + 按钮权限：登录后调用 `GET /user/getUserBtnByCurrentUser`，缓存 `{url: btnStr}`；`admin`（返回无 `userBtn` 字段）视为全权限；提供 `hasBtn(url, code)` 方法
- [ ] P1.7 Tabbar 与我的页：用户姓名/角色显示、修改密码、退出登录、版本号（`GET /platformConfig/getPlatform/appVersion`）

**测试用例**

| 用例 | 前置 | 步骤 | 预期结果 | 结果 |
|---|---|---|---|---|
| P1-T1 | 验证码开关=1 | 打开登录页，观察验证码区域 | 显示验证码图片；点击可刷新（uuid 变化） | ☐ |
| P1-T2 | P1-T1 | 输入 admin/错误密码/验证码，登录 | Toast 提示账号或密码错误，不跳转 | ☐ |
| P1-T3 | P1-T1 | 输入 admin/123456/正确验证码，登录 | 跳转首页；本地存储存在 token、userId、userInfo | ☐ |
| P1-T4 | P1-T3 | 刷新浏览器 | 仍在首页（登录态保持），不跳登录页 | ☐ |
| P1-T5 | P1-T3 | 手动清除本地 token 后触发任意接口（如刷新首页触发的接口） | 收到 500 `loginOut` → 清理登录态并跳登录页 | ☐ |
| P1-T6 | P1-T3 | 进入"我的"，查看信息；修改密码为 `123456a`，退出，用新密码登录，再改回 `123456` | 用户信息正确；改密与重新登录成功 | ☐ |
| P1-T7 | P1-T3 | 控制台检查按钮权限缓存 | `btnStrMap` 存在；admin 场景视为全权限（`hasBtn` 返回 true） | ☐ |
| P1-T8 | P1-T3 | 依次切换 5 个 Tab | 页面正常、无报错；登录态全程保持 | ☐ |

**DoD**：P1-T1~T8 全部 ✅；请求拦截的四种分支（成功/业务错误/loginOut/网络错误）均有验证。

**交付物**：可登录的 H5 框架；提交 `移动端H5-P1`。

---

### P2 公共业务组件（1.5 天）

**目标**：完成开单与查询所需的全部选择器组件，供 P3~P5 复用。

**前置**：P1 通过；测试数据已准备（第 1.5 节）。

**任务清单**

- [ ] P2.1 `components/GoodsPicker.vue`：商品搜索（`GET /material/getMaterialByParam?q=`）+ 条码查询（`GET /material/getMaterialByBarCode?barCode=&organId=&depotId=&mpList=&prefixNo=`）；展示名称/规格/单位/库存/价格；支持多选或单选返回
- [ ] P2.2 价格分派逻辑（按单据类型前缀）：零售 `LSCK/LSTH` → `commodityDecimal`；销售 `XSDD/XSCK/XSTH` → `wholesaleDecimal`；采购 `CGDD/CGRK/CGTH` → `purchaseDecimal`；其它入库/出库 → 采购价
- [ ] P2.3 `components/MemberPicker.vue`：统一往来单位选择（会员 `findBySelect_retail`（返回 `advanceIn`）/ 客户 `findBySelect_cus` / 供应商 `findBySelect_sup`），搜索+分页加载
- [ ] P2.4 `components/DepotPicker.vue`：`GET /depot/findDepotByCurrentUser`，默认选中默认仓库
- [ ] P2.5 `components/AccountPicker.vue`：账户列表（与 PC 一致接口 `/account/getAccount`），支持默认账户
- [ ] P2.6 `components/PersonPicker.vue`：经手人/销售员选择（与 PC 一致接口）
- [ ] P2.7 `components/ScanInput.vue`：扫码输入框（自动聚焦、回车回调）+ 摄像头扫码降级（`getUserMedia` + `BarcodeDetector`，不支持时提示手动输入）
- [ ] P2.8 `components/BillStatusTag.vue`：状态标签（0 未审核 / 1 已审核；列表展示用）

**测试用例**

| 用例 | 前置 | 步骤 | 预期结果 | 结果 |
|---|---|---|---|---|
| P2-T1 | 测试数据就绪 | 在演示页 GoodsPicker 输入"测试商品" | 返回商品 A，字段完整（名称/单位/库存/价格） | ☐ |
| P2-T2 | P2-T1 | 输入条码 `TEST0001` 查询 | 返回商品 A 及 H5主仓库 库存 | ☐ |
| P2-T3 | P2-T1 | 分别以 `LSCK/XSCK/CGRK` 查询商品 A 价格 | 依次为 10 / 9 / 6（零售价/销售价/采购价） | ☐ |
| P2-T4 | 会员已建 | 打开 MemberPicker 选择会员 | 显示"H5测试会员"，若能取到预付款则显示余额 | ☐ |
| P2-T5 | 已配置仓库权限（可选） | 打开 DepotPicker | 仅显示当前用户有权限的仓库；默认仓库被选中 | ☐ |
| P2-T6 | 基础数据就绪 | 打开 AccountPicker / PersonPicker | 分别显示 `H5现金` / `H5测试员` | ☐ |
| P2-T7 | 手机浏览器 | 打开 ScanInput，手动输入条码回车 | 回调触发并返回条码；不支持摄像头的环境给出提示 | ☐ |

**DoD**：P2-T1~T7 全部 ✅；组件在独立演示页可复现。

**交付物**：公共组件库；提交 `移动端H5-P2`。

---

### P3 零售开单（核心，2~2.5 天）

**目标**：完成零售出库/退货的完整开单闭环（含保存并审核、编辑回显）。

**前置**：P2 通过。

**任务清单**

- [ ] P3.1 `components/BillForm.vue` 表单引擎（配置驱动）：按 25 号文档 6.3 的 `billType` 配置渲染字段
- [ ] P3.2 `components/BillItemList.vue` 明细：商品行（名称/规格/单位/数量/单价/金额/备注）、数量增减、删除、合计
- [ ] P3.3 零售出库表单：会员（可空）、收款类型（现付/预付款）、收款金额、找零（自动）、仓库、账户、备注、附件
  - 预付款校验：选中会员且 payType=预付款 时，本地先校验 `advanceIn >= 单据金额`，不足给友好提示（后端仍会兜底报错）
- [ ] P3.4 零售退货表单：关联原零售单（可选）、数量/单价、付款金额/找零、账户
- [ ] P3.5 保存逻辑：`POST /depotHead/addDepotHeadAndDetail`，body `{info: JSON.stringify(billMain), rows: JSON.stringify(rows)}`
  - `billMain` 零售出库：`type='出库'`、`subType='零售'`、`organId/payType/accountId/changeAmount/(getAmount/backAmount 仅前端)/operTime/number/remark/fileName/status/totalPrice`
  - `billMain` 零售退货：`type='入库'`、`subType='零售退货'`、`totalPrice` 与 `changeAmount` 取负
  - "保存"= `status:'0'`；"保存并审核"= `status:'1'`（一次请求）
- [ ] P3.6 编辑回显：`GET /depotHead/getDetailByNumber?number=` + `GET /depotItem/getDetailList?headerId=`；仅 `status='0'` 可编辑；更新走 `PUT /depotHead/updateDepotHeadAndDetail`
- [ ] P3.7 明细字段按后端实际读取组装：`barCode/depotId/operNumber/unitPrice/allPrice/sku/batchNumber/expirationDate/snList/remark`
- [ ] P3.8 边界处理：库存不足、低于最低售价、单品金额精度（提交保留两位小数）

**测试用例**

| 用例 | 前置 | 步骤 | 预期结果 | 结果 |
|---|---|---|---|---|
| P3-T1 | 商品 A 库存 100；记下开单前账户余额 | 零售出库：散客、商品 A × 2、现付、保存并审核 | 保存成功；PC 列表出现该单且状态"已审核"；库存 100→98；账户 +20 | ☐ |
| P3-T2 | 会员预付款 200 | 会员 + 预付款收款 20，保存并审核 | 成功；PC 会员预付款余额 −20 | ☐ |
| P3-T3 | 会员预付款 0 | 会员 + 预付款收款 20 | 前端提示余额不足（或后端报"会员预付款余额不足"），单据不保存 | ☐ |
| P3-T4 | — | 收款金额 50、单据金额 20 | 找零显示 30；保存后 PC 显示一致 | ☐ |
| P3-T5 | 存在未审核零售单 | 手机编辑数量 2→3 并保存 | 保存成功；PC 明细数量为 3 | ☐ |
| P3-T6 | 存在已审核零售单 | 尝试编辑 | 编辑入口不可用/提示已审核 | ☐ |
| P3-T7 | 存在已审核零售出库单 | 做零售退货关联该单，数量 1，保存并审核 | 库存 +1；PC 可见退货单，金额为负 | ☐ |
| P3-T8 | 商品 B 库存 0（或数量大于库存） | 开单保存 | 后端返回"库存不足"类错误并 Toast 展示，不产生脏数据 | ☐ |
| P3-T9 | 商品 A 最低售价 8 | 单价改为 5 保存 | 后端返回低于最低售价错误并提示 | ☐ |
| P3-T10 | — | 明细输入 3 行 × 单价 0.1、数量 3（或用 0.1+0.2 场景） | 合计精确为 0.9/两位小数，无浮点尾差 | ☐ |
| P3-T11 | 扫码枪/手动输入 | 输入条码 `TEST0001` 回车 | 自动加一行商品 A，并带出零售价与库存 | ☐ |

**DoD**：P3-T1~T11 全部 ✅；PC 与手机数据一致；`docs/test-evidence/P3/` 留有证据。

**交付物**：零售开单闭环；提交 `移动端H5-P3`。

---

### P4 销售出库 / 销售退货（1.5~2 天）

**目标**：复用表单引擎完成销售单据，口径与 PC 一致。

**前置**：P3 通过。

**任务清单**

- [ ] P4.1 销售出库表单：客户（必填）、明细、优惠率/优惠后金额、本次收款、本次欠款（自动）、扣除订金（有订金时）、其它费用、结算账户、销售人员、备注、附件
- [ ] P4.2 金额口径与 PC 一致：`本次欠款 = 优惠后金额 + 其它费用 − 订金 − 本次收款`
- [ ] P4.3 保存：`type='出库'`、`subType='销售'`；字段名与 PC `SaleOutModal` 保持一致（`discount/discountMoney/discountLastMoney/otherMoney/deposit/changeAmount/debt/salesMan/accountId`）
- [ ] P4.4 销售退货：关联原销售出库（可选）、退货数量不能超过可退数量（前端参考 PC 校验，后端兜底）、金额取负
- [ ] P4.5 关联订单（可选做，若排期紧可标注"暂不支持"并在进度表说明）：出库关联销售订单、回写订单状态由后端自动完成

**测试用例**

| 用例 | 前置 | 步骤 | 预期结果 | 结果 |
|---|---|---|---|---|
| P4-T1 | — | 不选客户直接保存 | 前端提示"请选择客户"，不提交 | ☐ |
| P4-T2 | 商品 A 库存充足 | 客户 + 商品 A × 10、单价 9、优惠率 10% | 优惠后金额 = 81；合计显示正确 | ☐ |
| P4-T3 | P4-T2 | 本次收款 30，保存并审核 | 本次欠款 = 51；PC 列表欠款一致；库存 −10；客户应收 51 | ☐ |
| P4-T4 | 有销售订单且含订金（可选） | 出库时勾选"扣除订金" | 欠款按订金抵扣后计算，PC 一致 | ☐ |
| P4-T5 | — | 选择销售人员 `H5测试员` 保存 | PC 详情显示销售人员 | ☐ |
| P4-T6 | 存在已审核销售出库 | 做销售退货 2 件 | 库存 +2；客户应收减少；PC 可见 | ☐ |
| P4-T7 | — | 对同一商品分别用 PC 与手机开等量同价单 | 两端列表/详情金额与欠款口径一致 | ☐ |

**DoD**：P4-T1~T7 全部 ✅；与 PC 口径逐项比对通过。

**交付物**：销售开单闭环；提交 `移动端H5-P4`。

---

### P5 采购入库 / 采购退货（1.5~2 天）

**目标**：完成采购单据，口径与 PC 一致。

**前置**：P4 通过。

**任务清单**

- [ ] P5.1 采购入库表单：供应商（必填）、明细、优惠率/优惠后金额、本次付款、扣除订金、其它费用、结算账户、备注、附件
- [ ] P5.2 保存：`type='入库'`、`subType='采购'`；字段与 PC `PurchaseInModal` 一致
- [ ] P5.3 采购退货：`type='出库'`、`subType='采购退货'`；关联原采购入库（可选）
- [ ] P5.4 单价默认取采购价（`prefixNo=CGRK`），其它入库/出库同样取采购价

**测试用例**

| 用例 | 前置 | 步骤 | 预期结果 | 结果 |
|---|---|---|---|---|
| P5-T1 | — | 不选供应商直接保存 | 前端提示"请选择供应商" | ☐ |
| P5-T2 | 账户 `H5现金` 余额≥100 | 供应商 + 商品 A × 10、单价 6、本次付款 60、账户现金，保存并审核 | 库存 +10；账户 −60；PC 供应商应付减少 | ☐ |
| P5-T3 | — | 其它费用填 5，保存 | 总额含其它费用；PC 一致 | ☐ |
| P5-T4 | 存在已审核采购入库 | 采购退货 2 件 | 库存 −2；应付减少；PC 可见 | ☐ |
| P5-T5 | — | 与 PC 各开一单比对详情 | 字段与金额口径一致 | ☐ |

**DoD**：P5-T1~T5 全部 ✅。

**交付物**：采购开单闭环；提交 `移动端H5-P5`。

---

### P6 单据中心（1.5 天）

**目标**：三类（六种）单据的列表、详情、审核/反审核；按钮按权限显隐。

**前置**：P3~P5 通过。

**任务清单**

- [ ] P6.1 列表页 `views/bills/index.vue`：类型 Tab（零售出库/零售退货/销售出库/销售退货/采购入库/采购退货）；`GET /depotHead/list`，`search=JSON.stringify({type,subType,status,number,organId,beginTime,endTime})`；分页 `currentPage/pageSize`
- [ ] P6.2 交互：`van-pull-refresh` 下拉刷新 + `van-list` 上拉加载；空状态、加载中、错误重试
- [ ] P6.3 筛选：时间范围（默认本月）、状态（未审核/已审核）、单号、往来单位
- [ ] P6.4 详情页 `views/sale/BillDetail.vue`：主表信息 + 明细 + 附件预览（图片）；金额区（总额/收款/欠款/找零按类型）
- [ ] P6.5 审核/反审核：`POST /depotHead/batchSetStatus`，`{status:'1'|'0', ids:'{id},'}`；二次确认；防重复点击；成功后刷新列表与详情
- [ ] P6.6 按钮权限：审核按钮需 `btnStr` 含 2，反审核含 7；无权限不显示
- [ ] P6.7 单据跳转：详情页提供"编辑"入口（仅未审核且有编辑权限 1）

**测试用例**

| 用例 | 前置 | 步骤 | 预期结果 | 结果 |
|---|---|---|---|---|
| P6-T1 | 已有各类型单据 | 切换 6 个 Tab，对照 PC 同条件列表 | 数量与关键字段一致（单号/往来单位/金额/状态） | ☐ |
| P6-T2 | P6-T1 | 设置时间范围、状态筛选 | 结果与 PC 一致 | ☐ |
| P6-T3 | 列表超过一页 | 下拉刷新、上拉加载 | 数据刷新正确、不重复/不丢行 | ☐ |
| P6-T4 | 任意单据 | 打开详情 | 主表/明细/金额/附件与 PC 一致 | ☐ |
| P6-T5 | 存在未审核单据 | 手机点"审核"并确认 | PC 状态变"已审核"；库存/账目相应更新 | ☐ |
| P6-T6 | 存在已审核单据 | 手机点"反审核"并确认 | 状态回退；库存/账目回退；PC 一致 | ☐ |
| P6-T7 | 使用受限角色账号（无审核权限） | 打开单据列表 | 审核/反审核按钮不显示 | ☐ |
| P6-T8 | — | 快速连点审核按钮 | 仅生效一次，无重复请求/重复弹窗 | ☐ |

**DoD**：P6-T1~T8 全部 ✅；权限显隐正确；操作后 PC 同步无误。

**交付物**：单据中心；提交 `移动端H5-P6`。

---

### P7 库存查询与首页看板（1 天）

**目标**：库存查询（搜索/扫码/仓库筛选）与首页统计卡片、快捷入口。

**前置**：P6 通过。

**任务清单**

- [ ] P7.1 库存页：`GET /material/getListWithStock`（分页），支持关键词、仓库、零库存显隐
- [ ] P7.2 扫码查单仓库存：`GET /depotItem/findStockByDepotAndBarCode?depotId=&barCode=`
- [ ] P7.3 商品库存分布（可选）：`GET /material/getMaterialDepotStock?depotIds=&materialId=`
- [ ] P7.4 首页卡片：`GET /depotHead/getBuyAndSaleStatistics`，显示今日销售/零售、昨日、本月、本年；值为 `'***'` 时按无权限展示
- [ ] P7.5 快捷入口：零售开单、销售出库、采购入库、库存查询、单据中心；按权限（菜单/按钮）过滤
- [ ] P7.6 待办数量（可选）：待审核单据数（可用列表接口 `total` 获取）

**测试用例**

| 用例 | 前置 | 步骤 | 预期结果 | 结果 |
|---|---|---|---|---|
| P7-T1 | 有库存数据 | 库存页搜索"测试商品" | 结果与 PC"报表查询 → 商品库存"一致（数量/单位/仓库） | ☐ |
| P7-T2 | — | 扫码/输入 `TEST0001` 查询 | 显示该商品在当前选中仓库的库存 | ☐ |
| P7-T3 | 多仓库有库存（可选） | 查看库存分布 | 各仓数量与 PC 一致 | ☐ |
| P7-T4 | 当日有已审核单据 | 打开首页 | 今日销售/零售金额与 PC 首页一致；无价格权限显示 `***` | ☐ |
| P7-T5 | 受限角色 | 打开首页 | 快捷入口按权限过滤，不显示无权限入口 | ☐ |

**DoD**：P7-T1~T5 全部 ✅。

**交付物**：库存与首页；提交 `移动端H5-P7`。

---

### P8 构建部署与真机联调（1 天）

**目标**：产出可上线版本，接入公众号菜单，真机全流程验收。

**前置**：P0~P7 全部通过；具备 HTTPS 域名与公众号（无则先本地/内网验收）。

**任务清单**

- [ ] P8.1 `npm run build` 产出 `dist/`；确认 `base=/m/` 资源路径正确
- [ ] P8.2 Nginx 配置（参考 25 号文档 3.4）：`/m/` 静态 + history 回退；`/jshERP-boot/` 反代；开启 gzip
- [ ] P8.3 公众号后台：自定义菜单挂 `https://域名/m/`；配置业务域名/JS 安全域名（如后续用 JS-SDK）
- [ ] P8.4 真机回归：iOS 微信、Android 微信、Safari、Chrome 全流程
- [ ] P8.5 弱网/断网：提示与重试
- [ ] P8.6 回滚方案：保留上一版 `dist` 备份，异常时切换目录
- [ ] P8.7 上线检查清单核对（第 8 节）

**测试用例**

| 用例 | 前置 | 步骤 | 预期结果 | 结果 |
|---|---|---|---|---|
| P8-T1 | — | `npm run build` | 构建成功，无报错；`dist/index.html` 引用路径以 `/m/` 开头 | ☐ |
| P8-T2 | Nginx 已部署 | 手机浏览器访问 `https://域名/m/` | 正常加载，无 404/白屏 | ☐ |
| P8-T3 | P8-T2 | 登录并调用任意接口 | 接口同域，无跨域错误 | ☐ |
| P8-T4 | 公众号配置完成 | 从公众号菜单进入 | 正常打开 H5 | ☐ |
| P8-T5 | iOS 微信 | 登录 → 零售开单 → 保存并审核 → 单据中心审核 | 全流程成功，无键盘遮挡/无法点击问题 | ☐ |
| P8-T6 | Android 微信 | 同 P8-T5 | 全流程成功 | ☐ |
| P8-T7 | 飞行模式 | 打开页面/提交单据 | 有明确错误提示，可重试，不白屏 | ☐ |
| P8-T8 | — | 按回滚方案回退版本 | 1 分钟内恢复上一版本 | ☐ |

**DoD**：P8-T1~T8 全部 ✅；上线检查清单全部勾选。

**交付物**：上线版本 + 部署文档 + 真机回归报告；提交 `移动端H5-P8`。

---

## 4. 全局测试规范

### 4.1 功能测试矩阵（每阶段结束回归一次）

| 模块 | 检查点 |
|---|---|
| 登录 | 验证码、错误提示、登录保持、过期跳转、退出 |
| 零售 | 散客/会员、现付/预付、找零、退货、库存/余额一致 |
| 销售 | 客户必填、优惠、订金、收款/欠款、销售员、退货 |
| 采购 | 供应商必填、付款、其它费用、退货、应付一致 |
| 单据中心 | 列表/筛选/分页/详情/审核/反审核/权限 |
| 库存 | 搜索/扫码/仓库筛选/分布 |
| 首页 | 统计口径、`***` 权限、快捷入口过滤 |

### 4.2 兼容性测试矩阵

| 环境 | 版本要求 | 必测 |
|---|---|---|
| iOS 微信内置浏览器 | 最新版 | 是 |
| Android 微信内置浏览器 | 最新版 | 是 |
| iOS Safari | 最新版 | 是 |
| Android Chrome | 最新版 | 是 |
| 小屏设备 | iPhone SE（375） | 是 |
| 大屏设备 | iPhone Pro Max / Android 大屏（≥414） | 是 |

### 4.3 数据一致性验证方法（每类单据必做）

1. 手机开单/审核后，到 PC 端对应列表查看（单号、金额、状态、往来单位）；
2. 库存核对：PC"报表查询 → 商品库存" 或接口 `/depotItem/findStockByDepotAndBarCode`；
3. 往来账核对：PC"报表查询 → 客户对账 / 供应商对账"；
4. 账户核对：PC"报表查询 → 账户统计"；
5. 若不一致，按第 9 节缺陷模板记录并定位（前端参数 / 后端行为 / 数据问题）。

### 4.4 测试数据与清理

- 测试数据统一带 `H5测试` 前缀，便于识别；
- 单据测试产生的数据**不删除**（保留对账证据），在 `docs/test-evidence/` 记录单号；
- 如需重跑干净用例，使用新建商品/新会员，避免污染既有数据。

---

## 5. 缺陷记录模板（发现即填，位于 `jshERP-h5/docs/bugs.md`）

```markdown
## BUG-{序号}
- 发现步骤（P{步}）：
- 环境：iOS/Android/浏览器 + 版本
- 前置数据：
- 复现步骤：
- 实际结果：
- 预期结果：
- 证据（截图/网络请求/响应 JSON）：
- 定位结论：
- 修复提交：
- 回归结果：
```

---

## 6. 完成定义（DoD）总则

一个功能被视为"完成"，必须同时满足：

1. 功能按 25 号文档与本文档实现，无自创范围；
2. 对应测试用例全部通过并勾选；
3. 无控制台报错、无阻断性 lint 错误（新增文件）；
4. 弱网/错误路径有提示；
5. 与 PC 端数据一致（涉及业务数据的改动）；
6. 文档进度表已更新，证据已归档。

---

## 7. 风险与应对（执行层面）

| 风险 | 触发场景 | 应对 |
|---|---|---|
| 验证码导致接口自动化困难 | CI/脚本测试 | 临时关闭 `checkcode_flag`，测试后恢复（第 1.3 节） |
| token 3 天过期 | 长时间测试 | 拦截 `loginOut` 自动跳登录；自动化脚本重登 |
| 后端字段/接口与文档不符 | 执行中发现 | 以实际接口为准，**先更新 25/26 号文档**再继续 |
| 金额精度 | 小数运算 | 统一两位小数；必要时引入 decimal.js |
| iOS 微信键盘/安全区 | 真机 | 表单页避免固定布局；P8 专项回归 |
| 权限差异 | 不同账号 | 使用受限角色账号专项测试（P6-T7/P7-T5） |
| 数据被测试污染 | 多人并行 | 统一 `H5测试` 前缀；按证据留痕 |

---

## 8. 上线检查清单（P8 逐项勾选）

- [ ] `npm run build` 成功，产物完整
- [ ] `base=/m/`、API 前缀 `/jshERP-boot` 正确
- [ ] Nginx `/m/` 静态与 history 回退正常
- [ ] API 反代正常（无跨域、登录可通）
- [ ] HTTPS 证书有效（公众号要求）
- [ ] 公众号菜单链接配置并生效
- [ ] iOS/Android 微信真机全流程通过
- [ ] 验证码开关状态确认（生产按管理要求）
- [ ] 回滚方案与备份就绪
- [ ] 测试证据归档完成，进度表全部 ✅

---

## 9. 进度跟踪表（执行者每步更新）

| 步骤 | 状态 | 执行人 | 开始日期 | 完成日期 | 测试证据 | 备注 |
|---|---|---|---|---|---|---|
| P0 脚手架 | ✅ 已完成 | AI | 2026-09-13 | 2026-09-13 | `jshERP-h5/docs/test-evidence/P0.md` | 5 用例全过；dev 服务 3001 运行中；构建产物 `/m/` 验证通过 |
| P1 认证与框架 | ☐ 未开始 | | | | | |
| P2 公共组件 | ☐ 未开始 | | | | | |
| P3 零售开单 | ☐ 未开始 | | | | | |
| P4 销售开单 | ☐ 未开始 | | | | | |
| P5 采购开单 | ☐ 未开始 | | | | | |
| P6 单据中心 | ☐ 未开始 | | | | | |
| P7 库存与看板 | ☐ 未开始 | | | | | |
| P8 部署与回归 | ☐ 未开始 | | | | | |

> 状态取值：☐ 未开始 / 🟡 进行中 / ✅ 已完成 / ⛔ 阻塞（阻塞需在备注写明原因与责任人）

---

## 10. 后端改动申请（默认空；确需改动时填写）

| 序号 | 申请步骤 | 改动内容 | 原因 | 影响面 | 负责人结论 |
|---|---|---|---|---|---|
| （无） | | | | | |

---

## 11. 常见问题排查（执行参考）

| 现象 | 排查方向 |
|---|---|
| `npm run dev` 端口占用 | 改端口或 `lsof -i:3001` 结束占用进程 |
| 接口 404 | 确认 `VITE_APP_API_BASE=/jshERP-boot`、代理是否生效（P0-T3） |
| 接口 500 且返回 `loginOut` | token 过期；重新登录 |
| 接口 400 | `search` 参数白名单/JSON 格式问题；对照 PC 同请求参数 |
| 保存单据报错"库存不足" | 检查仓库、数量、是否已开启负库存 |
| 保存单据报错"会员预付款余额不足" | 校验预付款余额或改用现付 |
| 明细保存后 PC 看不到某字段 | 对照 `DepotItemService` 读取字段，检查 `rows` 组装 |
| 手机端金额与 PC 差 0.01 | 前端浮点误差；改整数分/字符串计算 |
| iOS 页面底部遮挡 | tabbar 安全区适配 `env(safe-area-inset-bottom)` |
| 微信内扫码不可用 | 一期使用扫码枪/手动输入降级；二期接 BFF 的 `wx.scanQRCode` |

---

## 12. 变更记录

| 日期 | 版本 | 内容 |
|---|---|---|
| 2026-09-13 | v1.0 | 初稿：按 25 号方案拆解 P0~P8 执行步骤、任务、测试用例与通过标准；作为后续 AI 与人的执行/验收契约 |
