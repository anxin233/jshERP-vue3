# 列表查询异常排查与解决方案

## 1. 排查结论

本次“数据查询异常”不是单一问题导致。结合前端列表查询代码、后端异常日志和本地接口验证，当前主要存在 4 类问题：

1. 部分单据列表没有传 `subType`，后端 `DepotHeadService.getBillCategory()` 对空值无保护，触发空指针，返回 `数据查询异常`。
2. 商品类别列表 SQL 仍引用 `offset/rows`，但 Mapper 方法没有该参数，触发 MyBatis `BindingException`。
3. 商品类别树接口前端传 `id=''`，后端参数是 `Long id`，空字符串转换失败。
4. 平台级列表接口对非 `admin` 用户返回 `null`，Controller 再执行分页封装时触发空指针，返回 `未知异常`。

其中第 1、2 类会直接出现前端提示“数据查询异常”。第 3、4 类表现为列表或树加载失败，前端可能显示“未知异常”或页面数据异常。

## 2. 前端提示链路

前端通用列表查询主要来自：

- `jshERP-web/src/mixins/JeecgListMixin.js`
- `jshERP-web/src/views/bill/mixins/BillListMixin.js`

核心逻辑是：

```js
getAction(this.url.list, params).then((res) => {
  if (res.code === 200) {
    // 正常渲染 rows/total
  } else {
    this.$message.warning(res.data.message)
  }
})
```

后端 `JshException.readFail()` 会把查询异常包装为：

```text
code = 300
message = 数据查询异常
```

因此前端看到的“数据查询异常”，实际来源是后端服务层捕获异常后抛出的业务错误，不是 Ant Design Vue 的前端组件问题。

## 3. 已验证接口结果

使用本地后端 `http://127.0.0.1:9999/jshERP-boot` 和登录用户 `jsh` 实测，代表性接口结果如下：

| 接口 | 结果 | 判断 |
| --- | --- | --- |
| `/depotHead/list`，`search={}` | `code=300` | 异常，缺少 `subType` 会触发后端空指针 |
| `/depotHead/list`，带 `type/subType` | `code=200` | 正常 |
| `/material/list` | `code=200` | 正常 |
| `/supplier/list` | `code=200` | 正常 |
| `/account/list` | `code=200` | 正常 |
| `/depot/list` | `code=200` | 正常 |
| `/person/list` | `code=200` | 正常 |
| `/inOutItem/list` | `code=200` | 正常 |
| `/materialCategory/list` | `code=300` | 异常，Mapper XML 参数不匹配 |
| `/materialCategory/getMaterialCategoryTree?id=` | `code=500` | 异常，空字符串无法转换为 Long |
| `/project/list` | `code=200` | 正常 |
| `/vehicle/list` | `code=200` | 正常 |
| `/workOrder/list` | `code=200` | 正常 |
| `/option/manage/list` | `code=200` | 正常 |
| `/option/group/manage/list` | `code=200` | 正常 |
| `/dict/type/list` | `code=200` | 正常 |
| `/dict/data/list` | `code=200` | 正常 |
| `/function/list`，非 admin 用户 | `code=500` | 异常，服务返回 null 后分页封装空指针 |
| `/tenant/list`，非 admin 用户 | `code=500` | 异常，服务返回 null 后分页封装空指针 |
| `/platformConfig/list`，非 admin 用户 | `code=500` | 异常，服务返回 null 后分页封装空指针 |

## 4. 根因一：部分单据列表缺少 subType

### 4.1 问题现象

后端日志显示：

```text
DepotHeadService - 异常码[300],异常提示[数据查询异常]
java.lang.NullPointerException
at com.jsh.erp.service.DepotHeadService.getBillCategory(DepotHeadService.java:1740)
at com.jsh.erp.service.DepotHeadService.select(DepotHeadService.java:115)
```

`DepotHeadService.getBillCategory(String subType)` 内部直接执行 `subType.equals(...)`，当 `subType` 为空时会触发空指针。

### 4.2 受影响页面

扫描 `jshERP-web/src/views/bill/**/*List.vue` 后，以下页面使用 `/depotHead/list`，但初始化 `queryParam` 中缺少 `subType`：

| 文件 | 影响页面 |
| --- | --- |
| `jshERP-web/src/views/bill/PurchaseInList.vue` | 采购入库 |
| `jshERP-web/src/views/bill/RetailOutList.vue` | 零售出库 |
| `jshERP-web/src/views/bill/SaleBackList.vue` | 销售退货 |
| `jshERP-web/src/views/bill/SaleOutList.vue` | 销售出库 |

这些页面打开列表时会请求 `/depotHead/list`，后端收到 `subType=null`，最终返回 `数据查询异常`。

### 4.3 验证结果

同一个接口在补充 `subType` 后可以正常返回：

| 查询参数 | 结果 |
| --- | --- |
| `{"type":"出库","subType":"零售"}` | `code=200` |
| `{"type":"入库","subType":"采购"}` | `code=200` |
| `{"type":"出库","subType":"销售"}` | `code=200` |
| `{"type":"入库","subType":"销售退货"}` | `code=200` |

### 4.4 解决方案

前端必须修复：

- 在 `PurchaseInList.vue` 中补充 `queryParam.subType = '采购'`。
- 在 `RetailOutList.vue` 中补充 `queryParam.subType = '零售'`。
- 在 `SaleOutList.vue` 中补充 `queryParam.subType = '销售'`。
- 在 `SaleBackList.vue` 中补充 `queryParam.subType = '销售退货'`。

后端建议同步兜底：

- `DepotHeadService.getBillCategory(String subType)` 增加空值判断。
- 如果 `subType` 为空，应返回空字符串、默认分类，或直接抛出明确参数错误。
- 不建议继续让空指针进入 `JshException.readFail()`，否则排查成本高。

## 5. 根因二：商品类别列表 Mapper 参数不匹配

### 5.1 问题现象

调用 `/materialCategory/list` 返回 `code=300`，后端日志显示：

```text
org.apache.ibatis.binding.BindingException:
Parameter 'offset' not found. Available parameters are [name, param1, parentId, param2]
```

### 5.2 代码原因

`MaterialCategoryService.select()` 已经使用：

```java
PageUtils.startPage();
```

但 `MaterialCategoryMapperEx.xml` 中仍保留手写分页：

```xml
<if test="offset != null and rows != null">
    limit #{offset},#{rows}
</if>
```

对应 Mapper 方法只有：

```java
selectByConditionMaterialCategory(@Param("name") String name, @Param("parentId") Integer parentId)
```

方法没有 `offset` 和 `rows` 参数，所以 MyBatis 解析 SQL 时直接报错。

### 5.3 解决方案

推荐方案：

- 删除 `MaterialCategoryMapperEx.xml` 中对 `offset/rows` 的手写分页片段。
- 保留 `PageUtils.startPage()`，让 PageHelper 统一接管分页。

不推荐方案：

- 给 Mapper 方法补 `offset/rows` 参数。

不推荐原因：

- 当前项目多数列表已经使用 PageHelper。
- 同时存在 PageHelper 和手写 `limit` 容易造成双重分页或总数统计异常。

## 6. 根因三：商品类别树 id 参数类型不一致

### 6.1 问题现象

商品类别页面加载树时，前端传参：

```js
let params = {}
params.id = ''
queryMaterialCategoryTreeList(params)
```

后端接口定义：

```java
getMaterialCategoryTree(@RequestParam("id") Long id)
```

空字符串无法转换为 `Long`，因此请求 `/materialCategory/getMaterialCategoryTree?id=` 会返回 `500`。

### 6.2 解决方案

推荐前后端同时收敛：

- 前端不要传空字符串，根节点可以传 `0`、`1`，或不传 `id`。
- 后端把 `id` 改为非必填参数，并对 `null` 设置默认根节点。

建议后端形态：

```java
@RequestParam(value = "id", required = false) Long id
```

然后在方法内部统一处理：

```java
if (id == null) {
    id = 0L;
}
```

具体默认值应以当前商品类别根节点设计为准。

## 7. 根因四：平台级列表非 admin 返回 null

### 7.1 问题现象

使用普通租户用户 `jsh` 调用以下接口时返回 `500`：

- `/function/list`
- `/tenant/list`
- `/platformConfig/list`

后端日志显示空指针发生在：

```text
BaseController.getDataTable(BaseController.java:91)
PageInfo.<init>
```

### 7.2 代码原因

`FunctionService.select()`、`TenantService.select()`、`PlatformConfigService.select()` 的逻辑类似：

```java
List<?> list = null;
if (BusinessConstants.DEFAULT_MANAGER.equals(userService.getCurrentUser().getLoginName())) {
    PageUtils.startPage();
    list = mapper.selectByCondition(...);
}
return list;
```

当当前用户不是默认管理员 `admin` 时，服务直接返回 `null`。

Controller 随后执行：

```java
return getDataTable(list);
```

`BaseController.getDataTable(null)` 会触发 `PageInfo` 空指针。

### 7.3 解决方案

建议按权限语义二选一：

方案 A：非 admin 不允许访问平台级列表。

- 前端菜单权限不向普通租户用户开放这些页面。
- 后端接口返回明确的权限错误，例如 `403` 或项目内统一的无权限业务码。

方案 B：非 admin 可以访问，但只能看到空列表。

- 服务层不要返回 `null`，改为返回 `Collections.emptyList()`。
- `BaseController.getDataTable()` 增加空列表兜底，避免任何服务返回 `null` 时导致 NPE。

更稳的做法是两者结合：

- 权限层控制入口。
- Controller/BaseController 做防御式兜底。

## 8. 已排除或非当前主因的问题

### 8.1 数据库字段缺失不是当前主因

当前本地库已执行 Flyway 到 V10/V11 相关修复，`jsh_depot_head.debt`、`jsh_depot_head.last_debt` 等字段已存在。

本轮后端日志没有出现 `Unknown column debt` 或 `Unknown column last_debt`，所以单据列表“数据查询异常”的主因不是字段缺失，而是 `subType` 为空。

### 8.2 AntD4 表格排序参数不是当前主因

前端 `JeecgListMixin.handleTableChange()` 仍使用：

```js
this.isorter.column = sorter.field
this.isorter.order = sorter.order
```

而后端 `PageUtils` 通常读取的是 `orderByColumn/isAsc`。

这会导致排序参数不统一，属于后续需要优化的问题，但不会导致当前 `数据查询异常`。

### 8.3 选项中心和字典接口路径本身可用

实测以下路径返回正常：

- `/option/manage/list`
- `/option/group/manage/list`
- `/dict/type/list`
- `/dict/data/list`

因此选项中心、字典管理当前不是本轮“好多列表查询异常”的主要根因。

## 9. 建议实施步骤

### 第一步：修复单据列表 subType

优先修改 4 个受影响页面：

- `PurchaseInList.vue`
- `RetailOutList.vue`
- `SaleOutList.vue`
- `SaleBackList.vue`

补齐对应 `queryParam.subType` 后，验证：

- 采购入库列表
- 零售出库列表
- 销售出库列表
- 销售退货列表

验收标准：

- 页面打开不再提示“数据查询异常”。
- 请求 `/depotHead/list` 时 `search` 中包含正确 `type/subType`。
- 后端日志不再出现 `DepotHeadService.getBillCategory` 空指针。

### 第二步：给 DepotHeadService 增加后端兜底

在 `getBillCategory(String subType)` 增加空值保护。

验收标准：

- 即使前端漏传 `subType`，后端也不再空指针。
- 返回结果语义明确，可以是空列表或明确参数错误。

### 第三步：修复商品类别列表分页 SQL

删除 `MaterialCategoryMapperEx.xml` 中的 `offset/rows` 手写分页片段。

验收标准：

- `/materialCategory/list` 返回 `code=200`。
- 商品类别列表分页、查询正常。
- 后端日志不再出现 `Parameter 'offset' not found`。

### 第四步：修复商品类别树 id 参数

前端不再传 `id=''`，后端允许 `id` 为空并设默认根节点。

验收标准：

- 商品类别页面左侧树正常加载。
- `/materialCategory/getMaterialCategoryTree` 不再因空字符串返回 500。

### 第五步：处理平台级列表权限和 null list

针对 `/function/list`、`/tenant/list`、`/platformConfig/list`：

- 明确这些页面是否只允许 `admin` 使用。
- 如果只允许 `admin`，前端菜单权限和后端接口都应阻止普通用户访问。
- 如果允许普通用户访问，服务层应返回空列表或按租户过滤后的列表，不能返回 `null`。
- `BaseController.getDataTable()` 建议增加 `null` 兜底。

验收标准：

- 普通用户访问时不再出现 `PageInfo` 空指针。
- 页面行为符合权限设计。

### 第六步：后续优化列表排序参数

统一前端排序参数与后端 `PageUtils`：

- 前端输出 `orderByColumn`、`isAsc`。
- 或后端兼容 `column/order`。

验收标准：

- 点击表格列排序后，接口参数和后端排序逻辑一致。
- 不影响分页和查询条件。

## 10. 回归验证清单

接口验证：

```text
/depotHead/list
/materialCategory/list
/materialCategory/getMaterialCategoryTree
/function/list
/tenant/list
/platformConfig/list
```

页面验证：

- 零售出库
- 采购入库
- 销售出库
- 销售退货
- 商品类别
- 菜单管理
- 租户管理
- 平台参数配置

后端日志验证：

- 不再出现 `DepotHeadService.getBillCategory` 空指针。
- 不再出现 `Parameter 'offset' not found`。
- 不再出现 `BaseController.getDataTable` / `PageInfo` 空指针。

构建验证：

```bash
cd jshERP-web
npm run build
```

启动验证：

```bash
cd jshERP-web
npm run serve
```

## 11. 优先级建议

优先级排序如下：

1. 单据列表 `subType` 缺失，影响业务列表打开，优先级最高。
2. 商品类别 Mapper 分页参数错误，属于确定性后端 bug。
3. 商品类别树 `id` 空字符串问题，影响商品类别页面完整加载。
4. 平台级列表非 admin 返回 null，需要结合权限设计处理。
5. 表格排序参数统一，属于后续质量优化。

---

## 12. 实施记录（2026-05）

已按本文档步骤 1–5 及补充项完成代码修改：

| 步骤 | 改动 |
|------|------|
| 1 | `PurchaseInList` / `RetailOutList` / `SaleOutList` / `SaleBackList` 补全 `queryParam.subType`；`AllocationOutList` 修正 `subType: "调拨"` |
| 2 | `DepotHeadService.getBillCategory()` 空值保护 + 字面量 `.equals` |
| 3 | `MaterialCategoryMapperEx.xml` 删除 `offset/rows` 手写 limit |
| 4 | 商品类别树：`MaterialCategoryController` 的 `id` 改为可选；`MaterialCategoryList` / `MaterialList` / `MaterialModal` 不再传 `id=''` |
| 5 | `FunctionService` / `TenantService` / `PlatformConfigService.select()` 非 admin 返回 `Collections.emptyList()`；`BaseController.getDataTable()` 对 `null` 兜底 |

**验证**：`npm run build`、`mvn compile` 已通过。请重启后端后按第 10 节回归清单验收页面与接口。

