# 管伊佳ERP 系统操作手册

> 本手册面向系统管理员和租户用户，帮助零基础用户快速上手操作本系统。

---

## 目录

- [一、系统简介](#一系统简介)
- [二、登录与账户](#二登录与账户)
- [三、管理员操作指南](#三管理员操作指南)
- [四、租户操作指南](#四租户操作指南)
- [五、常见问题](#五常见问题)

---

## 一、系统简介

管伊佳ERP（原华夏ERP）是一套开源的进销存 + 财务 + 工单管理系统，支持多租户模式，即一套系统可以同时服务多家企业，各企业数据完全隔离、互不可见。

### 系统角色说明

| 角色 | 说明 | 登录方式 |
|------|------|----------|
| **平台管理员** | 管理所有租户、系统配置、功能菜单等，不参与具体业务操作 | 使用管理员账号登录 |
| **租户管理员** | 某个企业的最高权限用户，可管理本企业的所有业务数据和内部用户 | 输入租户编号 + 用户名 + 密码 |
| **租户普通用户** | 由租户管理员创建，拥有部分功能权限 | 输入租户编号 + 用户名 + 密码 |

### 功能模块总览

```
管伊佳ERP
├── 首页仪表盘          ← 数据概览
├── 零售管理            ← 零售出库、零售退货
├── 采购管理            ← 请购单、采购订单、采购入库、采购退货
├── 销售管理            ← 销售订单、销售出库、销售退货
├── 仓库管理            ← 其它入库/出库、调拨、组装/拆卸
├── 财务管理            ← 收入单、支出单、收款单、付款单、转账单、收预付款
├── 报表查询            ← 各类统计报表（库存、采购、销售、账户、对账等）
├── 商品管理            ← 商品类别、商品信息、多单位、多属性
├── 基础资料            ← 供应商、客户、会员、客户车辆、仓库、收支项目、结算账户、经手人
├── 项目管理            ← 项目类别、项目信息
├── 工单管理            ← 工单信息（含车辆选择、项目/商品关联、结算收款）
└── 系统管理            ← 角色、用户、机构、日志、配置、功能管理、租户管理、选项管理
```

---

## 二、登录与账户

### 2.1 访问系统

- 在浏览器中打开系统地址（默认为 `http://localhost:3000`）
- 进入登录页面

### 2.2 登录操作

1. **租户编号**：填写您所属企业的租户编号（如 `jsh`）
2. **用户名**：填写您的用户名（如 `admin`）
3. **密码**：填写密码（初始密码 `123456`）
4. 点击 **「登录」** 按钮

> **默认账户**：租户 `jsh`，用户名 `admin`，密码 `123456`

### 2.3 修改密码

登录后，点击页面右上角的用户头像 → 选择「修改密码」→ 输入旧密码和新密码 → 点击确定。

---

## 三、管理员操作指南

> 以下内容面向**平台管理员**，用于管理整个系统的基础设置。

### 3.1 租户管理

**位置**：系统管理 → 租户管理

租户即使用本系统的企业单位，每个租户的数据完全隔离。

| 操作 | 步骤 |
|------|------|
| **新增租户** | 点击「新增」→ 填写租户编号、企业名称、联系人信息、到期时间、用户数上限 → 点击「确定」 |
| **编辑租户** | 在列表中找到目标租户 → 点击「编辑」→ 修改信息 → 保存 |
| **禁用/启用租户** | 点击「编辑」→ 切换启用状态 → 保存 |
| **删除租户** | 选中租户 → 点击「删除」→ 确认（删除后该租户下所有数据不可恢复，请谨慎操作） |

### 3.2 功能管理

**位置**：系统管理 → 功能管理

管理系统的菜单结构和按钮权限。

- **查看菜单树**：点击「树形展示」可以看到完整的菜单层级
- **编辑菜单**：点击某个菜单的「编辑」按钮，可修改名称、排序、图标、路径等
- **新增菜单**：点击「新增」，填写菜单编号、名称、上级菜单、路由地址等

> 注意：修改菜单后需要退出重新登录才能看到变化。

### 3.3 角色管理

**位置**：系统管理 → 角色管理

角色用来控制用户可以看到哪些菜单和按钮。

| 操作 | 步骤 |
|------|------|
| **新增角色** | 点击「新增」→ 输入角色名称和描述 → 保存 |
| **分配菜单权限** | 点击角色行的「菜单权限」按钮 → 勾选该角色可访问的菜单 → 保存 |
| **分配按钮权限** | 点击角色行的「按钮权限」按钮 → 勾选该角色可使用的操作按钮（新增、编辑、删除等）→ 保存 |

### 3.4 用户管理

**位置**：系统管理 → 用户管理

| 操作 | 步骤 |
|------|------|
| **新增用户** | 点击「新增」→ 填写用户名、登录名、密码、所属角色 → 保存 |
| **编辑用户** | 点击「编辑」→ 修改用户信息 → 保存 |
| **重置密码** | 点击「重置密码」→ 输入新密码 → 确认 |
| **禁用用户** | 点击「编辑」→ 将状态设为禁用 → 保存 |

### 3.5 机构管理

**位置**：系统管理 → 机构管理

用于设置企业的组织架构（部门），支持多层级。

- 点击「新增」添加部门
- 支持拖拽调整部门层级
- 可将用户关联到对应部门

### 3.6 系统配置

**位置**：系统管理 → 系统配置

可配置的系统参数包括：

- 公司名称、仓库启用标记
- 是否强制审核、多级审核
- 积分等级设置
- 其他业务开关选项

### 3.7 选项管理

**位置**：系统管理 → 选项管理

管理系统中各种下拉选项（如工单状态、客户来源、车辆用途、派工人员等）。

| 操作 | 步骤 |
|------|------|
| **查看选项组** | 进入页面后显示所有选项分组列表 |
| **编辑选项** | 点击某个分组的「编辑选项」→ 可新增、修改、删除该分组下的选项值 |
| **新增选项** | 在选项编辑页面点击「新增」→ 输入显示名称和值 → 保存 |

> 每个租户拥有独立的选项数据，互不影响。首次使用时系统会自动复制一份默认选项。

### 3.8 日志管理

**位置**：系统管理 → 日志管理

记录系统中所有用户的操作日志，可按时间范围、操作类型查询，用于审计追踪。

---

## 四、租户操作指南

> 以下内容面向**租户用户**（企业用户），涵盖日常业务操作。

### 4.1 基础资料设置（使用前必做）

在进行业务操作前，需要先设置好基础资料，这是后续所有业务的基础。

#### 4.1.1 仓库信息

**位置**：基础资料 → 仓库信息

| 操作 | 步骤 |
|------|------|
| **新增仓库** | 点击「新增」→ 填写仓库名称、负责人、排序号 → 设置是否为默认仓库 → 保存 |
| **设置默认仓库** | 编辑仓库 → 勾选「默认仓库」→ 保存（开单时会自动带入默认仓库） |

#### 4.1.2 结算账户

**位置**：基础资料 → 结算账户

设置企业的收付款账户（如银行卡、现金、微信、支付宝等）。

| 操作 | 步骤 |
|------|------|
| **新增账户** | 点击「新增」→ 填写账户名称、编号、初始金额 → 设置是否默认 → 保存 |

> 建议至少创建一个结算账户，工单结算、财务收付款都需要选择账户。

#### 4.1.3 供应商信息

**位置**：基础资料 → 供应商信息

管理商品采购的供应商。

| 操作 | 步骤 |
|------|------|
| **新增供应商** | 点击「新增」→ 填写供应商名称、联系人、电话、地址等 → 保存 |
| **导入供应商** | 点击「导入」→ 下载模板 → 按模板格式填写 → 上传导入 |

#### 4.1.4 客户信息

**位置**：基础资料 → 客户信息

管理销售业务中的客户。

| 操作 | 步骤 |
|------|------|
| **新增客户** | 点击「新增」→ 填写客户名称、联系人、电话等 → 保存 |
| **导入/导出** | 支持批量导入导出，操作同供应商 |

#### 4.1.5 客户车辆

**位置**：基础资料 → 客户车辆

管理客户的车辆信息，用于工单管理中关联车辆。

| 操作 | 步骤 |
|------|------|
| **新增车辆** | 点击「新增」→ 填写车牌号、客户姓名、手机号、车型、VIN码等 → 保存 |
| **编辑车辆** | 点击「编辑」→ 修改信息 → 保存 |
| **启用/禁用** | 选中车辆 → 点击「启用」或「禁用」按钮 |

#### 4.1.6 经手人管理

**位置**：基础资料 → 经手人管理

设置业务经手人，用于单据中选择负责人。

#### 4.1.7 收支项目

**位置**：基础资料 → 收支项目

设置财务管理中的收入/支出分类项目。

---

### 4.2 商品管理

#### 4.2.1 商品类别

**位置**：商品管理 → 商品类别

先创建商品分类，再添加商品。

| 操作 | 步骤 |
|------|------|
| **新增类别** | 点击「新增」→ 输入类别名称 → 选择上级类别（无上级则为根类别）→ 保存 |

#### 4.2.2 商品信息

**位置**：商品管理 → 商品信息

| 操作 | 步骤 |
|------|------|
| **新增商品** | 点击「新增」→ 填写商品名称、规格、型号、所属类别、单位 → 填写价格信息（采购价、零售价、销售价、最低售价）→ 保存 |
| **设置库存** | 点击「批量设置库存」→ 为每个仓库设置初始库存数量 |
| **导入商品** | 点击「导入」→ 下载 Excel 模板 → 填写后上传 |
| **导出商品** | 点击「导出」→ 下载当前商品列表 |

> 商品的「条码」字段可用于扫码出入库。

#### 4.2.3 多单位

**位置**：商品管理 → 多单位

当商品有多种计量单位时（如：箱/瓶，1箱=12瓶），在此配置换算关系。

#### 4.2.4 多属性

**位置**：商品管理 → 多属性

为商品设置颜色、尺寸等属性，用于 SKU 管理。

---

### 4.3 采购管理

#### 4.3.1 采购业务流程

```
请购单(可选) → 采购订单(可选) → 采购入库 → 付款
                                    ↓
                              采购退货(如需)
```

#### 4.3.2 采购入库（最常用）

**位置**：采购管理 → 采购入库

**新增采购入库单步骤**：

1. 点击「新增」按钮，打开单据表单
2. **单据头部**：
   - 选择供应商
   - 选择结算账户
   - 填写付款金额（可以为 0，后续付款）
3. **商品明细**：
   - 点击「添加商品」或扫码录入
   - 填写数量、单价（金额自动计算）
   - 选择入库仓库
4. 检查无误后点击「保存」

#### 4.3.3 采购退货

**位置**：采购管理 → 采购退货

操作方式同采购入库，选择「关联采购入库单」可自动带入商品信息。

---

### 4.4 销售管理

#### 4.4.1 销售业务流程

```
销售订单(可选) → 销售出库 → 收款
                    ↓
              销售退货(如需)
```

#### 4.4.2 销售出库（最常用）

**位置**：销售管理 → 销售出库

**新增销售出库单步骤**：

1. 点击「新增」
2. 选择客户、结算账户
3. 添加商品明细（商品、数量、单价、仓库）
4. 填写收款金额
5. 保存

#### 4.4.3 销售退货

**位置**：销售管理 → 销售退货

可关联原销售出库单，系统自动带入商品及数量。

---

### 4.5 零售管理

适用于不记录客户名称的零售场景。

#### 4.5.1 零售出库

**位置**：零售管理 → 零售出库

1. 点击「新增」
2. 选择结算账户、会员（可选）
3. 扫码或手动添加商品
4. 填写数量，系统自动计算金额
5. 保存

#### 4.5.2 零售退货

**位置**：零售管理 → 零售退货

操作同零售出库，金额为负数。

---

### 4.6 仓库管理

#### 4.6.1 其它入库/出库

**位置**：仓库管理 → 其它入库 / 其它出库

用于非采购/销售的库存变动（如赠品入库、损耗出库等）。

#### 4.6.2 调拨出库

**位置**：仓库管理 → 调拨出库

将商品从一个仓库转移到另一个仓库。

1. 点击「新增」
2. 添加商品 → 选择调出仓库和调入仓库
3. 填写数量 → 保存

#### 4.6.3 组装单 / 拆卸单

**位置**：仓库管理 → 组装单 / 拆卸单

- **组装**：将多个零件组装为一个成品（零件出库，成品入库）
- **拆卸**：将一个成品拆分为多个零件（成品出库，零件入库）

---

### 4.7 财务管理

#### 4.7.1 各类财务单据

| 单据类型 | 位置 | 说明 |
|----------|------|------|
| **收入单** | 财务管理 → 收入单 | 记录企业的其他收入（非销售收入） |
| **支出单** | 财务管理 → 支出单 | 记录企业的支出费用 |
| **收款单** | 财务管理 → 收款单 | 向客户收款（应收转实收） |
| **付款单** | 财务管理 → 付款单 | 向供应商付款（应付转实付） |
| **转账单** | 财务管理 → 转账单 | 企业内部账户间转账 |
| **收预付款** | 财务管理 → 收预付款 | 预收客户款项 |

#### 4.7.2 通用操作步骤

1. 点击「新增」
2. 选择往来单位（客户/供应商）
3. 选择结算账户
4. 填写金额和备注
5. 保存

---

### 4.8 项目管理

#### 4.8.1 项目类别

**位置**：项目管理 → 项目类别

先创建项目的分类（如「常规保养」「维修」「钣金喷漆」等）。

#### 4.8.2 项目信息

**位置**：项目管理 → 项目信息

| 操作 | 步骤 |
|------|------|
| **新增项目** | 点击「新增」→ 选择项目类别 → 填写项目名称、工时费、备注等 → 保存 |

> 项目信息会在工单管理中使用，创建工单时可选择关联项目。

---

### 4.9 工单管理（核心业务模块）

**位置**：工单管理 → 工单信息

工单管理是维修/服务行业的核心模块，完整流程如下：

#### 4.9.1 工单状态流程

```
新建工单(待派工) → 开始维修(维修中) → 完工(已完工) → 结算收款(已收款)
     ↓                ↓                                    ↑
   取消工单          取消工单                       部分收款(待收款) → 继续收款
```

| 状态 | 含义 |
|------|------|
| 待派工 | 工单已创建，等待分配维修人员 |
| 维修中 | 已派工，正在维修 |
| 已完工 | 维修完成，等待客户结算 |
| 待收款 | 已部分收款，还有余额未收 |
| 已收款 | 全部款项已收完 |
| 已取消 | 工单已取消作废 |

#### 4.9.2 新增工单

1. 点击「新增」按钮，弹出**全屏表单**
2. **车辆信息区域**（顶部）：
   - 在「选择车辆」搜索框中输入车牌号/客户姓名/手机号/VIN码
   - 如果搜索到已有车辆 → 点击选择，信息自动带入
   - 如果是新车辆 → 直接填写下方的客户姓名、手机号、车牌号等信息
   - 如果车辆无牌照 → 勾选「无牌」，车牌号变为非必填
   - 点击「完善更多信息」可打开客户车辆详细录入弹窗
3. **工单基本信息**：
   - 工单编号（系统自动生成）
   - 工单状态（默认「待派工」）
   - 故障描述、接车时间、预计完工时间
   - 派工人员（下拉选择）
4. **关联项目**：
   - 点击「选择项目」→ 在项目列表中勾选 → 确认
   - 可添加多个项目，工时费自动累计
5. **关联商品/配件**：
   - 点击「选择商品」→ 搜索并选择商品 → 填写数量和单价
   - 金额自动计算
6. **应收金额**：系统根据项目工时费 + 商品金额自动计算
7. 确认无误后点击「保存」

#### 4.9.3 工单操作（快捷按钮）

在工单列表中，**选中一条工单**（点击行前的复选框），页面上方会出现操作按钮：

| 按钮 | 当前状态要求 | 说明 |
|------|-------------|------|
| **开始维修** | 待派工 | 将工单状态改为「维修中」 |
| **完工** | 维修中 | 标记维修完成，状态变为「已完工」 |
| **结算收款** | 已完工 | 弹出结算弹窗，选择账户并输入金额 |
| **继续收款** | 待收款 | 对未收完的余额继续收款 |
| **取消工单** | 待派工/维修中 | 取消该工单 |

#### 4.9.4 工单结算

1. 选中已完工/待收款的工单
2. 点击「结算收款」或「继续收款」
3. 在弹窗中：
   - **结算账户**（必填）：默认带入第一个账户，可切换
   - **结算金额**：默认为剩余应收金额，可修改
4. 点击「确定」完成结算
5. 结算记录会自动纳入**报表查询 → 账户统计**

#### 4.9.5 编辑/删除工单

- **编辑**：点击操作列的「编辑」（已收款/已取消的工单不可编辑）
- **删除**：点击操作列的「删除」（已收款/已取消的工单不可删除）

---

### 4.10 报表查询

#### 4.10.1 常用报表说明

| 报表 | 位置 | 用途 |
|------|------|------|
| **商品库存** | 报表查询 → 商品库存 | 查看各仓库的当前库存数量 |
| **账户统计** | 报表查询 → 账户统计 | 查看各结算账户的收支明细和余额（含工单结算） |
| **采购统计** | 报表查询 → 采购统计 | 按时间段统计采购金额 |
| **销售统计** | 报表查询 → 销售统计 | 按时间段统计销售金额 |
| **零售统计** | 报表查询 → 零售统计 | 按时间段统计零售金额 |
| **进销存统计** | 报表查询 → 进销存统计 | 综合查看商品的进货、销售、库存变动 |
| **客户对账** | 报表查询 → 客户对账 | 查看每个客户的应收/已收/欠款明细 |
| **供应商对账** | 报表查询 → 供应商对账 | 查看每个供应商的应付/已付/欠款明细 |
| **库存预警** | 报表查询 → 库存预警 | 库存低于安全库存的商品提醒 |
| **入库/出库明细** | 报表查询 → 入库明细 / 出库明细 | 查看每笔入库/出库的详细记录 |
| **入库/出库汇总** | 报表查询 → 入库汇总 / 出库汇总 | 按商品汇总的入库/出库统计 |

#### 4.10.2 报表通用操作

1. 进入报表页面
2. 设置**查询条件**（时间范围、商品名称、仓库等）
3. 点击「查询」查看数据
4. 大部分报表支持「导出」为 Excel 文件

---

### 4.11 选项管理（自定义下拉选项）

**位置**：系统管理 → 选项管理

系统中很多下拉选择框（如工单状态、客户来源、车辆用途、派工人员等）的选项是可以自定义的。

#### 操作步骤

1. 进入「选项管理」页面，查看所有选项分组
2. 点击某个分组的「编辑选项」
3. 在选项列表中可以：
   - **新增选项**：点击「新增」→ 输入名称和值
   - **修改选项**：直接编辑现有选项的名称
   - **删除选项**：点击删除按钮移除不需要的选项
   - **设为默认**：勾选某个选项为默认值
4. 保存后，相关的下拉框会立即生效

#### 快捷添加

在业务表单中使用下拉框时，如果没有找到想要的选项：

1. 打开下拉框
2. 滚动到底部，点击「＋ 添加新选项」
3. 输入名称后确认，新选项立即可用

> 每个租户的选项数据是独立的，修改不会影响其他租户。

---

## 五、常见问题

### Q1：登录后看不到某些菜单？

**原因**：您的用户角色没有分配对应菜单的权限。

**解决**：联系管理员，进入「系统管理 → 角色管理」→ 找到您的角色 → 点击「菜单权限」→ 勾选缺失的菜单 → 保存。用户需退出重新登录。

### Q2：添加商品时没有类别可选？

**原因**：还没有创建商品类别。

**解决**：先到「商品管理 → 商品类别」创建分类，再添加商品。

### Q3：开单时找不到仓库/账户？

**原因**：还没有设置基础资料。

**解决**：先到「基础资料 → 仓库信息 / 结算账户」创建至少一条记录。

### Q4：工单结算时报错？

**排查步骤**：
1. 确认工单状态为「已完工」或「待收款」
2. 确认已选择结算账户
3. 确认结算金额大于 0 且不超过剩余应收

### Q5：报表数据为空？

**排查步骤**：
1. 检查查询的时间范围是否正确
2. 确认是否已有对应的业务数据
3. 确认单据状态是否已审核（未审核的单据可能不计入统计）

### Q6：如何批量导入商品/客户/供应商？

1. 进入对应列表页面
2. 点击「导入」按钮
3. 先点击「下载模板」获取 Excel 模板
4. 按模板格式填写数据（注意不要修改表头）
5. 上传填好的文件
6. 系统会提示导入结果

### Q7：删除的数据能恢复吗？

系统使用**逻辑删除**，已删除的数据在数据库中仍然保留（标记为已删除），但在界面上不再显示。如需恢复，请联系系统管理员操作数据库。

### Q8：多个用户能同时操作吗？

可以。系统支持多用户同时在线操作，各自的操作互不影响。但建议避免两个用户同时编辑同一条记录。

---

## 附录

### A. 系统初始配置清单（新租户快速上手）

按以下顺序完成初始配置，即可开始日常业务操作：

| 步骤 | 操作 | 位置 |
|------|------|------|
| 1 | 创建仓库 | 基础资料 → 仓库信息 |
| 2 | 创建结算账户 | 基础资料 → 结算账户 |
| 3 | 添加供应商 | 基础资料 → 供应商信息 |
| 4 | 添加客户 | 基础资料 → 客户信息 |
| 5 | 创建商品类别 | 商品管理 → 商品类别 |
| 6 | 添加商品 | 商品管理 → 商品信息 |
| 7 | 设置初始库存 | 商品管理 → 商品信息 → 批量设置库存 |
| 8 | 创建项目类别（如需工单）| 项目管理 → 项目类别 |
| 9 | 创建项目（如需工单）| 项目管理 → 项目信息 |
| 10 | 开始业务操作 | 采购/销售/零售/工单等 |

### B. 快捷键说明

| 快捷键 | 功能 |
|--------|------|
| Enter | 在表单中按回车可快速确认 |
| Esc | 关闭当前弹窗 |

### C. 浏览器要求

- 推荐使用 Chrome、Edge、Firefox 等现代浏览器
- 最低分辨率建议 1366×768，推荐 1920×1080
- 不支持 IE 浏览器
