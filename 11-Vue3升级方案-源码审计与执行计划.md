# Vue3 升级方案：源码审计、版本适配与执行计划

## 1. 文档目的

本文用于评审当前 `jshERP-web` 从 Vue2 技术栈升级到 Vue3 技术栈的可行路径。

用户要求：

1. 阅读并理解最新前端代码。
2. 查看每个主要组件和依赖使用的版本。
3. 联网核对 Vue3 适配版本。
4. 制定升级 Vue3 的计划。
5. 输出 Markdown 文档。
6. 审核文档。
7. 若审核无问题，再执行升级。
8. 要求整体页面显示和操作逻辑不变。

结论先行：**当前不满足“审核无问题后直接全量执行”的条件。**

原因不是 Vue3 本身不可升级，而是当前项目使用了大量 Vue2 + Ant Design Vue 1.x 的破坏性 API。若直接执行全量升级，会高概率破坏表单、图标、动态路由、弹窗、表格、上传、插槽和全局插件行为。

因此，本文建议只进入“分阶段迁移计划”，不建议立即执行全量 Vue3 代码迁移。

## 2. 当前前端基线

### 2.1 当前分支

当前分支：

```text
feature/vue3.0
```

### 2.2 当前构建状态

已执行当前 Vue2 项目基线构建：

```bash
npm run build
```

结果：

```text
Build complete
```

说明当前 Vue2 基线可以生产构建，这是后续升级必须保持的回退基准。

当前本机环境：

```text
node v24.10.0
npm 11.6.1
yarn 未安装
```

注意：当前项目是 Vue CLI 3 / webpack 4 时代工程。Node 24 能构建当前项目，但升级依赖后可能暴露更多 peer dependency 和 loader 兼容问题。

## 3. 当前依赖版本盘点

来源：`jshERP-web/package.json`

| 类别 | 当前依赖 | 当前版本 | 当前用途判断 |
| --- | --- | --- | --- |
| 核心框架 | `vue` | `^2.7.16` | 当前仍是 Vue2 运行模式 |
| 路由 | `vue-router` | `^3.0.1` | Vue2 路由，使用 `new Router`、`addRoutes` |
| 状态管理 | `vuex` | `^3.1.0` | Vue2 Vuex |
| UI 组件库 | `ant-design-vue` | `1.5.2` | 核心 UI，项目高度依赖 |
| 图表 | `viser-vue` | `^2.4.4` | dashboard 图表 |
| 图表数据 | `@antv/data-set` | `^0.11.2` | 图表数据处理 |
| 富文本 | `@tinymce/tinymce-vue` | `^2.0.0` | Vue2 版 TinyMCE wrapper |
| 请求 | `axios` | `^0.18.0` | API 请求封装 |
| 区域选择 | `vue-area-linkage` | `^5.1.0` | 地区联动组件 |
| 裁剪 | `vue-cropper` | `^0.4.8` | 图片裁剪 |
| 拖拽 | `vuedraggable` | `^2.20.0` | `JEditableTable` 拖拽 |
| 拖拽尺寸 | `vue-draggable-resizable` | `^2.3.0` | 列表 mixin 中使用 |
| i18n | `vue-i18n` | `^8.7.0` | Vue2 i18n |
| 本地存储 | `vue-ls` | `^3.2.0` | `Vue.ls` 全局大量使用 |
| 图片预览 | `vue-photo-preview` | `^1.1.3` | 图片预览插件 |
| 打印 | `vue-print-nb-jeecg` | `^1.0.9` | 打印插件 |
| 分栏 | `vue-splitpane` | `^1.0.4` | 分栏组件 |
| 代码编辑 | `codemirror` | `^5.46.0` | `JCodeEditor` |
| 日期 | `moment` | `^2.29.4` | 表单和报表大量使用 |
| 日期 | `dayjs` | `^1.8.0` | 全局 filter 和图表 |
| 引导 | `intro.js` | `^4.2.2` | 页面引导 |
| DOM | `jquery` | `^1.12.4` | `JEditableTable` 直接使用 |
| 构建 | `@vue/cli-service` | `^3.3.0` | Vue CLI 3 |
| SFC 编译 | `vue-template-compiler` | `^2.6.10` | 与 `vue@2.7.16` 版本不一致 |
| loader | `vue-loader` | `^15.7.0` | Vue2 loader |
| CSS | `less-loader` | `^4.1.0` | webpack 4 时代 |
| CSS | `sass-loader` | `^7.3.1` | webpack 4 时代 |

## 4. 源码使用特征审计

### 4.1 代码规模

本次扫描范围：`jshERP-web/src`

```text
Vue/JS 文件总数：318
.vue 文件：255
.js 文件：63
```

视图层主要模块：

| 模块 | 文件数量特征 |
| --- | --- |
| `views/bill` | 单据页面与弹窗很多，业务最重 |
| `views/system` | 系统配置、角色、用户、菜单等 |
| `views/report` | 大量报表和日期筛选 |
| `views/financial` | 财务单据 |
| `components/jeecg/JEditableTable.vue` | 超大核心组件，约 120KB |

### 4.2 Vue2 全局 API 使用

扫描结果：

| 语法/API | 命中数量 | Vue3 影响 |
| --- | ---: | --- |
| `Vue.ls` | 151 | 需要改为 `app.config.globalProperties` 或封装 storage service |
| `Vue.use` | 14 | 需要改为 `app.use` |
| `Vue.component` | 4 | 需要改为 `app.component` |
| `Vue.prototype` | 2 | 需要改为 `app.config.globalProperties` |
| `Vue.filter` | 4 | Vue3 移除 filter，需要改为方法/全局 properties/工具函数 |
| `new Vue` | 2 | 需要改为 `createApp` |
| `this.$set` | 10 | Vue3 不再需要，但代码需确认响应式对象 |
| `$listeners` | 6 | Vue3 合并到 `$attrs` |
| `$scopedSlots` | 3 | Vue3 slot 统一 |

### 4.3 模板语法风险

扫描结果：

| 语法/API | 命中数量 | Vue3 影响 |
| --- | ---: | --- |
| `slot-scope` | 278 | 需要改为 `v-slot` / `#default` |
| `.native` | 79 | Vue3 移除 `.native` |
| `.sync` | 6 | Vue3 改为 `v-model:prop` |
| filter 管道 | 存在 | Vue3 移除模板 filters |

### 4.4 Ant Design Vue 使用风险

扫描结果：

| 语法/API | 命中数量 | 风险 |
| --- | ---: | --- |
| `a-form` | 2402 | 表单体系高度依赖 Ant Design Vue 1.x |
| `v-decorator` | 426 | Ant Design Vue Vue3 版本不再使用该 Form.create 模式 |
| `this.form` | 316 | 大量依赖旧表单实例 |
| `<a-icon>` | 317 | Vue3 版 Ant Design Vue 使用独立 icons 包 |
| `a-table` | 260 | 插槽、分页、列配置需核对 |
| `a-modal` | 183 | `v-model/visible/open` 差异需核对 |
| `scopedSlots` | 181 | 表格列插槽迁移风险高 |

这是本项目升级 Vue3 的最大风险点。尤其 `v-decorator` 和 `<a-icon>` 不是靠改 package 版本能解决的。

### 4.5 动态路由风险

当前 `src/router/index.js` 使用：

```js
Vue.use(Router)
export default new Router({
  mode: 'history',
  base: process.env.BASE_URL,
  routes: constantRouterMap
})
```

当前 `src/permission.js` 使用：

```js
router.addRoutes(store.getters.addRouters)
```

Vue Router 4 需要：

1. `new Router` 改为 `createRouter`。
2. `mode: 'history'` 改为 `history: createWebHistory(base)`。
3. `*` 路由改为 `/:pathMatch(.*)*`。
4. `addRoutes` 改为逐条 `addRoute`。
5. 动态路由注册后跳转逻辑需要重新验证。

## 5. 联网核对后的 Vue3 适配版本建议

联网方式：

1. `npm view <package> version peerDependencies --json`
2. Vue 官方迁移文档
3. Vue Router 官方迁移文档
4. Vuex 官方迁移文档
5. Vue CLI / Vite 官方文档

### 5.1 推荐目标版本

建议把“升级 Vue3.0”理解为升级到当前稳定 Vue3 大版本，而不是锁死 `3.0.0`。

推荐目标：

```text
vue: 3.5.35
@vue/compat: 3.5.35
@vue/compiler-sfc: 3.5.35
```

原因：

1. 当前 Vue3 最新稳定版本为 `3.5.35`。
2. `@vue/compat` 要求和 `vue` 同版本。
3. 官方迁移构建建议先使用 `@vue/compat`，并在构建中 alias `vue` 到 `@vue/compat`。

### 5.2 依赖版本映射

| 当前依赖 | 当前版本 | Vue3 适配版本/替代 | 处理建议 |
| --- | --- | --- | --- |
| `vue` | `^2.7.16` | `3.5.35` | 先用 `@vue/compat` 运行，最终切纯 Vue3 |
| `vue-template-compiler` | `^2.6.10` | `@vue/compiler-sfc@3.5.35` | 必须替换 |
| `vue-loader` | `^15.7.0` | `vue-loader@16+` | 若继续 Vue CLI/webpack 必须升级 |
| `@vue/cli-service` | `^3.3.0` | `@vue/cli-service@5.0.9` 或 Vite | 官方提示 Vue CLI 维护模式，新项目推荐 Vite |
| `vue-router` | `^3.0.1` | `vue-router@4.6.4` | 需要 API 迁移 |
| `vuex` | `^3.1.0` | `vuex@4.1.0` | API 基本保留，但初始化方式变 |
| `ant-design-vue` | `1.5.2` | `ant-design-vue@4.2.6` | 最大改造点，表单/图标/插槽要系统迁移 |
| `@ant-design/icons-vue` | 无 | `7.0.1` | 替代 `<a-icon type="">` |
| `vue-i18n` | `^8.7.0` | `vue-i18n@11.4.4` | 若当前实际未深度使用，可延后 |
| `@tinymce/tinymce-vue` | `^2.0.0` | `@tinymce/tinymce-vue@6.3.0` | 需要补 `tinymce` peer dependency |
| `vuedraggable` | `^2.20.0` | `vuedraggable@4.1.0` | `JEditableTable` 需要专项验证 |
| `vue-draggable-resizable` | `^2.3.0` | `3.0.0` | Vue3 peer dependency |
| `vue-cropper` | `^0.4.8` | `vue-cropper@1.1.4` 或 `vue3-cropper@0.4.0` | 需看组件 API 差异 |
| `vue-ls` | `^3.2.0` | `vue-ls@4.2.0` 或自封装 storage | 建议封装，避免全局 `Vue.ls` 扩散 |
| `vue-photo-preview` | `^1.1.3` | `vue3-photo-preview@0.3.0` 或替代预览组件 | 需验证样式和 API |
| `vue-print-nb-jeecg` | `^1.0.9` | `vue-print-nb@1.7.5` 或自定义 print directive | 原 jeecg 包需验证是否支持 Vue3 |
| `viser-vue` | `^2.4.4` | 建议改 `@antv/g2plot@2.4.35` 或 ECharts | `viser-vue` 维护风险高 |
| `codemirror` | `^5.46.0` | 短期保留 5.x，长期改 CodeMirror 6 + `vue-codemirror@6.1.1` | `JCodeEditor` 专项迁移 |
| `axios` | `^0.18.0` | `axios@1.16.1` | 响应拦截器和取消请求需检查 |
| `js-cookie` | `^2.2.0` | `3.0.8` | API 基本可控 |
| `moment` | `^2.29.4` | `2.30.1` 或逐步 dayjs | 当前 Antd 日期依赖 moment，迁移时需核对 |
| `dayjs` | `^1.8.0` | `1.11.21` | 可升级 |
| `intro.js` | `^4.2.2` | `8.3.2` | 页面引导需回归 |
| `clipboard` | `^2.0.4` | `2.0.11` | 可升级 |
| `jquery` | `^1.12.4` | 不建议升到 `4.0.0` | `JEditableTable` 依赖 jQuery，先保留或逐步去除 |
| `less` | `^3.9.0` | `4.6.4` | 与构建工具联动 |
| `less-loader` | `^4.1.0` | Vue CLI 5/webpack5 可用更高版本 | 需与 webpack 版本匹配 |
| `sass` | `^1.32.13` | `1.100.0` | 可升级 |
| `sass-loader` | `^7.3.1` | Vue CLI 5/webpack5 可用更高版本 | 需与 webpack 版本匹配 |

## 6. 推荐迁移策略

### 6.1 不推荐一次性全量迁移

不推荐直接执行：

```bash
npm install vue@latest ant-design-vue@latest vue-router@latest vuex@latest
```

原因：

1. `ant-design-vue@1.5.2` 到 `4.2.6` 是核心破坏性升级。
2. 当前有 426 处 `v-decorator`，需要重写表单模型。
3. 当前有 317 处 `<a-icon>`，需要逐一映射到 `@ant-design/icons-vue`。
4. 当前有 278 处 `slot-scope`，需要迁移为 `v-slot`。
5. 当前有 79 处 `.native`，Vue3 不支持。
6. `router.addRoutes` 在 Vue Router 4 中不存在。
7. `JEditableTable.vue` 使用 jQuery、vuedraggable、复杂插槽和动态表格行为，是高风险组件。

### 6.2 推荐分阶段路径

#### 阶段 0：冻结基线

目标：确保 Vue2 当前页面可以作为回归参照。

动作：

1. 保留当前 Vue2 分支。
2. 记录 `npm run build` 成功输出。
3. 启动当前 Vue2 dev server，截图核心页面。
4. 准备 Playwright 或人工回归清单。
5. 禁止在迁移分支混入业务需求。

#### 阶段 1：构建工具升级，但不改运行框架

目标：先把构建链从 Vue CLI 3 时代提升到可迁移状态。

动作：

1. 统一包管理器，建议 npm。
2. 增加 lockfile，后续固定依赖。
3. 评估 Vue CLI 5 还是 Vite。
4. 若追求最小变更，先用 Vue CLI 5 + webpack5。
5. 若追求长期维护，最终迁移 Vite。

审计结论：由于当前项目依赖 `vue.config.js`、webpack chain、Less 主题覆盖、gzip 插件、路径代理，第一步直接 Vite 会放大风险。建议先 Vue CLI 5，后续再 Vite。

#### 阶段 2：引入 `@vue/compat`

目标：在 Vue3 运行时下尽量保留 Vue2 行为。

动作：

1. `vue` 升到 `3.5.35`。
2. 安装 `@vue/compat@3.5.35`。
3. `vue-template-compiler` 替换为 `@vue/compiler-sfc@3.5.35`。
4. `vue-loader` 升到 16+。
5. 在构建配置中 alias：

```js
config.resolve.alias.set('vue', '@vue/compat')
```

6. 配置兼容模式：

```js
compilerOptions: {
  compatConfig: {
    MODE: 2
  }
}
```

7. 此阶段目标仅为编译和启动，不追求清空所有 warning。

#### 阶段 3：迁移 Vue 全局 API

目标：入口、插件、全局属性先稳定。

动作：

1. `new Vue` 改为 `createApp`。
2. `Vue.use` 改为 `app.use`。
3. `Vue.component` 改为 `app.component`。
4. `Vue.prototype` 改为 `app.config.globalProperties`。
5. `Vue.filter` 改为工具函数或全局方法。
6. 将 `Vue.ls` 包装为 `storageService`，再注入 `$ls`，减少改动面。

#### 阶段 4：迁移 Vue Router 4

目标：动态菜单和权限路由不变。

动作：

1. `new Router` 改为 `createRouter`。
2. `mode: 'history'` 改为 `createWebHistory(base)`。
3. `path: '*'` 改为 `/:pathMatch(.*)*`。
4. `router.addRoutes(routes)` 改为：

```js
routes.forEach(route => router.addRoute(route))
```

5. 验证登录后动态菜单、刷新页面、404、子路径 `/erp/`。

#### 阶段 5：迁移 Vuex 4

目标：保留现有 store 模块和调用方式。

动作：

1. `new Vuex.Store` 改为 `createStore`。
2. `app.use(store)`。
3. 继续保留 Vuex，不在同一阶段改 Pinia。

#### 阶段 6：迁移 Ant Design Vue

目标：页面视觉和交互不变。

这是最大阶段，必须拆模块做。

建议顺序：

1. 先迁移布局组件：Layout、Menu、Breadcrumb、Tabs。
2. 再迁移表格组件：Table、columns、scopedSlots。
3. 再迁移基础弹窗：Modal、Drawer、Popconfirm。
4. 再迁移表单：`v-decorator` 到新 Form API。
5. 最后迁移图标：`<a-icon type="">` 到 `@ant-design/icons-vue`。

表单迁移不可一刀切，必须按模块：

1. 登录/注册。
2. 系统配置。
3. 基础资料。
4. 项目/车辆/工单。
5. 单据模块。
6. 报表模块。
7. `JEditableTable`。

#### 阶段 7：替换不兼容插件

重点插件：

1. `vue-photo-preview` -> `vue3-photo-preview` 或重写 preview。
2. `vue-print-nb-jeecg` -> `vue-print-nb` 或自定义 directive。
3. `vuedraggable` -> `vuedraggable@4.1.0`。
4. `vue-draggable-resizable` -> `3.0.0`。
5. `@tinymce/tinymce-vue` -> `6.3.0`，补 `tinymce`。
6. `vue-area-linkage` 需确认 Vue3 支持，不支持则本地封装或替换。
7. `viser-vue` 建议替换为 `@antv/g2plot` 或 ECharts。

#### 阶段 8：全量回归

必须覆盖：

1. 登录。
2. 菜单动态加载。
3. 首页。
4. 用户/角色/菜单权限。
5. 商品资料。
6. 商品分类。
7. 项目管理。
8. 车辆管理。
9. 工单管理。
10. 采购、销售、零售、调拨、组装、拆卸等单据。
11. 财务单据。
12. 报表筛选。
13. 上传、打印、导入、导出。
14. `JEditableTable` 行编辑、拖拽、合计、校验、扫码录入。
15. 子路径 `/erp/` 部署。

## 7. 执行门禁

任何阶段进入下一阶段前，必须满足：

1. `npm run build` 通过。
2. dev server 可启动。
3. 登录流程可用。
4. 动态菜单可用。
5. 当前阶段涉及页面可人工或自动回归。
6. 无新增 console error。
7. 页面截图与 Vue2 基线可接受。

## 8. 本次文档审核

### 8.1 审核结果

审核结论：**不通过直接全量执行条件。**

### 8.2 阻断原因

1. 426 处 `v-decorator` 表明表单体系高度绑定 Ant Design Vue 1.x。
2. 317 处 `<a-icon>` 需要完整图标映射。
3. 278 处 `slot-scope` 和 181 处 `scopedSlots` 影响表格、菜单、复杂插槽。
4. `JEditableTable.vue` 是超大核心组件，依赖 jQuery、vuedraggable、复杂动态列和行内编辑。
5. 动态路由依赖 `router.addRoutes`，Vue Router 4 已移除。
6. 当前构建工具是 Vue CLI 3，和 Vue3 目标栈跨度过大。
7. 多个 Vue2 插件没有明确的一对一无损 Vue3 替代品。

### 8.3 执行判断

根据用户设定的条件：“发现没有问题后，进行执行”。

本次审核已经发现重大问题，因此**不应直接执行全量 Vue3 迁移**。

可以执行的安全动作仅限：

1. 保留本文档。
2. 建立回归清单。
3. 做阶段 0 基线冻结。
4. 单独开提交尝试构建工具升级。

不建议现在直接改 `package.json` 到 Vue3 并批量替换代码。

## 9. 建议下一步

建议下一步只执行“阶段 0 + 阶段 1”：

1. 新增前端回归测试清单。
2. 记录 Vue2 页面截图。
3. 固定 npm lockfile。
4. 尝试 Vue CLI 5 升级。
5. 不改 Ant Design Vue、不改 Vue3。

若阶段 1 成功，再进入 `@vue/compat` 阶段。

## 10. 参考资料

1. Vue 3 Migration Build  
   https://v3-migration.vuejs.org/migration-build

2. Vue Router：Migrating from Vue 2  
   https://router.vuejs.org/guide/migration/

3. Vuex：Migrating to 4.0 from 3.x  
   https://vuex.vuejs.org/guide/migrating-to-4-0-from-3-x.html

4. Vue CLI：Maintenance Mode  
   https://cli.vuejs.org/guide/

5. Vite：Getting Started / Node version requirement  
   https://vite.dev/guide/

6. Ant Design Vue GitHub  
   https://github.com/vueComponent/ant-design-vue

7. npm version checks executed with:

```bash
npm view <package> version peerDependencies --json
```
