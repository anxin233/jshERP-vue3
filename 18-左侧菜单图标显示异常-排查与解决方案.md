# 左侧菜单图标显示异常 — 排查与解决方案

> 文档性质：问题分析 + 修复方案（**不含代码改动**）  
> 适用前端：`jshERP-web`（**Vue 3.5.35** + **Ant Design Vue 4.2.6**，构建工具 **Vite 8**；见 [`jshERP-web/package.json`](jshERP-web/package.json)）  
> 现象参考：侧栏一级菜单大量显示「圆圈问号」图标，仅少数菜单图标正常。

---

## 一、现象说明

根据当前界面表现，可归纳为：

| 菜单项 | 实际显示 | 数据库/接口常见 `icon` 值 | 是否正常 |
|--------|----------|---------------------------|----------|
| 首页 | 仪表盘类图标 | `home`（后端固定注入） | 正常 |
| 零售管理 | 问号圆圈 | `gift` | **异常** |
| 采购管理 | 问号圆圈 | `retweet` | **异常** |
| 销售管理 | 问号圆圈 | `shopping-cart` | **异常** |
| 仓库管理 | 问号圆圈 | `hdd` | **异常** |
| 财务管理 | 问号圆圈 | `money-collect` | **异常** |
| 报表查询 | 问号圆圈 | `pie-chart` | **异常** |
| 商品管理 | 问号圆圈 | `shopping` | **异常** |
| 基础资料 | 四宫格图标 | `appstore` | 正常 |
| 工单管理 | 问号圆圈 | 常为 `NULL`/空，或后续手工配置未映射名 | **异常** |
| 系统管理 | 齿轮图标 | `setting` | 正常 |
| 项目管理 | 问号圆圈 | `project` | **异常** |
| 二级菜单（如零售出库） | 列表/档案类图标 | 多为 `profile` | 正常 |

**规律**：一级目录使用「业务类」图标名时几乎全部失败；使用 `setting` / `appstore` / `home` 或子级 `profile` 时正常。  
这说明问题**不是窄屏布局单独导致**，而是**图标名称 → SVG 映射不完整**；与屏幕宽度无直接关系。

---

## 二、数据流与相关源码

### 2.1 从后端到侧栏的链路

```
jsh_function.icon（MySQL）
    ↓
FunctionController.getMenuByFunction()  →  JSON: { text, icon, url, children }
    ↓  （另在列表首位注入首页 icon=home）
queryPermissionsByUser → Vuex user.permissionList
    ↓
GlobalLayout.vue → SideMenu → SMenu（menu/index.js）
    ↓
renderIconVNode(menu.icon) → LegacyIcon(type=icon)
    ↓
resolveIconAsn(type) → outlinedIconAsn[key] || QuestionCircleOutlined
```

### 2.2 关键文件

| 文件 | 作用 |
|------|------|
| [`jshERP-boot/.../FunctionController.java`](jshERP-boot/src/main/java/com/jsh/erp/controller/FunctionController.java) | 菜单接口 `icon` 字段来自 `function.getIcon()`；首页写死 `home` |
| [`jshERP-web/src/store/modules/user.js`](jshERP-web/src/store/modules/user.js) | `GetPermissionList` 将菜单 JSON 存入 `permissionList` |
| [`jshERP-web/src/components/page/GlobalLayout.vue`](jshERP-web/src/components/page/GlobalLayout.vue) | `menus = permissionMenuList` 传给侧栏 |
| [`jshERP-web/src/components/menu/index.js`](jshERP-web/src/components/menu/index.js) | `renderIconVNode` 渲染菜单图标 |
| [`jshERP-web/src/components/legacy/LegacyIcon.vue`](jshERP-web/src/components/legacy/LegacyIcon.vue) | 调用 `resolveIconAsn` 生成 SVG |
| [`jshERP-web/src/components/legacy/legacy-icon-asn.js`](jshERP-web/src/components/legacy/legacy-icon-asn.js) | **旧版 icon `type` 字符串 → icons-svg 定义** 的白名单映射 |
| [`jshERP-web/src/views/system/modules/icon/Icons.vue`](jshERP-web/src/views/system/modules/icon/Icons.vue) | 菜单管理里可选图标列表（同样走 LegacyIcon） |
| [`jshERP-boot/docs/jsh_erp.sql`](jshERP-boot/docs/jsh_erp.sql) | 出厂菜单及 `icon` 初始值 |

### 2.3 当前前端技术栈（版本核实）

依据 [`jshERP-web/package.json`](jshERP-web/package.json) 与 `package-lock.json`，**当前仓库内唯一前端**为 `jshERP-web`，主要依赖版本如下：

| 包 | 版本 | 说明 |
|----|------|------|
| `vue` | **3.5.35** | Vue 3（非 Vue 2.7） |
| `@vue/compiler-sfc` | 3.5.35 | 与 Vue 主版本一致 |
| `ant-design-vue` | **4.2.6** | Ant Design Vue 4 |
| `@ant-design/icons-vue` | 7.0.1 | 图标 Vue 组件库 |
| `vue-router` | 4.6.4 | Vue Router 4 |
| `vuex` | 4.1.0 | Vuex 4 |
| `vite` | 8.0.16 | 开发/打包（已非 vue-cli/webpack） |

说明：根目录 `CLAUDE.md` 等旧文档仍写「Vue 2.7 + yarn serve + vue.config.js」，与**当前 `package.json` 不一致**；以 `package.json` 为准。工作区内未发现独立的 `jshERP-web-v3` 目录，现有 `jshERP-web` 即 Vue 3 版前端。

### 2.4 Ant Design Vue 4 升级背景

[`legacy-icon-asn.js`](jshERP-web/src/components/legacy/legacy-icon-asn.js) 文件头注释说明：为规避 **icons-vue 在 compat 模式下 Outlined 组件丢 prop** 的问题，改为直接引用 `@ant-design/icons-svg` 的 ASN 定义，由 `LegacyIcon` 手写渲染。

因此：**并非** `@ant-design/icons-vue` 整包失效，而是项目**主动改为「有限白名单映射」**，白名单未覆盖的 `type` 一律无法显示对应图标。

---

## 三、根因分析

### 3.1 直接原因：未映射图标被强制回退为「问号」

[`legacy-icon-asn.js`](jshERP-web/src/components/legacy/legacy-icon-asn.js) 中：

```javascript
export function resolveIconAsn(type, theme) {
  const normalizedType = normalizeIconType(type)
  // ...
  return outlinedIconAsn[normalizedType] || QuestionCircleOutlined
}
```

当 `jsh_function.icon`（或接口返回的 `icon`）在 `outlinedIconAsn` 中**没有对应 key** 时，**固定渲染 `QuestionCircleOutlined`**，界面上即「圆圈问号」。

`normalizeIconType` 仅做：`trim`、去掉 `icon-` 前缀、去掉 `-o` 后缀，**不会**把 `shopping-cart` 转成 `ShoppingCartOutlined` 这类组件名。

### 3.2 业务数据使用的图标名，多数不在白名单内

出厂数据（[`jsh_erp.sql`](jshERP-boot/docs/jsh_erp.sql)）一级菜单 `icon` 与前端映射对照：

| 菜单 | DB `icon` | `outlinedIconAsn` 是否包含 | 结果 |
|------|-----------|------------------------------|------|
| 系统管理 | `setting` | 是 | 正常 |
| 基础资料 | `appstore` | 是 | 正常 |
| 首页 | `home` | 是（映射为 `DashboardOutlined`） | 正常 |
| 零售管理 | `gift` | **否** | 问号 |
| 采购管理 | `retweet` | **否** | 问号 |
| 销售管理 | `shopping-cart` | **否** | 问号 |
| 仓库管理 | `hdd` | **否** | 问号 |
| 财务管理 | `money-collect` | **否** | 问号 |
| 报表查询 | `pie-chart` | **否** | 问号 |
| 商品管理 | `shopping` | **否** | 问号 |

扩展模块（[`V8__menu_permission_fix.sql`](jshERP-boot/src/main/resources/db/migration/V8__menu_permission_fix.sql) 等）：

| 菜单 | DB `icon` | 映射 | 结果 |
|------|-----------|------|------|
| 项目管理 | `project` | **否** | 问号 |
| 工单管理 | 插入语句**未写 icon 列**（多为 `NULL`） | — | 若为空则不渲染图标；若后台后来选了未映射名则问号 |

子菜单普遍为 `profile`，白名单**有** `profile` → `ProfileOutlined`，故二级项显示为列表/档案图标，与截图一致。

### 3.3 菜单图标选择器与运行时映射不一致（易误导排查）

[`Icons.vue`](jshERP-web/src/views/system/modules/icon/Icons.vue) 的 `webIcons` / `dataIcons` 等列表包含 `gift`、`hdd`、`shopping-cart`、`pie-chart`、`money-collect`、`project` 等**上百个**名称，但 [`legacy-icon-asn.js`](jshERP-web/src/components/legacy/legacy-icon-asn.js) 仅维护约 **70 个** key。

管理员在「菜单管理」里选的图标名**合法存在于选择器列表**，但**运行时仍可能显示问号**——因为选择器与 `outlinedIconAsn` 未同步维护。

### 3.4 可排除的原因

| 猜测 | 结论 |
|------|------|
| 窄屏 / Drawer 导致图标丢失 | **不成立**。`renderIconVNode` 与 `device` 无关，同一套逻辑 |
| 接口未返回 `icon` | **不成立**。`FunctionController` 明确 `item.put("icon", function.getIcon())` |
| 子路由未传 icon | 一级 SubMenu 使用 `menu.icon`，与路由 meta 无关 |
| 权限过滤掉 icon | 仅过滤菜单节点，不修改 icon 字段 |

---

## 四、解决方案（推荐优先级）

### 方案 A：补全 `legacy-icon-asn.js` 映射（改动小、风险低，**推荐首选**）

**思路**：保持现有 `LegacyIcon` 架构，把数据库和 `Icons.vue` 中实际用到的 `type` 补进 `outlinedIconAsn`。

**建议至少补充的一级菜单映射**（需从 `@ant-design/icons-svg/es/asn/` 按同名 Outlined 引入）：

| `type`（DB） | 建议 ASN 组件 |
|--------------|----------------|
| `gift` | `GiftOutlined` |
| `retweet` | `RetweetOutlined` |
| `shopping-cart` | `ShoppingCartOutlined` |
| `shopping` | `ShoppingOutlined` |
| `hdd` | `HddOutlined` |
| `money-collect` | `MoneyCollectOutlined` |
| `pie-chart` | `PieChartOutlined` |
| `project` | `ProjectOutlined` |
| `folder` | `FolderOutlined`（项目类别等） |
| `file-text` | `FileTextOutlined`（项目信息等） |

**实施步骤**：

1. 在测试库执行，导出所有在用图标名：
   ```sql
   SELECT DISTINCT icon FROM jsh_function
   WHERE delete_flag = '0' AND icon IS NOT NULL AND icon <> '';
   ```
2. 与 `outlinedIconAsn` 的 key 做差集，得到「缺失列表」。
3. 在 [`legacy-icon-asn.js`](jshERP-web/src/components/legacy/legacy-icon-asn.js) 中为每个缺失项：
   - `import XxxOutlined from '@ant-design/icons-svg/es/asn/XxxOutlined'`
   - 在 `outlinedIconAsn` 中增加 `'kebab-name': XxxOutlined`
4. 打开 **系统管理 → 菜单管理 → 图标选择器**，逐页确认曾经问号的名称是否已正常显示。
5. 刷新浏览器（必要时硬刷新），检查侧栏一级菜单图标。

**可选优化**：将 `resolveIconAsn` 的默认回退从 `QuestionCircleOutlined` 改为 `null`（[`LegacyIcon.vue`](jshERP-web/src/components/legacy/LegacyIcon.vue) 已对无定义返回 `null`），避免未知图标误导为「帮助/疑问」。

---

### 方案 B：同步维护「可选图标列表」与映射表

**思路**：以 `outlinedIconAsn` 的 key 为唯一真相源，收缩 [`Icons.vue`](jshERP-web/src/views/system/modules/icon/Icons.vue) 中不可渲染的图标名，或生成脚本从 `outlinedIconAsn` 自动生成选择器列表。

**步骤**：

1. 编写脚本或文档约定：新增图标 = 同时改 `legacy-icon-asn.js` + `Icons.vue`。
2. 菜单管理界面只展示已映射的图标，避免运营配置「看得见、显示不出来」的名称。
3. 对历史数据执行 SQL，把非法 `icon` 批量改为已映射名（见方案 C）。

---

### 方案 C：仅改数据库 `icon` 字段（不改前端代码时可用）

**思路**：把 `jsh_function.icon` 改成当前白名单里已有的名称。

示例（需按环境核对 `number` / `name` 后执行）：

```sql
-- 示例：将一级菜单 icon 改为已映射项（请按实际 number 调整）
UPDATE jsh_function SET icon = 'profile' WHERE number = '0401' AND icon = 'gift';
UPDATE jsh_function SET icon = 'sync'      WHERE number = '0502' AND icon = 'retweet';
-- ... 其余项需人工挑选语义接近且已映射的 key：stock/fund/tool/database 等
```

**缺点**：图标语义与产品设计可能不一致；每新增菜单都要查白名单，不可持续。仅适合临时止血。

---

### 方案 D：动态加载 `@ant-design/icons-vue`（长期、改动大）

**思路**：废弃手写白名单，根据 `type` 动态解析为 `XxxOutlined` 组件（或统一使用 `<Icon :component="..." />`）。

**适用**：希望与 Ant Design 全量图标集对齐、减少后续维护成本。  
**注意**：需重新验证 Ant Design Vue 4 compat 下是否仍存在「Outlined 丢 prop」问题；工作量和回归范围大于方案 A。

---

## 五、验证步骤

1. **接口层**：浏览器 Network 查看 `queryPermissionsByUser`（或项目内等价菜单接口）返回 JSON，确认各一级项 `icon` 字段为 `gift`、`shopping-cart` 等，而非空。
2. **映射层**：在控制台临时执行（开发环境）：
   - 对返回的每个 `icon` 检查是否存在于 `outlinedIconAsn`（或是否命中 `QuestionCircle` 回退）。
3. **UI 层**：侧栏一级菜单与「菜单管理 → 图标选择器」中同名图标显示一致。
4. **回归**：顶部 Header、表格操作列、按钮中其他 `legacy-icon` 用法未受影响。

---

## 六、与「窄屏侧栏样式」问题的关系

窄屏侧栏错位（Logo 高度、Header 宽度、Drawer 断点等）属于 **布局/CSS** 范畴，见前期布局分析。  
**本次图标问题**属于 **图标映射层**，二者可分开处理；补全图标映射后，窄屏与宽屏下问号问题应同时消失。

---

## 七、建议实施顺序（汇总）

| 顺序 | 动作 | 预估工作量 |
|------|------|------------|
| 1 | SQL 导出 `DISTINCT icon`，生成缺失清单 | 0.5h |
| 2 | 按方案 A 补全 `legacy-icon-asn.js`（优先一级菜单 8+2 项） | 1~2h |
| 3 | 菜单管理图标选择器抽测 + 侧栏目测 | 0.5h |
| 4 | 方案 B：约束 Icons.vue 与映射表一致（可选） | 1h |
| 5 | 调整 `resolveIconAsn` 默认回退策略（可选） | 0.5h |

---

## 八、附录：当前 `outlinedIconAsn` 已支持的部分 key（节选）

已支持、与出厂菜单相关的包括：`setting`、`appstore`、`home`、`dashboard`、`profile`、`user`、`tool`、`stock`、`fund`、`database`、`sync`、`folder` 等。

**出厂一级菜单中未支持、会导致问号的 key**：`gift`、`retweet`、`shopping-cart`、`shopping`、`hdd`、`money-collect`、`pie-chart`、`project`。

---

*文档生成依据：对 `jshERP-web` / `jshERP-boot` 源码与 `jsh_erp.sql`、Flyway 脚本的静态分析，并结合当前侧栏截图现象核对。*

---

## 九、实施记录（2026-05）

已按 **方案 A** 落地：

1. 新增 [`jshERP-web/scripts/generate-legacy-icon-asn.js`](jshERP-web/scripts/generate-legacy-icon-asn.js)，从 `Icons.vue` 自动生成 **339** 个图标映射。
2. 重新生成 [`legacy-icon-asn.js`](jshERP-web/src/components/legacy/legacy-icon-asn.js)；未知 icon 回退由 `QuestionCircleOutlined` 改为 **`null`**（不显示误导性问号）。
3. 前端命令：`npm run generate:icons`（见 `package.json`）。
4. Flyway **V12**：为工单管理菜单补 `icon=tool`（[`V12__workorder_menu_icon.sql`](jshERP-boot/src/main/resources/db/migration/V12__workorder_menu_icon.sql)）。

**验证**：重启后端使 V12 生效 → 前端 `npm run serve` → 硬刷新浏览器，检查侧栏一级菜单图标与「菜单管理 → 图标选择器」一致。
