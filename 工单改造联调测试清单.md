# 工单「服务项目 / 维修材料」下拉改造 - 联调测试清单

> 测试日期：2026-04-18  
> 测试环境：本地开发环境  
> 前端：http://localhost:3000  
> 后端：http://localhost:9999

---

## 一、后端验证（已完成 ✅）

### 1.1 MaterialController#findBySelect 接口
- ✅ 已确认返回 `materialId` 字段（jsh_material 主键）
- ✅ 已确认返回 `commodityDecimal` 字段（零售价）
- ✅ 接口路径：`GET /material/findBySelect`

### 1.2 ProjectController#list 接口
- ✅ 接口路径：`GET /project/list`
- ✅ 支持 `search.name` 参数（项目名称搜索）
- ⚠️ **enabled 字段问题已修复**：
  - 数据库字段类型：`bit(1)`
  - SQL 查询：`p.enabled = #{enabled}`（直接比较 bit 值）
  - 前端已移除 `enabled: '1'` 筛选条件（避免类型不匹配）

---

## 二、前端改造验证（待测试）

### 2.1 服务项目下拉搜索 🔄

**测试步骤：**
1. 登录系统：http://localhost:3000
2. 进入「工单管理」模块
3. 点击「新增工单」
4. 在「服务项目」区域，点击「添加项目」按钮
5. 在新增的空行中，点击「服务项目名称」下拉框

**预期结果：**
- [ ] 下拉框显示项目列表（应显示 5 个项目：ERP系统开发、移动商城APP、企业管理咨询、112、345、666、洗车）
- [ ] 输入关键字（如"ERP"）后，列表实时过滤
- [ ] 选项展示格式：`项目名称 ¥价格`
- [ ] 选中项目后，自动回填：`projectId`、`projectName`、`unitPrice`
- [ ] 金额自动计算：`amount = unitPrice × quantity × (discountRate/100)`

**实际结果：**
- [ ] 通过 / [ ] 失败（记录问题）

---

### 2.2 维修材料下拉搜索 🔄

**测试步骤：**
1. 在同一工单中，点击「手动添加」按钮（维修材料区域）
2. 在新增的空行中，点击「商品名称」下拉框
3. 观察下拉选项展示

**预期结果：**
- [ ] 首次打开下拉时，自动获取当前用户第一个仓库作为 `depotId`
- [ ] 下拉框显示商品列表
- [ ] 选项展示格式：
  ```
  商品名称                库存 XX
  规格 型号
  ```
- [ ] 输入关键字（条码/名称/助记码）后，列表实时过滤
- [ ] 选中商品后，自动回填：`materialId`、`materialName`、`standard`、`model`、`unit`、`unitPrice`
- [ ] 金额自动计算

**实际结果：**
- [ ] 通过 / [ ] 失败（记录问题）

---

### 2.3 从项目库批量导入 🔄

**测试步骤：**
1. 点击「从项目库导入」按钮
2. 在弹窗中勾选多个项目
3. 点击「导入所选」

**预期结果：**
- [ ] 批量添加多行服务项目
- [ ] 每行的 `_projectOptions` 种子数据正确（包含已选项目）
- [ ] 下拉框能正确显示已选项目名称
- [ ] 不与行内下拉搜索冲突

**实际结果：**
- [ ] 通过 / [ ] 失败（记录问题）

---

### 2.4 从商品库批量选择 🔄

**测试步骤：**
1. 点击「从商品库选择」按钮
2. 在弹窗中勾选多个商品
3. 点击「确定」

**预期结果：**
- [ ] 批量添加多行维修材料
- [ ] 每行的 `_materialOptions` 种子数据正确
- [ ] 下拉框能正确显示已选商品名称
- [ ] 库存信息正确展示

**实际结果：**
- [ ] 通过 / [ ] 失败（记录问题）

---

### 2.5 历史数据兼容性 🔄

**测试步骤：**
1. 在数据库中手动创建一条旧工单（`projectId` 和 `materialId` 为 NULL）：
   ```sql
   -- 创建测试工单
   INSERT INTO jsh_work_order (order_no, vehicle_id, customer_name, customer_phone, status, create_time, tenant_id)
   VALUES ('WO-TEST-001', 1, '测试客户', '13800138000', 'pending', NOW(), 63);
   
   -- 添加未关联项目库的服务项目
   INSERT INTO jsh_work_order_project (order_id, project_id, project_name, unit_price, quantity, discount_rate, amount)
   VALUES (LAST_INSERT_ID(), NULL, '手动录入的旧项目', 100.00, 1, 100, 100.00);
   
   -- 添加未关联商品库的维修材料
   INSERT INTO jsh_work_order_material (order_id, material_id, material_name, unit_price, quantity, discount_rate, amount)
   VALUES (LAST_INSERT_ID(), NULL, '手动录入的旧材料', 50.00, 1, 100, 50.00);
   ```
2. 在工单列表中找到该测试工单，点击「编辑」

**预期结果：**
- [ ] 服务项目行显示：`手动录入的旧项目` + 橙色标签「未关联项目库」+ 链接「清空并重选」
- [ ] 维修材料行显示：`手动录入的旧材料` + 橙色标签「未关联商品库」+ 链接「清空并重选」
- [ ] 点击「清空并重选」后，该行变为可选择状态（下拉框）
- [ ] 其他字段（单价、数量等）保持可编辑

**实际结果：**
- [ ] 通过 / [ ] 失败（记录问题）

---

### 2.6 保存校验 🔄

**测试步骤：**
1. 新增工单，添加一行服务项目，但不选择任何项目（保持名称为空或仅输入文本）
2. 点击「确定」保存

**预期结果：**
- [ ] 若服务项目名称非空但 `projectId` 为空，弹出提示：  
   `"存在未关联项目库的服务项目，请在「服务项目名称」中重新选择或清空该行"`
- [ ] 若维修材料名称非空但 `materialId` 为空，弹出提示：  
   `"存在未关联商品库的维修材料，请在「商品名称」中重新选择或清空该行"`
- [ ] 阻止提交，不发送请求

**实际结果：**
- [ ] 通过 / [ ] 失败（记录问题）

---

## 三、边界情况测试 🔄

### 3.1 空库存商品
- [ ] 商品库存为 0 时，下拉选项是否正常显示
- [ ] 是否允许选择库存为 0 的商品

### 3.2 无仓库权限
- [ ] 用户无仓库权限时，`materialDepotId` 是否为 null
- [ ] 商品下拉是否仍能正常工作（不传 `depotId` 参数）

### 3.3 大数据量
- [ ] 项目库超过 50 条时，搜索是否正常
- [ ] 商品库超过 50 条时，搜索是否正常
- [ ] 防抖机制是否生效（300ms）

### 3.4 网络异常
- [ ] 接口请求失败时，下拉框是否显示友好提示
- [ ] Loading 状态是否正确

---

## 四、回归测试 🔄

### 4.1 查看模式
- [ ] 查看已保存的工单，服务项目和维修材料是否正确显示（只读）

### 4.2 编辑模式
- [ ] 编辑已有工单，下拉框是否正确回显已选项
- [ ] 修改后保存，数据是否正确更新

### 4.3 多租户隔离
- [ ] 切换租户后，项目库和商品库是否仅展示当前租户数据

---

## 五、问题记录

### 问题 1：enabled 字段类型不匹配（已修复 ✅）
- **现象**：前端传 `enabled: '1'`，但数据库字段是 `bit(1)` 类型，导致查询结果为空
- **原因**：MyBatis 的 `p.enabled = #{enabled}` 直接比较 bit 值与字符串 '1'，类型不匹配
- **解决方案**：前端移除 `enabled: '1'` 筛选条件，不传该参数（后端默认查询所有未删除项目）
- **修改文件**：`WorkOrderModal.vue` 第 918 行

### 问题 2：（待记录）

---

## 六、测试结论

- [ ] 所有测试通过，改造成功
- [ ] 存在问题，需要修复（见问题记录）

**测试人员签字：**  
**测试日期：**
