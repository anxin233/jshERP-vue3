# 列表搜索框样式异常排查与解决方案

## 1. 结论

前端升级到 Ant Design Vue 4 后，各模块列表页搜索区出现“标签、输入框、按钮不在同一水平基线或列内不对齐”的问题，根因不是某一个列表页字段配置错误，而是旧版 AntD 表单结构和新版 AntD4 表单结构不一致。

当前项目列表查询区域仍大量使用旧写法：

```vue
<a-form layout="inline">
  <a-row :gutter="24">
    <a-col :md="6" :sm="24">
      <a-form-item label="单据编号" :labelCol="labelCol" :wrapperCol="wrapperCol">
        <a-input />
      </a-form-item>
    </a-col>
  </a-row>
</a-form>
```

同时公共样式仍命中 AntD 1.x 的旧 DOM 类名：

```less
.ant-form-item-control-wrapper {
  flex: 1 1;
  display: inline-block;
  vertical-align: middle;
}
```

在 Ant Design Vue 4 中，表单项结构已经改为 `.ant-form-item-label`、`.ant-form-item-control`、`.ant-form-item-control-input` 等新结构。旧的 `.ant-form-item-control-wrapper` 不再是有效的输入区容器，因此搜索框宽度、标签宽度、行高和按钮区域都不能按原来的布局规则对齐。

## 2. 已确认的代码证据

### 2.1 公共搜索区样式仍是旧版结构

文件：`jshERP-web/src/components/page/GlobalLayout.vue`

当前 `.table-page-search-wrapper` 样式中仍存在：

```less
.ant-form-inline {
  .ant-form-item {
    display: flex;
    margin-bottom: 12px;
    margin-right: 0;

    .ant-form-item-control-wrapper {
      flex: 1 1;
      display: inline-block;
      vertical-align: middle;
    }

    > .ant-form-item-label {
      line-height: 32px;
      padding-right: 8px;
      width: auto;
    }
    .ant-form-item-control {
      height: 32px;
      line-height: 32px;
    }
  }
}
```

其中 `.ant-form-item-control-wrapper` 是旧 AntD 表单结构，升级 AntD4 后不应继续作为核心布局选择器。

### 2.2 列表页普遍混用 inline form 和 grid form

已确认 `table-page-search-wrapper` 出现在约 75 个源码文件中，覆盖单据、财务、报表、系统、基础资料、项目、车辆、工单、选择弹窗等模块。

已确认 `layout="inline"` 出现在约 76 处。典型页面包括：

- `jshERP-web/src/views/bill/RetailOutList.vue`
- `jshERP-web/src/views/bill/SaleOutList.vue`
- `jshERP-web/src/views/financial/MoneyInList.vue`
- `jshERP-web/src/views/material/MaterialList.vue`
- `jshERP-web/src/views/system/CustomerList.vue`
- `jshERP-web/src/views/project/ProjectList.vue`
- `jshERP-web/src/views/vehicle/VehicleList.vue`
- `jshERP-web/src/views/workorder/WorkOrderList.vue`

这些页面大多在 `a-row/a-col` 网格中放 `a-form layout="inline"`，并继续使用：

```js
labelCol: {
  span: 5
},
wrapperCol: {
  span: 18,
  offset: 1
}
```

该写法在旧版 AntD 下依赖旧 DOM 和旧样式补偿，升级 AntD4 后会出现列内控件宽度不一致、标签和输入框基线不一致、按钮区域偏移等问题。

### 2.3 按钮区域仍使用浮动式写法

典型列表页中查询按钮区域仍是：

```vue
<span style="float: left;overflow: hidden;" class="table-page-search-submitButtons">
  <a-col :md="6" :sm="24">
    <a-button type="primary">查询</a-button>
    <a-button>重置</a-button>
  </a-col>
</span>
```

问题点：

- `span` 包裹 `a-col` 不是稳定的 grid 结构。
- `float:left` 会脱离正常布局流。
- AntD4 表单和按钮高度、间距变化后，按钮组更容易和输入框不在同一基线。

这也是截图中“查询/重置/收起”和搜索框行高、垂直位置不一致的原因之一。

## 3. 影响范围

本问题会影响所有使用 `.table-page-search-wrapper` 的列表页和选择弹窗，主要包括：

- 零售、采购、销售、仓库、组装拆卸、调拨、其他出入库等单据列表
- 收款、付款、收入、支出、转账、预收款等财务列表
- 商品、客户、供应商、仓库、账户、会员、人员、单位等基础资料列表
- 系统管理中的用户、角色、菜单、字典、租户、日志等列表
- 项目、车辆、工单列表
- 报表查询页面
- 商品、批号、序列号、单据关联等选择弹窗

因此不建议逐个页面手工微调 margin、width 或 labelCol。应先从公共搜索区样式统一修复。

## 4. 建议修复方案

### 4.1 第一阶段：只修公共搜索区样式

目标：不改业务页面结构，先让所有列表页恢复基础对齐。

建议在 `GlobalLayout.vue` 的 `.table-page-search-wrapper` 中将旧 AntD1 选择器替换为 AntD4 结构：

```less
.table-page-search-wrapper {
  .ant-form {
    width: 100%;
  }

  .ant-form-inline {
    width: 100%;

    .ant-form-item {
      display: flex;
      align-items: center;
      width: 100%;
      margin-right: 0;
      margin-bottom: 12px;
    }

    .ant-form-item-label {
      flex: 0 0 76px;
      max-width: 76px;
      padding-right: 8px;
      line-height: 32px;
      text-align: right;
      white-space: nowrap;
    }

    .ant-form-item-label > label {
      height: 32px;
      line-height: 32px;
    }

    .ant-form-item-control {
      flex: 1 1 0;
      min-width: 0;
      line-height: 32px;
    }

    .ant-form-item-control-input {
      min-height: 32px;
    }

    .ant-input,
    .ant-input-number,
    .ant-select,
    .ant-picker {
      width: 100%;
    }
  }

  .table-page-search-submitButtons {
    display: inline-flex;
    align-items: center;
    height: 32px;
    margin-bottom: 12px;
    white-space: nowrap;
  }
}
```

关键点：

- 删除 `.ant-form-item-control-wrapper` 依赖。
- 将搜索项统一变成 `display:flex`。
- 给 label 固定宽度，避免“单据编号”“商品信息”“结算账户”等不同字数造成输入框起点不一致。
- 输入组件统一 `width:100%`。
- 按钮组使用 `inline-flex` 垂直居中，先抵消 `float/span/a-col` 旧结构造成的错位。

### 4.2 第二阶段：规范查询按钮结构

第一阶段通过后，再逐步将旧按钮结构：

```vue
<span style="float: left;overflow: hidden;" class="table-page-search-submitButtons">
  <a-col :md="6" :sm="24">
    ...
  </a-col>
</span>
```

改为更稳定的 grid 写法：

```vue
<a-col :md="6" :sm="24">
  <span class="table-page-search-submitButtons">
    <a-button type="primary" @click="searchQuery">查询</a-button>
    <a-button style="margin-left: 8px" @click="searchReset">重置</a-button>
    <a @click="handleToggleSearch" style="margin-left: 8px">
      {{ toggleSearchStatus ? '收起' : '展开' }}
      <legacy-icon :type="toggleSearchStatus ? 'up' : 'down'" />
    </a>
  </span>
</a-col>
```

该阶段应按模块分批做，不建议一次性全文替换，因为部分报表页面按钮区包含打印、导出、统计文本，结构和普通列表不完全一致。

### 4.3 第三阶段：统一搜索表单结构

长期建议建立统一的 `SearchForm` 或 `SearchToolbar` 规范：

- 搜索区统一使用 `a-row/a-col` 控制布局。
- 不再混用 `layout="inline"` 和 `labelCol/wrapperCol`。
- 普通列表统一 4 列布局：`a-col :md="6" :sm="24"`。
- 窄屏统一单列。
- label 宽度统一由公共 CSS 控制，不在每个页面重复定义。

推荐方向：

```vue
<a-form class="jsh-search-form">
  <a-row :gutter="24">
    <a-col :md="6" :sm="24">
      <a-form-item label="单据编号">
        <a-input v-model:value="queryParam.number" />
      </a-form-item>
    </a-col>
  </a-row>
</a-form>
```

这一步属于结构治理，不建议和第一阶段样式修复混在同一次提交中。

## 5. 实施步骤

1. 修改公共样式，不动业务页面：
   - 文件：`jshERP-web/src/components/page/GlobalLayout.vue`
   - 替换 `.table-page-search-wrapper` 内旧 `.ant-form-item-control-wrapper` 相关规则。
   - 增加 AntD4 `.ant-form-item-control`、`.ant-form-item-control-input`、`.ant-picker`、`.ant-select` 的宽度和 flex 规则。

2. 构建验证：
   ```bash
   cd jshERP-web
   npm run build
   ```

3. 页面验证第一批：
   - `RetailOutList.vue`：零售出库
   - `SaleOutList.vue`：销售出库
   - `PurchaseOrderList.vue`：采购订单
   - `MoneyInList.vue`：收款单
   - `MaterialList.vue`：商品信息
   - `CustomerList.vue`：客户列表
   - `ProjectList.vue`：项目列表
   - `VehicleList.vue`：车辆列表
   - `WorkOrderList.vue`：工单列表

4. 验证通过后，再处理按钮区结构：
   - 优先普通列表页。
   - 暂缓报表页和弹窗页，因为这些页面按钮区结构差异更大。

5. 最后做搜索表单结构治理：
   - 建立统一搜索区组件或样式规范。
   - 逐步删除各页面重复的 `labelCol/wrapperCol`。
   - 将搜索区从“旧 inline form 兼容”迁移到“AntD4 grid form 标准写法”。

## 6. 验收标准

### 静态检查

```bash
rg -n 'ant-form-item-control-wrapper' jshERP-web/src
```

预期：

- 公共搜索区样式中不再出现该旧选择器。
- 如果仍有命中，需要确认不是用于兼容历史文档或注释。

```bash
rg -n 'table-page-search-wrapper' jshERP-web/src
```

用于确认后续需要逐步治理的列表页范围。

### 构建验证

```bash
cd jshERP-web
npm run build
```

预期：构建通过。

### 页面验证

重点检查：

- 第一行和展开行的 label 垂直居中。
- 同一列内输入框起点一致。
- 输入框、下拉框、日期范围框宽度一致。
- 查询、重置、展开/收起按钮与第一行搜索框垂直对齐。
- 展开/收起后布局不跳动。
- 窄屏下搜索项变为单列，不出现输入框挤压或按钮换行错位。

## 7. 注意事项

- 不建议在每个列表页单独调 `labelCol/wrapperCol`，这会扩大维护成本，而且不能解决 AntD4 DOM 变化的根因。
- 不建议继续依赖 `.ant-form-item-control-wrapper`，这是旧版 AntD 表单结构。
- 不建议第一阶段同时重构所有查询按钮 DOM，报表和弹窗页面结构差异较大，容易引入额外行为问题。
- 本次问题属于 AntD4 样式适配问题，不涉及后端接口、查询参数或列表数据逻辑。

