# 复制为 .env.local 后执行 yarn build，用于子路径部署（如 https://域名/erp/）
# publicPath 必须以 / 开头和结尾
VUE_APP_PUBLIC_PATH=/erp/
# 须与 Nginx 反代路径一致：浏览器请求 /erp/jshERP-boot/... -> 后端 /jshERP-boot/...
VUE_APP_API_BASE=/erp/jshERP-boot
