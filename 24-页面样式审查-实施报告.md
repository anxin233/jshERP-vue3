# 24-页面样式审查 — 实施报告

| 项 | 内容 |
| --- | --- |
| 依据文档 | [24-页面样式审查-问题优化方向与实施步骤.md](./24-页面样式审查-问题优化方向与实施步骤.md) |
| 实施分支 | `feature/vue3.0` |
| 实施日期 | 2026-07-20 |
| 范围 | 阶段 1～7 公共层落地；阶段 3 全量列表迁移未做完（见未做项） |

---

## 1. 实际取值（阶段 1）

| Token / 常量 | 取值 | 落点 |
| --- | --- | --- |
| Header / Logo 高度 | **49px** | `--jsh-header-height` / `--jsh-logo-height`、`config/layout.js` |
| 侧栏展开宽 | **180px** | `--jsh-sider-width`、`SIDER_WIDTH` |
| 侧栏折叠宽 | **80px** | `--jsh-sider-collapsed-width` |
| 主色 | **#1890FF** | `PRIMARY_COLOR`、`ConfigProvider.token.colorPrimary`、Vite `modifyVars` |

---

## 2. 各阶段完成情况

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| 0 准备 | 已做 | 在 `feature/vue3.0` 上实施 |
| 1 布局 Token | **已完成** | `layout-tokens.less` + `config/layout.js`；`GlobalLayout` / `GlobalHeader` / `Logo` / `SideMenu` 统一引用 |
| 2 主题 Token | **已完成** | `App.vue` 增加 `a-config-provider :theme`；light=浅底深字、dark=深底浅字；去掉顶栏图标写死白色 |
| 3 列表搜索 | **部分完成** | 新增 `ListSearchBar.vue`；**ProjectList** 已样例改造；其余 ~75 列表待分批 B1～B4 |
| 4 表格/弹窗 | **已完成** | td padding 8px；遮罩 0.45；modal max-height；depot-mask 去魔法数 |
| 5 页签/侧栏 | **已完成** | `page-toggle` 过渡对齐；页签减少 `!important`；侧栏细滚动条可见 |
| 6 float/内联 | **部分完成** | `.bill-toolbar` / `.dropdown-btn` flex；工单状态按钮 class；全量 float 清理未做 |
| 7 文档 | **本报告** | 见本文 |

---

## 3. 关键文件清单

| 动作 | 路径 |
| --- | --- |
| 新增 | `jshERP-web/src/assets/less/layout-tokens.less` |
| 新增 | `jshERP-web/src/config/layout.js` |
| 新增 | `jshERP-web/src/components/list/ListSearchBar.vue` |
| 修改 | `main.js`（引入 tokens + common.less） |
| 修改 | `App.vue`、`defaultSettings.js`、`vite.config.js` |
| 修改 | `GlobalLayout.vue`、`GlobalHeader.vue`、`Logo.vue`、`SideMenu.vue` |
| 修改 | `TabLayout.vue`、`common.less` |
| 修改 | `WorkOrderList.vue`、`ProjectList.vue` |

---

## 4. 视觉变更说明（相对改造前）

1. **顶栏默认 light**：由「主色底 + 白字」改为「白底 + 深字」（与 Ant Design light 语义一致）。若需恢复主色顶栏，可在 `layout-tokens.less` 将 `--jsh-header-bg-light` 设为 `var(--jsh-color-primary)`、`--jsh-header-color-light` 设为 `#fff`。
2. **侧栏宽度 150→180**：中文菜单更宽裕。
3. **表格行更紧凑**：td 上下 padding 15→8。
4. **弹窗遮罩更深**：0.1→0.45。

---

## 5. 未做项 / 后续

| 项 | 建议 |
| --- | --- |
| 阶段 3 全量列表迁移 | 按文档 B1→B4 分批替换为 `ListSearchBar`，去掉冲突 `labelCol` |
| 阶段 6 单据 Modal float | 扫码区/金额行逐步改 `.bill-toolbar` |
| 界面设置动态换肤 | 已打通 `store.app.color` → ConfigProvider；若 SettingDrawer 未启用需产品确认 |
| 手工回归 | 登录 → 首页 → 商品/项目列表 → 工单 → 侧栏折叠 → 多页签 → 大弹窗 |

---

## 6. 回归检查表（请本地勾选）

- [ ] 后端 9999 + 前端 3000 可登录
- [ ] 固定头下列表首行不被挡
- [ ] 侧栏展开/折叠，顶栏与内容左边距同步
- [ ] Logo 与顶栏高度齐平
- [ ] light/dark 切换（若开启）无反色
- [ ] 项目列表查询/重置正常
- [ ] 工单状态按钮样式正常
- [ ] 弹窗遮罩可见、内容可滚动

---

**文档结束。**
