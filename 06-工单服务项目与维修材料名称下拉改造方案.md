# 工单「服务项目 / 维修材料」名称改为库内下拉：改造方案

> 文档性质：设计与实施说明（不含具体代码补丁）。  
> 适用模块：`jshERP-web/src/views/workorder/modules/WorkOrderModal.vue` 及关联接口、弹窗组件。  
> 版本：v1.0

---

## 1. 需求摘要

| 场景 | 现状 | 目标 |
|------|------|------|
| 服务项目 | 「添加项目」空行后，**名称**为普通输入框，可任意填写 | 空行（及编辑场景下需统一时）**名称**为**下拉框**，选项来自**项目库**（与「从项目库导入」同一数据源），**不允许**随意填写 |
| 维修材料 | 「手动添加」空行后，**商品名称**为普通输入框 | **名称**为**下拉框**，选项来自**商品库**，选项需展示**库存**，并支持**筛选/搜索**，**不允许**随意填写 |

补充期望（由产品确认）：

- 「添加项目 / 手动添加」仍保留：点击后新增一行，该行「名称」列进入**必选库项**的交互（下拉），而不是开放文本。

---

## 2. 现状与代码锚点（便于落地）

### 2.1 前端

- **工单弹窗**：`WorkOrderModal.vue`
  - 服务项目：`addProjectRow()` 推入 `projectId: null`、`projectName: ''`，名称列模板 `projectName` 使用 `<a-input v-model="record.projectName" />`。
  - 维修材料：`addMaterialRow()` 推入 `materialId: null`、`materialName: ''` 等，名称列 `materialName` 使用 `<a-input v-model="record.materialName" />`。
  - 已有库选择能力：
    - **项目**：`ProjectSelectModal.vue` → `GET /project/list`（分页 + `search.name`）。
    - **商品**：`JSelectMaterialModal.vue`（`openMaterialSelect`）→ 商品列表接口，表格含 **库存** 等列，支持多条件筛选。

### 2.2 后端（数据模型已支持关联 ID）

- `WorkOrderProject`：`projectId`（关联 `jsh_project`）、`projectName` 等；注释已说明允许为空（历史手动录入）。
- `WorkOrderMaterial`：`materialId`（关联 `jsh_material`）、`materialName`、`standard`、`unit` 等。

保存时 `projects` / `materials` 随工单提交；后端若已按 `projectId` / `materialId` 做校验或冗余名称，需与产品确认是否在**强制库选**后增加服务端校验（见下文）。

---

## 3. 方案总览

### 3.1 交互原则

1. **名称列**：在非查看模式下，将「输入框」替换为 **`a-select`（建议 `show-search`）** 或 **可搜索下拉 + 远程分页**（商品数据量大时推荐远程）。
2. **选中项后**：写入 `projectId` / `materialId`，并**回填**名称、单价、规格、单位等（与现有 `onProjectsSelected` / `onMaterialsSelected` 逻辑对齐，避免重复造字段规则）。
3. **禁止自由录入**：下拉仅允许从选项中选择；不采用可输入的 `combobox` 混输（除非产品明确要「其它」类例外，本方案按**不允许**处理）。

### 3.2 服务项目（项目库）

- **选项来源**：与 `ProjectSelectModal` 一致，调用 **`/project/list`**（或后端提供的**轻量列表接口**，若全量过大再议）。
- **筛选**：
  - 数据量小：`a-select` + `showSearch` + `filterOption` 按名称/助记字段过滤。
  - 数据量大：远程搜索（输入关键字防抖后请求 `name` 条件），下拉仅展示当前页结果（需与后端约定分页参数）。
- **展示字段**：至少「项目名称」；可选展示类别、参考价（与列表列一致即可）。
- **空行状态**：`projectId == null` 时显示占位「请选择服务项目」；选中后 `projectId` 有值，`projectName` 等与库一致。

### 3.3 维修材料（商品库）

- **选项来源**：与 `JSelectMaterialModal` 一致的数据能力（商品列表接口，需带 **库存** 字段）。
- **库存展示**：
  - 列表类接口返回的 `stock` 多为**当前查询条件下、按仓库等聚合后的库存**（以现有 `JSelectMaterialModal` 列定义为准）。
  - **待确认**：库存是**全仓合计**还是**指定仓库**？若按仓，是否在下拉旁增加「仓库」筛选或与弹窗一致在查询参数中带 `depotId`。
- **筛选**：
  - 最小实现：关键词搜索（条码/名称/助记码）+ 与现有商品选择器相同的筛选项（规格型号、仓库、类别等）——可二选一：
    - **A**：行内下拉只做**远程搜索 + 精简展示**（名称 + 库存 + 规格）；
    - **B**：行内仅「选择」按钮唤起 **复用 `JSelectMaterialModal` 单选模式**（若组件支持），避免表格内塞满筛选项。
- **回填**：选中后与 `onMaterialsSelected` 一致：写入 `materialId`、`materialName`、`standard`、`unit`、`unitPrice`（如 `commodityDecimal`）等。

### 3.4 与现有按钮的关系

- **「从项目库导入」/「从商品库选择」**：当前为批量弹窗多选。改造后：
  - **方案 1（推荐）**：保留按钮，行为不变；行内下拉作为**快捷单条**录入，减少反复打开弹窗。
  - **方案 2**：若产品希望界面极简，可隐藏「手动添加」类按钮，仅保留库选——**需产品签字**，本方案默认 **方案 1**。

---

## 4. 后端与数据（建议）

1. **校验（可选但推荐）**：保存工单时，若业务要求「名称必须来自库」，则：
   - `projectId` / `materialId` 非空；
   - 或后端根据 ID 反查名称与前端提交冗余字段一致性校验。
2. **性能**：若 `/project/list`、`/material/list` 默认 payload 过大，可增加：
   - `GET /project/options?keyword=`  
   - `GET /material/options?keyword=&depotId=`  
   仅返回下拉所需字段（id、name、price、stock、standard、unit）。
3. **历史数据**：已存在 `projectId`/`materialId` 为空的旧工单，**编辑**时是否强制改为库选——需产品规则（允许只读展示旧文本 vs 必须重选）。

---

## 5. 实施步骤建议（任务拆分）

| 序号 | 任务 | 说明 |
|------|------|------|
| 1 | 产品/业务确认 | 确认下文「待确认问题」 |
| 2 | 接口评估 | 确认现有 `/project/list`、商品列表是否满足搜索+库存；不足则加轻量接口 |
| 3 | 服务项目列 | `projectName` 槽位改为 `a-select`，绑定 `projectId`，`change` 时拉详情或直接用列表行数据回填单价等 |
| 4 | 维修材料列 | `materialName` 槽位改为可搜索下拉或弹窗单选，展示库存列于 option 或表格二次确认 |
| 5 | 校验 | 提交前校验每行服务项目、材料已选库项（若业务要求） |
| 6 | 回归 | 查看模式、编辑回填、`loadDetail` 与 `projectId`/`materialId` 兼容；与导入按钮并存场景 |

---

## 6. 待确认问题（请业务/产品答复）

1. **库存口径**：下拉中展示的「库存」是**全仓总和**，还是**指定某一默认仓库**，还是**用户可选仓库后再看库存**？
2. **多规格 / 多属性商品**：商品存在多 SKU 时，工单材料行是否必须选到**具体条码/SKU**？当前 `WorkOrderMaterial` 是否需扩展字段（如 `materialExtendId` / `sku`）——若仅选主物料是否足够？
3. **无库存是否允许选择**：是否允许负库存开单（与系统「负库存」参数是否联动）？
4. **历史工单**：旧数据名称手填、无 ID，编辑时是否允许继续保存为「仅名称」还是必须重选？
5. **服务项目**：项目库是否需过滤「仅启用」或按类别可见范围（租户/角色）？

---

## 7. 风险与注意点

- **表格内嵌复杂下拉**：行数多时每行远程请求可能造成压力，建议防抖、缓存关键字结果、或限制并发。
- **与「从商品库选择」体验一致**：避免行内展示的库存、价格与弹窗选择结果不一致（同一套 API 与查询条件）。
- **仅前端限制「不可输入」**：仍建议关键业务在后端校验 `projectId`/`materialId`，防止接口绕过。

---

## 8. 文档维护

- 实施完成后请将：最终采用的接口路径、库存口径、是否保留双入口（行内+弹窗）写回本文档「修订记录」。

**修订记录**

| 日期 | 版本 | 说明 |
|------|------|------|
| （填） | v1.0 | 初稿 |
