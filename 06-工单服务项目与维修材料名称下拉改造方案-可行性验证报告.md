# 工单「服务项目 / 维修材料」名称改为库内下拉：可行性验证报告

> 验证日期：2026-04-18  
> 验证人：Claude  
> 原方案文档：`06-工单服务项目与维修材料名称下拉改造方案.md`

---

## 一、验证结论

**✅ 方案整体可行**，现有代码架构完全支持该改造，具备以下基础：

1. **数据模型已支持**：`WorkOrderProject` 和 `WorkOrderMaterial` 均有 `projectId` / `materialId` 字段，允许为空（兼容历史数据）
2. **前端组件已存在**：`ProjectSelectModal` 和 `JSelectMaterialModal` 已实现库选功能，可复用或改造
3. **后端接口完备**：`/project/list` 和 `/material/findBySelect` 接口支持搜索、分页、库存查询
4. **回填逻辑清晰**：现有 `onProjectsSelected` 和 `onMaterialsSelected` 方法已实现字段回填，可作为下拉选择的参考模板

---

## 二、现状代码验证

### 2.1 前端现状（WorkOrderModal.vue）

#### 服务项目部分
- **当前实现**：
  - 第 214-217 行：`projectName` 使用 `<a-input>` 普通输入框
  - 第 693-699 行：`addProjectRow()` 添加空行，`projectId: null, projectName: ''`
  - 第 677-691 行：`onProjectsSelected()` 从项目库导入时，回填 `projectId`、`projectName`、`unitPrice` 等

- **验证结果**：✅ 可直接改造
  - 将 `<a-input>` 替换为 `<a-select>` 即可
  - 已有 `ProjectSelectModal` 组件（第 203 行按钮调用），接口为 `/project/list`

#### 维修材料部分
- **当前实现**：
  - 第 272-275 行：`materialName` 使用 `<a-input>` 普通输入框
  - 第 732-738 行：`addMaterialRow()` 添加空行，`materialId: null, materialName: ''`
  - 第 713-728 行：`onMaterialsSelected()` 从商品库选择时，回填 `materialId`、`materialName`、`standard`、`unit`、`unitPrice` 等

- **验证结果**：✅ 可直接改造
  - 将 `<a-input>` 替换为 `<a-select>` 或复用 `JSelectMaterialModal` 单选模式
  - 已有 `MaterialSelectModal` 组件（第 261 行按钮调用），接口为 `/material/findBySelect`

### 2.2 后端现状

#### 项目接口（ProjectController.java）
- **接口路径**：`GET /project/list`（第 55-65 行）
- **支持参数**：
  - `search.name`：项目名称搜索
  - `search.categoryId`：类别筛选
  - `search.enabled`：启用状态筛选
- **返回数据**：`List<ProjectEx>`，包含 `id`、`name`、`totalPrice`、`categoryName`、`description` 等
- **验证结果**：✅ 完全满足下拉选项需求，支持分页和搜索

#### 商品接口（MaterialController）
- **接口路径**：`GET /material/findBySelect`（api.js 第 59 行）
- **支持参数**：
  - `q`：关键词（条码/名称/助记码）
  - `standardOrModel`：规格型号
  - `depotId`：仓库筛选
  - `color`、`brand`、`categoryId`、`mfrs` 等扩展筛选
- **返回数据**：包含 `id`、`name`、`standard`、`model`、`stock`（库存）、`commodityDecimal`（单价）等
- **验证结果**：✅ 完全满足下拉选项需求，**已包含库存字段**

#### 数据模型（Entity）
- **WorkOrderProject**（第 8-9 行）：
  ```java
  /** 关联 jsh_project，允许为空（手动录入时不关联） */
  private Long projectId;
  private String projectName;
  ```
- **WorkOrderMaterial**（第 8-10 行）：
  ```java
  /** 关联 jsh_material，允许为空（手动录入时不关联） */
  private Long materialId;
  private String materialName;
  ```
- **验证结果**：✅ 数据模型已支持 ID 关联，注释明确说明允许为空（兼容历史数据）

### 2.3 商品选择组件（JSelectMaterialModal.vue）

- **功能验证**：
  - 第 111-122 行：表格支持多选/单选（`:rowSelection` 的 `type` 可配置）
  - 第 193-204 行：列定义包含 `stock`（库存）列
  - 第 20-98 行：支持多条件筛选（关键词、规格型号、仓库、颜色、品牌、类别等）
  - 第 267-280 行：`loadMaterialData()` 调用 `getMaterialBySelect` 接口，支持分页和搜索

- **验证结果**：✅ 组件功能完备，**已展示库存**，可直接复用或改造为单选模式

---

## 三、方案实施路径

### 3.1 服务项目改造（推荐方案）

**方案 A：行内远程搜索下拉（推荐）**

```vue
<!-- WorkOrderModal.vue 第 214-217 行替换为 -->
<template slot="projectName" slot-scope="text, record">
  <a-select
    v-if="!isViewMode"
    v-model="record.projectId"
    show-search
    :filter-option="false"
    :not-found-content="projectSearchLoading ? undefined : '无匹配项目'"
    placeholder="请选择服务项目"
    style="width:100%"
    @search="onProjectSearch"
    @change="(val) => onProjectChange(val, record)"
    allow-clear>
    <a-spin v-if="projectSearchLoading" slot="notFoundContent" size="small" />
    <a-select-option
      v-for="p in projectOptions"
      :key="p.id"
      :value="p.id">
      {{ p.name }} <span style="color:#999">(¥{{ p.totalPrice }})</span>
    </a-select-option>
  </a-select>
  <span v-else>{{ text }}</span>
</template>
```

**配套方法**：
```javascript
data() {
  return {
    projectOptions: [],
    projectSearchLoading: false,
    projectSearchTimer: null
  }
},
methods: {
  onProjectSearch(keyword) {
    clearTimeout(this.projectSearchTimer)
    this.projectSearchTimer = setTimeout(() => {
      this.loadProjectOptions(keyword)
    }, 300) // 防抖 300ms
  },
  loadProjectOptions(keyword) {
    this.projectSearchLoading = true
    const searchObj = keyword ? { name: keyword } : {}
    getAction('/project/list', {
      currentPage: 1,
      pageSize: 20,
      search: JSON.stringify(searchObj)
    }).then(res => {
      if (res.code === 200) {
        this.projectOptions = res.data.rows || []
      }
    }).finally(() => {
      this.projectSearchLoading = false
    })
  },
  onProjectChange(projectId, record) {
    const project = this.projectOptions.find(p => p.id === projectId)
    if (project) {
      record.projectId = project.id
      record.projectName = project.name
      record.unitPrice = project.totalPrice || 0
      this.recalcProject(record)
    } else {
      record.projectId = null
      record.projectName = ''
    }
  }
}
```

**优点**：
- 用户体验好，输入即搜索
- 减少弹窗操作，提升录入效率
- 与现有「从项目库导入」按钮并存，互不冲突

### 3.2 维修材料改造（推荐方案）

**方案 B：复用弹窗单选模式（推荐）**

由于商品数据量大、筛选条件多（仓库、类别、规格等），且需展示库存，建议：

```vue
<!-- WorkOrderModal.vue 第 272-275 行替换为 -->
<template slot="materialName" slot-scope="text, record">
  <div v-if="!isViewMode" style="display:flex;align-items:center">
    <a-input
      v-model="record.materialName"
      placeholder="请选择商品"
      readonly
      style="flex:1;cursor:pointer"
      @click="openMaterialSelectForRow(record)" />
    <a-button
      size="small"
      icon="search"
      @click="openMaterialSelectForRow(record)"
      style="margin-left:4px" />
  </div>
  <span v-else>{{ text }}</span>
</template>
```

**配套方法**：
```javascript
data() {
  return {
    currentEditingMaterialRow: null // 记录当前编辑的行
  }
},
methods: {
  openMaterialSelectForRow(record) {
    this.currentEditingMaterialRow = record
    this.$refs.materialModal.show()
    this.$refs.materialModal.disableMultiSelect() // 需在组件内增加单选模式
  },
  onMaterialsSelected(materials) {
    if (this.currentEditingMaterialRow && materials.length === 1) {
      // 单行编辑模式
      const m = materials[0]
      this.currentEditingMaterialRow.materialId = m.id
      this.currentEditingMaterialRow.materialName = m.name
      this.currentEditingMaterialRow.standard = m.standard || ''
      this.currentEditingMaterialRow.model = m.model || ''
      this.currentEditingMaterialRow.unit = m.unit || ''
      this.currentEditingMaterialRow.unitPrice = m.commodityDecimal || 0
      this.recalcMaterial(this.currentEditingMaterialRow)
      this.currentEditingMaterialRow = null
    } else {
      // 批量导入模式（原逻辑）
      materials.forEach(m => {
        if (!this.materialItems.find(i => i.materialId === m.id)) {
          this.materialItems.push({
            rowKey: 'm_' + Date.now() + Math.random(),
            materialId: m.id,
            materialName: m.name,
            standard: m.standard || '',
            model: m.model || '',
            unit: m.unit || '',
            unitPrice: m.commodityDecimal || 0,
            quantity: 1,
            discountRate: 100,
            amount: this.calcAmount(m.commodityDecimal || 0, 1, 100),
            remark: ''
          })
        }
      })
    }
  }
}
```

**优点**：
- 复用现有 `JSelectMaterialModal` 组件，**已展示库存**
- 支持复杂筛选（仓库、类别、规格等），满足业务需求
- 避免表格内嵌复杂下拉，性能更好

---

## 四、待确认问题（对应原方案第 6 节）

### 4.1 库存口径 ⚠️ **需产品确认**
- **问题**：`JSelectMaterialModal` 的 `stock` 字段是**全仓总和**还是**指定仓库**？
- **验证结果**：
  - 组件支持 `depotId` 筛选（第 30-37 行）
  - 接口 `/material/findBySelect` 支持 `depotId` 参数
  - **推测**：库存为**按仓库筛选后的库存**，若不选仓库则为全仓总和
- **建议**：与产品确认默认展示逻辑，是否需要在工单中增加「默认仓库」字段

### 4.2 多规格商品 ⚠️ **需产品确认**
- **问题**：商品存在多 SKU 时，是否需要选到具体条码？
- **验证结果**：
  - `WorkOrderMaterial` 仅有 `materialId`，无 `materialExtendId` 或 `sku` 字段
  - 当前设计为**主物料级别**，不区分 SKU
- **建议**：若需支持多 SKU，需扩展数据模型

### 4.3 无库存是否允许选择 ⚠️ **需产品确认**
- **问题**：库存为 0 或负数时，是否允许选择？
- **验证结果**：当前 `JSelectMaterialModal` 无库存校验逻辑
- **建议**：与产品确认是否需要前端提示或后端校验

### 4.4 历史工单编辑 ⚠️ **需产品确认**
- **问题**：旧数据 `projectId`/`materialId` 为空，编辑时是否强制重选？
- **验证结果**：数据模型注释明确「允许为空」，支持兼容
- **建议**：
  - **方案 1**：编辑时若 ID 为空，显示只读文本（不可改）
  - **方案 2**：编辑时强制重选（需产品签字）

### 4.5 服务项目过滤 ⚠️ **需产品确认**
- **问题**：项目库是否需过滤「仅启用」或按租户/角色可见范围？
- **验证结果**：`/project/list` 接口支持 `search.enabled` 参数
- **建议**：默认仅展示启用项目（`enabled=1`）

---

## 五、风险与注意事项

### 5.1 性能风险（中等）
- **问题**：服务项目若采用行内远程搜索，多行同时编辑时可能产生并发请求
- **缓解措施**：
  - 防抖 300ms
  - 缓存已搜索的关键字结果
  - 限制并发请求数（如使用 `axios` 的 `CancelToken`）

### 5.2 用户体验风险（低）
- **问题**：从「自由输入」改为「强制库选」，可能影响用户习惯
- **缓解措施**：
  - 保留「从项目库导入」/「从商品库选择」按钮，提供双入口
  - 下拉支持模糊搜索，降低查找成本
  - 若库中无匹配项，提供「新增」快捷入口（如 `JSelectMaterialModal` 第 44 行已有）

### 5.3 数据一致性风险（低）
- **问题**：前端限制「不可输入」，但接口仍可能被绕过
- **缓解措施**：
  - **强烈建议**在后端 `WorkOrderService` 保存时增加校验：
    ```java
    if (project.getProjectId() == null) {
        throw new BusinessRunTimeException("服务项目必须从项目库选择");
    }
    if (material.getMaterialId() == null) {
        throw new BusinessRunTimeException("维修材料必须从商品库选择");
    }
    ```

---

## 六、实施建议

### 6.1 分阶段实施（推荐）

**阶段 1：服务项目改造（1-2 天）**
1. 实现行内远程搜索下拉
2. 回归测试：新增、编辑、查看、导入按钮并存场景
3. 验证历史数据兼容性

**阶段 2：维修材料改造（2-3 天）**
1. 改造 `JSelectMaterialModal` 支持单选模式
2. 实现行内「选择」按钮唤起弹窗
3. 回归测试：库存展示、仓库筛选、批量导入

**阶段 3：后端校验（1 天）**
1. 增加 `projectId`/`materialId` 非空校验
2. 增加 ID 有效性校验（防止传入不存在的 ID）

**阶段 4：产品验收（1 天）**
1. 确认待确认问题（第四节）
2. 用户培训与文档更新

### 6.2 回归测试清单

- [ ] 新增工单：服务项目下拉搜索、选择、回填
- [ ] 新增工单：维修材料弹窗选择、库存展示、回填
- [ ] 编辑工单：已有数据正常回显
- [ ] 编辑工单：历史数据（ID 为空）兼容处理
- [ ] 查看模式：只读展示正常
- [ ] 「从项目库导入」按钮：与行内下拉并存，无冲突
- [ ] 「从商品库选择」按钮：与行内选择并存，无冲突
- [ ] 提交保存：数据完整性校验
- [ ] 多租户：租户隔离正常（项目库、商品库仅展示本租户数据）

---

## 七、总结

### ✅ 可行性结论
**方案完全可行**，现有代码架构、数据模型、接口能力均已具备，改造成本低、风险可控。

### 📋 关键依赖
1. **产品确认**：第四节 5 个待确认问题
2. **后端支持**：建议增加保存时的 ID 校验（可选但推荐）
3. **前端改造**：预计 4-5 天开发 + 1 天测试

### 🎯 推荐方案
- **服务项目**：行内远程搜索下拉（方案 A）
- **维修材料**：复用弹窗单选模式（方案 B）
- **双入口保留**：「添加项目」/「手动添加」+ 「从库导入」/「从库选择」并存

### 📌 下一步行动
1. 产品经理确认第四节待确认问题
2. 技术评审通过后，按阶段 1 → 2 → 3 → 4 实施
3. 实施完成后更新原方案文档「修订记录」
