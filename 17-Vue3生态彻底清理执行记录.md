# Vue3 生态彻底清理执行记录

## 1. 目标

本轮目标是在已经完成 Vite、Vue3、Ant Design Vue 4 改造的基础上，继续清理 `jshERP-web` 中残留的 Vue2 生态依赖、Vue2 模板写法、Vue CLI/Webpack 旧打包痕迹，并完成至少三轮源码审查与构建验证。

## 2. 本轮实施计划

1. 删除无源码引用的 Vue2 生态依赖。
2. 将地区组件从 `vue-area-linkage` 替换为 Ant Design Vue 4 `a-cascader`。
3. 将列表列宽拖拽从 `vue-draggable-resizable` 替换为本地 Vue3 组件。
4. 将可编辑表格中的 `vuedraggable` 与 `jquery` 替换为原生 HTML5 拖拽和原生 DOM 事件。
5. 将 `vue-print-nb-jeecg` 替换为本地 Vue3 `v-print` 指令插件。
6. 清理文档、脚本、注释中的旧 Vue2/Vue CLI 迁移残留。
7. 执行三轮复扫与构建验证。

## 3. 已完成改造

### 3.1 删除无引用依赖

已删除以下源码无直接引用的 Vue2 生态包：

- `vue-i18n`
- `@tinymce/tinymce-vue`
- `vue-cropper`
- `vue-splitpane`

验证结果：`npm run build` 通过。

### 3.2 地区组件 Vue3 化

涉及文件：

- `jshERP-web/src/components/jeecg/JAreaLinkage.vue`
- `jshERP-web/src/main.js`

处理内容：

- 删除 `vue-area-linkage` 插件注册。
- 删除旧地区组件样式入口。
- 使用 `area-data` + Ant Design Vue 4 `a-cascader` 实现地区选择。
- 保持 `change/input/update:value/update:modelValue` 事件输出，兼容原调用方。
- 卸载 `vue-area-linkage`。

验证结果：`npm run build` 通过。

### 3.3 列宽拖拽 Vue3 化

涉及文件：

- `jshERP-web/src/components/table/ColumnResizeHandle.vue`
- `jshERP-web/src/mixins/JeecgListMixin.js`

处理内容：

- 新增本地 Vue3 原生列宽拖拽组件。
- `JeecgListMixin.js` 不再引用 `vue-draggable-resizable`。
- 卸载 `vue-draggable-resizable`。

验证结果：`npm run build` 通过。

### 3.4 可编辑表格 Vue3 化

涉及文件：

- `jshERP-web/src/components/jeecg/JEditableTable.vue`

处理内容：

- 删除 `vuedraggable` 包裹结构。
- 使用原生 `draggable`、`dragstart`、`dragover`、`drop`、`dragend` 实现行拖拽。
- 保留 `rowResort()`、`emitDragged()`、排序字段重算和表单值同步逻辑。
- 删除 `jquery` 引用。
- 使用原生 DOM 事件委托替换回车后自动跳到下一个输入框逻辑。
- 在 `beforeUnmount` 中解绑输入跳转事件。
- 卸载 `vuedraggable` 与 `jquery`。

验证结果：`npm run build` 通过。

### 3.5 打印插件 Vue3 化

涉及文件：

- `jshERP-web/src/utils/print.js`
- `jshERP-web/src/main.js`

处理内容：

- 新增本地 Vue3 `v-print` 指令插件。
- 支持 `v-print="'#selector'"` 局部打印。
- 打印前复制 input、textarea、select、canvas 当前状态。
- 支持 `ignore-print="true"` 与 `ignoreprint="true"` 排除打印元素。
- `main.js` 改为引用 `@/utils/print`。
- 卸载 `vue-print-nb-jeecg`。

验证结果：`npm run build` 通过。

### 3.6 Vue3 组件关系清理

涉及文件：

- `jshERP-web/src/components/AvatarList/List.vue`
- `jshERP-web/src/components/AvatarList/Item.vue`

处理内容：

- 删除子组件直接读取 `$parent.size` 的 Vue2 风格耦合。
- 父组件通过 `provide` 提供尺寸，子组件通过 `inject` 读取。

验证结果：`npm run build` 通过。

### 3.7 旧迁移残留清理

处理内容：

- 删除已完成使命的旧迁移脚本：
  - `jshERP-web/scripts/migrate-v-decorator.js`
  - `jshERP-web/scripts/finish-remaining-form-migration.js`
- 更新 `jshERP-web/docs/ANTDV4_MIGRATION_STATUS.md`，移除旧 Vue2 依赖待办描述。
- 更新 `jshERP-web/README.md`，移除 `vue-cropper` 旧技术栈条目。
- 更新 `jshERP-web/index.html` 与 `jshERP-web/src/utils/request.js` 中 Vue CLI/Vue config 历史注释。
- 更新 `jshERP-web/public/static/color.less` 中含 `jquery` 字样的历史算法注释。

验证结果：`npm run build` 通过。

## 4. 三轮审查结果

### 第一轮：依赖与源码引用审查

扫描范围：`jshERP-web/src`、`package.json`。

结果：

- `slot-scope`: 0
- `scopedSlots`: 0
- `v-decorator`: 0
- `this.$form`: 0
- `$listeners`: 0
- `$scopedSlots`: 0
- `<a-icon`: 0
- Vue2 生态包源码引用：已清零

发现并修复：

- `JEditableTable.vue` 残留 `vuedraggable/jquery`。
- `vue-print-nb-jeecg` 仍作为 Vue2 插件注册。
- `AvatarList/Item.vue` 使用 `$parent.size`。

### 第二轮：Vite 与 Vue3 适配审查

扫描范围：源码、配置、HTML 模板、公共静态目录。

结果：

- `@vue/compat`: 0
- `vue-template-compiler`: 0
- `@vue/cli`: 0
- `vue-loader`: 0
- `require.context`: 0
- `process.env.VUE_APP`: 0
- 项目源码中的 `webpack`/`vue.config.js` 旧打包痕迹：已清零

发现并修复：

- `index.html` 旧注释提到 `vue.config.js publicPath`。
- `request.js` 旧注释提到 `vue.config.js`。

### 第三轮：全项目旧生态关键字复扫

扫描范围：全项目，排除 `package-lock.json` 第三方元数据。

结果：

- `vue-i18n`: 0
- `vuedraggable`: 0
- `vue-draggable-resizable`: 0
- `vue-area-linkage`: 0
- `vue-print-nb-jeecg`: 0
- `vue-cropper`: 0
- `vue-splitpane`: 0
- `@tinymce/tinymce-vue`: 0
- `jquery`: 0
- Vue2 模板与实例 API 关键字：0

说明：

- `package-lock.json` 中 `tapable` 的 funding URL 包含 `https://opencollective.com/webpack`，这是第三方包元数据，不是项目打包配置或源码引用，未修改。

## 5. 构建与冒烟验证

已执行：

```bash
cd jshERP-web
npm run build
```

结果：通过。

已执行 Vite preview 冒烟：

- `http://127.0.0.1:4173/erp/`: 200
- `http://127.0.0.1:4173/erp/static/favicon.ico`: 200
- `http://127.0.0.1:4173/erp/static/translate.js`: 200

预览服务验证后已停止。

构建仍存在的非阻断提示：

- `intro.js` CSS 中旧 IE hack `*display: inline` 被 Lightning CSS 提示。
- 部分 chunk 大于 500 KB。
- 本机 npm 用户配置中若干旧镜像字段提示将来版本不再支持。

这些提示不是 Vue2 生态残留，也不是本轮构建失败项。

## 6. 仍建议单独专项处理的非 Vue2 依赖

以下依赖不属于 Vue2 生态，但版本较旧，建议后续单独升级并做业务回归：

- `axios`: 当前 0.18.x，升级到 1.x 需要检查拦截器、错误对象结构和下载接口。
- `codemirror`: 当前 5.x，升级到 6.x 是 API 重构，应单独处理代码编辑器组件。
- `intro.js`: 当前 4.x，可升级以消除旧 CSS hack 警告，但需要验证引导样式。
- `js-cookie`: 当前 2.x，升级到 3.x 需要检查封装层。

## 7. 结论

本轮已完成 `jshERP-web` 中可识别的 Vue2 生态依赖与旧写法清理，并完成三轮复扫、构建验证和 Vite preview 冒烟验证。当前项目依赖与源码入口已经进入 Vue3 + Vite + Ant Design Vue 4 生态。

需要注意：我已完成自动化构建和 HTTP 冒烟验证，但没有代替人工完成所有业务页面的点击级回归。单据、财务、报表、打印、可编辑表格等高风险页面仍建议在真实后端联调环境下逐项人工验证。
