# 25 - 移动端 H5 开发方案与实施步骤（Vue3 + Vant）

> 文档状态：已评审定稿（待执行）
> 编写日期：2026-09-13
> 适用项目：管伊佳 ERP（jshERP-vue3）
> 相关代码：`jshERP-boot/`（后端，零改动）、`jshERP-web/`（PC 前端，不改动）
> 待创建工程：`jshERP-h5/`（移动端，独立工程）

---

## 1. 背景与目标

### 1.1 背景

现有系统只有 PC Web 端（`jshERP-web`），门店、仓库、老板在外出场景下无法便捷使用。计划新增独立的移动端 H5 工程，优先覆盖高频场景：

- 门店/柜台：零售开单、收款、退货
- 业务员/仓库：销售出库、采购入库、查库存
- 管理者：单据审批、经营看板

### 1.2 目标

1. 新增独立前端工程 `jshERP-h5/`，**不修改 `jshERP-web`，后端零改动**
2. 运行形态：微信公众号自定义菜单入口的 H5（手机浏览器亦可直接访问）
3. 一期（MVP）范围：**零售 + 销售 + 采购** 三类单据 + 库存查询 + 单据中心 + 首页看板 + 登录/我的
4. 与 PC 端共用同一套后端接口、同一账号体系与权限体系，数据实时一致

### 1.3 关键决策（已确认）

| 决策项 | 结论 |
|---|---|
| 技术栈 | Vue 3 + Vite + Vant 4 |
| 状态管理 | Pinia |
| 工程目录名 | `jshERP-h5` |
| 一期范围 | 零售出库/退货 + 销售出库/退货 + 采购入库/退货 |
| 部署路径 | 同域子路径 `/m/`，API 反代 `/jshERP-boot/`（推荐，可改子域名） |
| 后端改动 | MVP 零改动；二期可选 BFF（微信网页授权/JS-SDK 扫码/模板消息） |

---

## 2. 技术选型

| 类别 | 选型 | 说明 |
|---|---|---|
| 框架 / 构建 | Vue 3.5.x + Vite 8.x | 与 `jshERP-web` 对齐，接口层与工具函数逻辑可直接迁移 |
| UI 组件库 | Vant 4 | Vue3 移动端生态最成熟（列表、表单、弹层、上传、日历等） |
| 路由 | Vue Router 4（history 模式） | base 使用 `/m/`；H5 为普通网页，可用静态路由 + 动态权限过滤 |
| 状态管理 | Pinia | 管理登录态、用户信息、按钮权限、系统配置缓存 |
| 请求 | axios 1.x | 独立封装，等价实现 PC 的 token 注入与错误处理 |
| 移动端适配 | postcss-px-to-viewport | 375 设计稿自动转 vw，另处理安全区 |
| 日期 | dayjs | 轻量，PC 端也在用 |
| 密码加密 | md5 | 后端登录要求密码为 MD5 后再提交 |
| 图表（看板） | ECharts 5（按需引入） | 替代 PC 的 G2Plot；一期首页卡片可先不画图 |
| 样式 | Less | 与 PC 端一致 |
| 金额计算 | 字符串 + 整数分或 decimal.js | 避免浮点误差，保留两位小数提交 |

### 2.1 可复用与需重写清单

**可直接迁移（改 import 路径即可）**

- `jshERP-web/src/api/api.js` 中的接口 URL 与参数结构（按模块拆分为 `auth/material/depot/supplier/dashboard`）
- `jshERP-web/src/utils/util.js`：`getMpListShort`、日期格式化（`getFormatDate`/`getNowFormatDateTime`）、报表参数白名单（如 `buildBuySaleReportQueryParams`）
- 业务常量：单据类型（`出库/入库/其它` + `零售/销售/采购/退货`）、按钮权限码（1 编辑、2 审核、7 反审核、3 导出、4 启用禁用、5 打印、6 作废）、收款类型（现付/预付款）
- 登录与权限交互逻辑：登录返回结构、`X-Access-Token` 约定、`getUserBtnByCurrentUser` 的数据结构

**需重新实现**

- 页面 UI 与交互（PC 的 Ant Design Vue 组件不可复用）
- 明细编辑：用购物车式卡片/行编辑替代 `JEditableTable`
- 请求封装中的重定向方式（H5 用 router 跳转，不用 `window.location.reload()`）
- 上传组件（Vant Uploader + 图片压缩）
- 图表（ECharts 替代 G2Plot）

### 2.2 明确不复用

- 动态路由生成 `generateIndexRouter/generateChildRouters`（移动端用静态路由 + 权限过滤）
- PC 的 Vuex store、Mixins、Antd 组件、vis 图表等

---

## 3. 与后端集成（零改动）

### 3.1 认证

- 验证码开关：`GET /platformConfig/getPlatform/checkcodeFlag`（免登录，返回 `'1'` 表示开启；当前环境为 `1`）
- 验证码图片：`GET /user/randomImage` → `{ code:200, data:{ uuid, base64 } }`
- 登录：`POST /user/login`，请求体：

```json
{
  "loginName": "admin",
  "password": "MD5(明文密码)",
  "code": "图形验证码（开启时必填）",
  "uuid": "验证码 uuid"
}
```

- 登录响应：`{ code:200, data:{ token, user, msgTip } }`
  - `token` 形如 `{uuid}_{tenantId}`（有租户时）
  - 存储：`token`、`user.id`、`user.loginName`、`user`（用户信息）
- 会话时效：Redis 会话 **3 天**（`MAX_SESSION_IN_SECONDS = 60*60*24*3`），过期需重新登录

### 3.2 请求约定

- 请求头：`X-Access-Token: {token}`
- 未登录：后端返回 **HTTP 500 + 响应体纯文本 `loginOut`**（不是 401），拦截器需清理登录态并跳转登录页
- 统一响应：
  - 普通接口：`{ code, data }`；业务异常：`{ code: 业务码, data:{ message } }`
  - 分页接口（TableDataInfo）：`{ code:200, data:{ rows, total } }`，分页参数 `currentPage/pageSize`
  - 列表查询条件：`search=JSON.stringify(queryParam)`（与 PC 一致，避免 HTTP 400）
- 需要单独处理的业务码：`200` 成功、`201` 警告提示、`500` 错误、`510` 报表特殊提示（PC 端各列表页单独提示）

### 3.3 接口清单（一期所需，均已在 PC 端验证可用）

| 模块 | 接口 | 方式 | 说明 |
|---|---|---|---|
| 登录 | `/user/login` | POST | 登录 |
| 登录 | `/user/randomImage` | GET | 图形验证码（uuid + base64） |
| 登录 | `/user/logout` | POST | 退出 |
| 权限 | `/function/findMenuByPNumber` | POST | 菜单树（`{pNumber:0,userId}`，返回 `text/url/component/children`） |
| 权限 | `/user/getUserBtnByCurrentUser` | GET | 按钮权限 `data.userBtn=[{url,btnStr}]`；admin 无该字段视为全权限 |
| 系统配置 | `/systemConfig/getCurrentSystemConfig` | GET | 含税/多账户等开关，控制表单显隐 |
| 平台配置 | `/platformConfig/getPlatform/appVersion` | GET | 移动端版本号（免登录） |
| 商品 | `/material/getMaterialByBarCode` | GET | 条码查商品；`prefixNo=LSCK/LSTH` 返回零售价 `commodityDecimal`，`XSDD/XSCK/XSTH` 返回销售价，`CGDD/CGRK/CGTH` 返回采购价 |
| 商品 | `/material/getMaterialByParam?q=` | GET | 关键词搜索（开单选择器） |
| 商品 | `/material/getListWithStock` | GET | 商品库存报表（库存页） |
| 库存 | `/depotItem/findStockByDepotAndBarCode` | GET | 指定仓库 + 条码的库存 |
| 仓库 | `/depot/findDepotByCurrentUser` | GET | 当前用户有权限的仓库 |
| 仓库 | `/depot/getAllList` | GET | 全部仓库 |
| 往来单位 | `/supplier/findBySelect_retail` | POST | 会员下拉（含预付款 `advanceIn`） |
| 往来单位 | `/supplier/findBySelect_cus` | POST | 客户下拉 |
| 往来单位 | `/supplier/findBySelect_sup` | POST | 供应商下拉 |
| 往来单位 | `/supplier/info?id=` | GET | 会员详情（预付款余额校验） |
| 单据保存 | `/depotHead/addDepotHeadAndDetail` | POST | 新增（`{info:JSON字符串, rows:JSON字符串}`，`status:'1'` 即保存并审核） |
| 单据保存 | `/depotHead/updateDepotHeadAndDetail` | PUT | 编辑（含 `id`） |
| 单据列表 | `/depotHead/list` | GET | 条件：`type/subType/status/number/organId/beginTime/endTime/depotId/accountId/materialParam...` |
| 单据详情 | `/depotHead/getDetailByNumber?number=` | GET | 主表 + 明细列表 |
| 单据明细 | `/depotItem/getDetailList?headerId=` | GET | 明细（编辑回显） |
| 审核 | `/depotHead/batchSetStatus` | POST | `{status:'1'/'0', ids:'1,2,'}` |
| 看板 | `/depotHead/getBuyAndSaleStatistics` | GET | `todayBuy/todaySale/todayRetailSale`、`month*`、`yesterday*`、`year*`；无价格权限时为 `'***'` |
| 上传 | `/systemConfig/upload` | POST | multipart，字段 `file` + `biz=bill`，返回 `data=路径` |

> 说明：零售/销售/采购的保存均走同一对接口，靠 `info.type`（出库/入库/其它）与 `info.subType`（零售/销售/采购/零售退货/销售退货/采购退货）区分。

### 3.4 开发与部署

- 开发环境：Vite dev server 端口 **3001**，代理：

```js
// vite.config.js 片段
server: {
  host: '0.0.0.0',
  port: 3001,
  proxy: {
    '/jshERP-boot': { target: 'http://localhost:9999', changeOrigin: true }
  }
}
```

- 环境变量：

```bash
# .env.development
VITE_APP_API_BASE=/jshERP-boot

# .env.production
VITE_APP_PUBLIC_PATH=/m/
VITE_APP_API_BASE=/jshERP-boot
```

- 生产 Nginx（同域，无跨域、无需后端 CORS 配置）：

```nginx
location /m/ {
  alias /www/jshERP-h5/dist/;
  try_files $uri $uri/ /m/index.html;
}
location /jshERP-boot/ {
  proxy_pass http://127.0.0.1:9999/jshERP-boot/;
  proxy_set_header Host $host;
  proxy_set_header X-Real-IP $remote_addr;
}
```

- 公众号接入：认证服务号 → 自定义菜单配置 H5 链接（可放在域名 `/m/` 下），无需后端改造

### 3.5 后端改动说明

- **MVP 零改动**：登录、验证码、上传、按钮权限、单据读写全部复用现有接口
- 可选后续（不影响一期）：
  - 新增 BFF（独立小服务）实现微信网页授权免密登录、JS-SDK 原生扫码、模板消息
  - 在平台配置 `app_version` 写入移动端版本号，用于版本提示

---

## 4. 工程结构（待创建）

```text
jshERP-h5/
├── .env.development                # API 前缀
├── .env.production                 # /m/ 部署前缀
├── index.html
├── package.json
├── vite.config.js                  # 端口 3001、代理、base
├── postcss.config.js               # px → vw
└── src/
    ├── main.js                     # 注册 Vant / Pinia / Router
    ├── App.vue                     # 全局容器 + keep-alive 策略
    ├── router/
    │   └── index.js                # 静态路由 + 登录守卫 + 权限过滤
    ├── store/
    │   ├── user.js                 # token/userId/userInfo/btnStr/permissions
    │   └── app.js                  # 系统配置缓存、网络状态
    ├── api/
    │   ├── request.js              # axios 封装：token、loginOut、错误码、Toast
    │   ├── auth.js                 # 登录/验证码/退出/改密
    │   ├── material.js             # 商品/条码/库存
    │   ├── supplier.js             # 会员/客户/供应商
    │   ├── depot.js                # 仓库/账户/经手人
    │   ├── bill.js                 # 单据增改查/审核/明细
    │   └── dashboard.js            # 看板统计
    ├── utils/
    │   ├── storage.js              # 本地存储（带前缀与过期）
    │   ├── format.js               # 金额/日期格式化
    │   ├── permission.js           # btnStr 解析与按钮显隐
    │   └── scan.js                 # 扫码（降级方案封装）
    ├── constants/
    │   └── business.js             # 单据类型、按钮码、收款类型、状态枚举
    ├── components/
    │   ├── GoodsPicker.vue         # 商品选择（搜索 + 扫码 + 价格分派）
    │   ├── MemberPicker.vue        # 会员/客户/供应商选择
    │   ├── DepotPicker.vue         # 仓库选择
    │   ├── AccountPicker.vue       # 结算账户选择
    │   ├── PersonPicker.vue        # 经手人/销售员选择
    │   ├── BillForm.vue            # 单据表单引擎（配置驱动三类单据）
    │   ├── BillItemList.vue        # 明细列表（数量/单价/金额编辑）
    │   ├── BillStatusTag.vue       # 单据状态标签
    │   └── ScanInput.vue           # 扫码输入框（含手动输入降级）
    ├── views/
    │   ├── login/index.vue
    │   ├── home/index.vue
    │   ├── sale/
    │   │   ├── BillList.vue        # 开单入口（按类型 Tab）
    │   │   ├── BillFormPage.vue    # 新增/编辑（零售/销售/采购共用）
    │   │   └── BillDetail.vue      # 单据详情
    │   ├── stock/index.vue         # 库存查询
    │   ├── bills/index.vue         # 单据中心（列表 + 审核）
    │   └── mine/index.vue          # 我的（改密/退出/版本）
    └── styles/
        ├── variables.less
        └── common.less
```

---

## 5. 页面与路由规划

### 5.1 路由表

| 路径 | 页面 | 说明 | Tab |
|---|---|---|---|
| `/login` | 登录 | 账号密码 + 验证码 | - |
| `/home` | 首页 | 经营看板 + 快捷入口 | 首页 |
| `/sale` | 开单 | 入口页：零售出库/退货、销售出库/退货、采购入库/退货 | 开单 |
| `/sale/new?bill=retail_out` | 新增单据 | 配置驱动：零售/销售/采购 | - |
| `/sale/edit/:id?bill=...` | 编辑单据 | 回显 + 修改 | - |
| `/sale/detail/:number` | 单据详情 | 明细、金额、附件 | - |
| `/stock` | 库存查询 | 搜索/扫码、仓库筛选 | 库存 |
| `/bills` | 单据中心 | 类型 Tab + 筛选 + 审核/反审核 | 单据 |
| `/mine` | 我的 | 用户、改密、退出、版本 | 我的 |

### 5.2 各页面功能要点

**登录页**

- 账号、密码（MD5）、图形验证码（读取开关，开启时显示，点击刷新）
- 记住登录名（可选）
- 登录成功缓存 token/userId/userInfo，跳首页

**首页**

- 卡片：今日销售、今日零售、昨日、本月、本年（`getBuyAndSaleStatistics`；`'***'` 时显示 `***`）
- 快捷入口：零售开单、销售出库、采购入库、库存查询、单据中心
- 按权限过滤入口（无对应菜单/按钮权限则隐藏）

**开单（核心）**

- 单据类型选择：零售出库 / 零售退货 / 销售出库 / 销售退货 / 采购入库 / 采购退货
- 表单字段（配置驱动，按类型显隐）：

| 字段 | 零售 | 销售 | 采购 |
|---|---|---|---|
| 会员卡号 | ✅（可无） | - | - |
| 客户 | - | ✅ 必填 | - |
| 供应商 | - | - | ✅ 必填 |
| 关联原单 | 退货时可选 | 出库可关联订单/退货关联出库 | 入库可关联订单/退货关联入库 |
| 收款类型 | ✅ 现付/预付款 | - | - |
| 优惠率/优惠后金额 | - | ✅ | ✅ |
| 本次收款/付款 | 收款金额 + 找零 | ✅（本次收款/欠款） | ✅（本次付款） |
| 扣除订金 | - | ✅ | ✅ |
| 其它费用 | - | ✅ | ✅ |
| 销售人员 | - | ✅ | - |
| 结算账户 | ✅ | ✅ | ✅（多账户按钮二期） |
| 经手人/备注/附件 | ✅ | ✅ | ✅ |
| 税率/税额列 | - | 按系统配置"商品价格含税"显隐 | 同左 |

- 明细行：扫码/搜索选商品 → 仓库、数量、单价（自动取对应价格）、金额自动计算
  - 商品开启序列号/批号时：弹层录入基础版（snList / batchNumber）
  - 后端会校验：库存不足、低于最低售价；前端提前给出友好提示
- 金额区：合计、优惠、收/付款、找零（零售）、欠款（销售）
- 操作按钮：保存、保存并审核（`status:'1'`）、返回
- 编辑：`getDetailByNumber` + `getDetailList` 回显；仅未审核单据可编辑

**库存查询**

- 关键词搜索（名称/条码/规格/型号）
- 扫码：输入框聚焦扫码枪回车 + 按钮唤起摄像头（降级）
- 仓库筛选（`findDepotByCurrentUser`）
- 列表：商品、规格、单位、库存数量；点击查看各仓库分布
- 零库存显隐开关

**单据中心**

- Tab：零售出库、零售退货、销售出库、销售退货、采购入库、采购退货
- 筛选：时间范围、状态（未审核/已审核）、单号、往来单位
- 列表：下拉刷新 + 上拉加载（`van-pull-refresh` + `van-list`）
- 详情：主表 + 明细 + 附件（可预览图片）
- 审核/反审核：按 `btnStr` 显隐（2 审核 / 7 反审核），二次确认

**我的**

- 当前用户信息（姓名/角色/部门）
- 修改密码（`/user/updatePwd` 类接口，与 PC 一致）
- 退出登录（清缓存 + 调 `/user/logout`）
- 版本号（`app_version`）+ 关于

---

## 6. 关键设计

### 6.1 请求封装（`api/request.js`）

等价实现 PC 端 `jshERP-web/src/utils/request.js` 的行为：

1. `baseURL = import.meta.env.VITE_APP_API_BASE || '/jshERP-boot'`
2. 请求拦截：存在 token 时加 `X-Access-Token`
3. 响应拦截：
   - HTTP 200：`return response.data`（由业务层判断 `code`）
   - HTTP 500 且响应体为 `loginOut`：清登录态 → 跳转 `/login?redirect=...`
   - 其他网络错误：Vant Toast 提示，超时可重试
4. 统一业务错误处理：`code!==200` 时 `Toast(data.message || '操作失败')`；`201` 用警告样式；`510` 特殊提示
5. 全局 loading 计数器（可选），支持按请求关闭

### 6.2 登录态与权限

- 存储键（建议与 PC 语义一致即可，无需相同前缀）：`token`、`userId`、`userInfo`、`btnStrList`、`menuList`
- 路由守卫：无 token → `/login`；已登录访问 `/login` → `/home`
- 按钮权限：登录后调用 `/user/getUserBtnByCurrentUser`，缓存 `{url: btnStr}` 映射；`admin`（无 `userBtn` 字段）视为全权限
- 页面/入口权限：可用 `/function/findMenuByPNumber` 返回的 `url` 集合过滤快捷入口与 Tab（可选，一期可先只做按钮级）

### 6.3 单据表单引擎（三类共用）

- `BillForm.vue` 接收 `billType`（如 `retail_out`），从 `constants/business.js` 读取配置：

```js
{
  retail_out: {
    title: '零售出库', type: '出库', subType: '零售', prefixNo: 'LSCK',
    partner: 'member', payType: true, changeAmount: true, debt: false, salesMan: false, discount: false
  },
  sale_out:   { title: '销售出库', type: '出库', subType: '销售', prefixNo: 'XSCK', partner: 'customer', payType: false, changeAmount: true, debt: true, salesMan: true, discount: true },
  purchase_in:{ title: '采购入库', type: '入库', subType: '采购', prefixNo: 'CGRK', partner: 'supplier', changeAmount: true, debt: true, discount: true }
  // ...退货类型同理
}
```

- 保存时组装与 PC 完全一致的 `info/rows` JSON 字符串，保证后端零改动
- 明细字段以 `DepotItemService` 实际读取为准：`barCode/depotId/operNumber/unitPrice/allPrice/sku/batchNumber/expirationDate/snList/remark`

### 6.4 扫码方案（一期降级）

- 一期不依赖微信 JS-SDK（需要后端签名）：
  - 扫码枪/蓝牙扫码器：输入框自动聚焦，回车即查
  - 摄像头扫码：`getUserMedia + BarcodeDetector`（Android Chrome/微信可用），iOS 不支持时引导手动输入
- 二期接 BFF 后切换为 `wx.scanQRCode`，体验最佳

### 6.5 移动端适配

- 375 设计稿 + `postcss-px-to-viewport`（或 Vant 官方 rem 方案）
- 安全区：tabbar / 底部操作栏使用 `env(safe-area-inset-bottom)`
- iOS：输入框聚焦页面顶起、`100vh` 问题；表单页避免固定高度
- 长列表：虚拟滚动或分页加载；图片懒加载
- 弱网：请求超时缩短、失败可重试；关键字搜索防抖

### 6.6 上传与附件

- Vant Uploader：`after-read` 中压缩（canvas，长边 ≤1280，质量 0.8）后上传
- 上传接口：`POST /systemConfig/upload`（字段 `file` + `biz=bill`），响应 `data` 为路径
- 回显：`/systemConfig/static/**` 前缀拼接完整 URL

---

## 7. 实施步骤（P0 ~ P8，约 11~13 人日）

| 阶段 | 任务 | 交付物 | 验收标准 | 工期 |
|---|---|---|---|---|
| **P0 脚手架** | 创建 `jshERP-h5`；Vite+Vue3+Vant+Pinia+Router+axios+viewport；env/proxy；目录骨架；ESLint | 可运行的空框架 | `npm run dev` 端口 3001 打开正常；`curl /jshERP-boot/...` 代理通 | 0.5 天 |
| **P1 认证与框架** | request 封装（token/loginOut/错误码）；登录页（MD5+验证码）；路由守卫；5 个 Tab 页；我的（退出/改密/版本） | 登录闭环 + 框架 | token 刷新保持；失效自动跳登录；改密成功 | 1~1.5 天 |
| **P2 公共组件** | GoodsPicker（搜索+扫码+价格分派）、MemberPicker（会员/客户/供应商）、DepotPicker、AccountPicker、PersonPicker、ScanInput、BillStatusTag | 可复用组件 | 组件在演示页可用；扫码/搜索结果正确 | 1.5 天 |
| **P3 零售开单** | 零售出库/退货：购物车明细、会员选择、预付款校验、收款/找零、保存与保存并审核、编辑回显 | 零售全流程 | 手机开的单在 PC 可见；库存、会员预付款正确；编辑回显一致 | 2~2.5 天 |
| **P4 销售出库/退货** | 客户、关联原单（可选）、优惠、订金、本次收款/欠款、销售人员 | 销售全流程 | 与 PC 口径一致；欠款金额正确；PC 可审核 | 1.5~2 天 |
| **P5 采购入库/退货** | 供应商、关联原单（可选）、优惠、本次付款、订金、其它费用 | 采购全流程 | 与 PC 口径一致；库存、应付正确 | 1.5~2 天 |
| **P6 单据中心** | 三类单据列表（Tab/筛选/刷新加载）、详情、审核/反审核（btnStr 控制） | 审批闭环 | 手机完成审核后 PC 同步显示；权限不同账号按钮显隐正确 | 1.5 天 |
| **P7 库存 + 看板** | 库存查询（搜索/扫码/仓库筛选）；首页统计卡片 + 快捷入口 | 查询与看板 | 数据与 PC 商品库存/首页一致 | 1 天 |
| **P8 部署联调** | `npm run build`；Nginx `/m/` + API 反代 + gzip；公众号菜单挂链；iOS/安卓微信真机回归 | 可上线版本 | 公众号菜单打开 → 登录 → 开单 → 审核 全流程通过 | 1 天 |

### 7.1 里程碑

- M1（P0~P1 完成）：登录进入首页框架
- M2（P3 完成）：零售开单可用（最先产生业务价值）
- M3（P5 完成）：三类单据全部可用
- M4（P6~P7 完成）：审批与查询闭环
- M5（P8 完成）：公众号可访问，灰度上线

### 7.2 一期暂不做（二期候选）

- 多账户拆分收款/付款（`accountIdList/accountMoneyList`）
- 微信网页授权免密登录、JS-SDK 原生扫码、模板消息
- 销售/采购订单单据（仅做直接出入库；"关联订单"能力保留可选）
- 报表导出/打印、财务模块、系统管理功能
- 盘点、调拨、组装拆卸等仓库单据

---

## 8. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 验证码开启（`checkcode_flag=1`） | 登录多一步，体验下降 | 一期正常实现图形验证码；如移动端不便，可由管理员评估关闭（配置项，非代码改动） |
| token 3 天过期 | 使用中断 | 统一拦截 `loginOut`，保留登录名，静默跳登录页 |
| 无微信 JS-SDK 原生扫码 | 扫码体验一般 | 一期扫码枪/摄像头降级；二期 BFF 接 `wx.scanQRCode` |
| iOS 微信兼容 | 键盘顶起、安全区、扫码 | 表单页避免固定布局；tabbar 安全区适配；真机回归纳入 P8 |
| 金额精度 | 与 PC 对账不一致 | 前端整数分/字符串计算，提交保留两位小数；与后端 BigDecimal 对齐 |
| 并发编辑 | 同一单据 PC/手机同时改 | 保存前校验状态；已审核单据禁止编辑 |
| 微信内 localStorage 被清 | 登录态丢失 | 启动时校验 token；接口 500 `loginOut` 兜底 |
| 权限差异 | 看到不该看的入口 | 移动端单独建角色；按钮/入口严格按 `btnStr`/菜单过滤 |
| 明细字段遗漏 | 保存报错或数据缺失 | 按 `DepotItemService` 实际读取字段组装；保存前自测 PC 回显 |

---

## 9. 测试与验收清单

**功能**

- [ ] 登录：验证码刷新、错误提示、记住登录名、退出
- [ ] 零售出库：散客/会员、现付/预付款（余额不足提示）、收款/找零、保存并审核、编辑
- [ ] 零售退货：关联原单、库存回增
- [ ] 销售出库：客户必填、优惠、订金、本次收款/欠款、销售人员
- [ ] 销售退货 / 采购入库 / 采购退货：金额与库存方向正确
- [ ] 单据中心：各类型列表筛选、分页加载、详情、审核/反审核
- [ ] 库存：搜索、扫码、仓库筛选、零库存显隐
- [ ] 看板：今日/本月/零售口径与 PC 一致；无价格权限显示 `***`
- [ ] 权限：不同角色按钮显隐、无权限入口隐藏

**兼容**

- [ ] iOS 微信、Android 微信、Safari/Chrome 手机浏览器
- [ ] 小屏（iPhone SE 375）、大屏（Pro Max 430）
- [ ] 弱网/断网提示

**数据一致性**

- [ ] 手机开单 → PC 端查看/审核 → 库存与报表一致
- [ ] PC 开单 → 手机端查看/审核
- [ ] 会员预付款充值/消费余额一致

---

## 10. 变更记录

| 日期 | 版本 | 内容 |
|---|---|---|
| 2026-09-13 | v1.0 | 初稿：技术选型、接口清单、页面规划、P0~P8 实施步骤（范围含零售+销售+采购） |
