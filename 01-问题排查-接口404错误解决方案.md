# 01-问题排查：接口404错误解决方案

## 问题描述

### 现象
访问接口 `http://localhost:3000/erp/jshERP-boot/platformConfig/getPlatform/url` 时，后台返回 **404 Not Found** 错误，提示资源未找到。

### 报错信息
```
HTTP/1.1 404 Not Found
X-Powered-By: Express
content-length: 0
```

### 环境信息
- **前端开发服务器**：`http://localhost:3000`
- **后端服务器**：`http://localhost:9999`
- **后端上下文路径**：`/jshERP-boot`
- **问题URL**：`http://localhost:3000/erp/jshERP-boot/platformConfig/getPlatform/url`

---

## 问题分析

### 1. 接口验证

#### 1.1 后端接口测试
直接访问后端接口，验证后端服务是否正常：

```bash
curl http://localhost:9999/jshERP-boot/platformConfig/getPlatform/url
```

**结果**：✅ 返回正常
```
HTTP/1.1 200 OK
Content-Type: text/plain;charset=UTF-8
Content-Length: 21

http://www.baidu.com/
```

**结论**：后端接口正常，Controller 映射正确。

#### 1.2 前端代理测试（正确路径）
测试前端代理是否正常工作：

```bash
curl http://localhost:3000/jshERP-boot/platformConfig/getPlatform/url
```

**结果**：✅ 返回正常
```
HTTP/1.1 200 OK
content-type: text/plain;charset=UTF-8
content-length: 21

http://www.baidu.com/
```

**结论**：前端代理配置正常，`/jshERP-boot` 路径可以正确转发到后端。

#### 1.3 前端代理测试（问题路径）
测试带 `/erp` 前缀的路径：

```bash
curl http://localhost:3000/erp/jshERP-boot/platformConfig/getPlatform/url
```

**结果**：❌ 返回 404
```
HTTP/1.1 404 Not Found
X-Powered-By: Express
content-length: 0
```

**结论**：前端开发服务器没有配置 `/erp/jshERP-boot` 的代理规则。

---

### 2. 根本原因分析

#### 2.1 配置文件对比

**生产环境配置（.env.production）**：
```bash
VUE_APP_PUBLIC_PATH=/erp/
VUE_APP_API_BASE=/erp/jshERP-boot
```

**开发环境配置**：
- ❌ **缺少 `.env.development` 文件**
- 默认使用根路径 `/`

**vue.config.js 代理配置**：
```javascript
devServer: {
    port: 3000,
    proxy: {
        '/jshERP-boot': {
            target: 'http://localhost:9999',
            ws: false,
            changeOrigin: true
        }
        // ❌ 缺少 /erp/jshERP-boot 的代理规则
    }
}
```

#### 2.2 问题根源

1. **开发环境和生产环境路径不一致**
   - 开发环境：`http://localhost:3000/jshERP-boot/...`
   - 生产环境：`http://域名/erp/jshERP-boot/...`

2. **前端代理规则缺失**
   - 只配置了 `/jshERP-boot` 的代理
   - 没有配置 `/erp/jshERP-boot` 的代理

3. **环境变量未配置**
   - 缺少 `.env.development` 文件
   - 开发环境无法使用 `/erp` 子路径

---

## 解决方案

### 方案选择：创建开发环境配置文件

**目标**：让开发环境和生产环境保持一致，都使用 `/erp` 子路径。

**优点**：
- ✅ 开发环境和生产环境路径完全一致
- ✅ 避免环境差异导致的问题
- ✅ 便于调试和测试
- ✅ 生产环境配置保持不变

---

### 实施步骤

#### 步骤1：创建开发环境配置文件

**文件路径**：`jshERP-web/.env.development`

**文件内容**：
```bash
# 开发环境配置（yarn serve / npm run serve）
# 与生产环境保持一致，使用 /erp/ 子路径
VUE_APP_PUBLIC_PATH=/erp/
VUE_APP_API_BASE=/erp/jshERP-boot
```

**说明**：
- `VUE_APP_PUBLIC_PATH`：前端应用的基础路径，影响路由和静态资源
- `VUE_APP_API_BASE`：API 请求的基础路径，影响所有后端接口调用

---

#### 步骤2：修改 vue.config.js

**文件路径**：`jshERP-web/vue.config.js`

**修改内容**：在 `devServer.proxy` 中添加 `/erp/jshERP-boot` 的代理规则

**修改前**：
```javascript
devServer: {
    port: 3000,
    proxy: {
        '/jshERP-boot': {
            target: 'http://localhost:9999',
            ws: false,
            changeOrigin: true
        }
    }
}
```

**修改后**：
```javascript
devServer: {
    port: 3000,
    proxy: {
        '/jshERP-boot': {
            target: 'http://localhost:9999', // 请求本地 需要jshERP-boot后台项目
            ws: false,
            changeOrigin: true
        },
        '/erp/jshERP-boot': {
            target: 'http://localhost:9999', // 支持 /erp 子路径访问
            ws: false,
            changeOrigin: true,
            pathRewrite: {
                '^/erp': '' // 去掉 /erp 前缀，转发到后端
            }
        }
    }
}
```

**说明**：
- 保留原有的 `/jshERP-boot` 代理规则，兼容旧的访问方式
- 新增 `/erp/jshERP-boot` 代理规则，支持子路径访问
- `pathRewrite` 将 `/erp/jshERP-boot` 重写为 `/jshERP-boot`，匹配后端路径

---

#### 步骤3：重启前端开发服务器

**操作步骤**：
```bash
# 1. 进入前端项目目录
cd jshERP-web

# 2. 停止当前运行的服务（按 Ctrl+C）

# 3. 重新启动开发服务器
yarn serve
# 或
npm run serve
```

**重要提示**：
- ⚠️ **必须重启服务器**，配置文件的修改才会生效
- ⚠️ 热重载（Hot Reload）不会应用 `.env` 和 `vue.config.js` 的修改

---

## 验证测试

### 1. 测试接口访问

#### 测试1：新路径（/erp 子路径）
```bash
curl http://localhost:3000/erp/jshERP-boot/platformConfig/getPlatform/url
```

**预期结果**：✅ 返回 200
```
http://www.baidu.com/
```

#### 测试2：旧路径（兼容性）
```bash
curl http://localhost:3000/jshERP-boot/platformConfig/getPlatform/url
```

**预期结果**：✅ 返回 200
```
http://www.baidu.com/
```

---

### 2. 测试前端页面访问

#### 访问首页
```
http://localhost:3000/erp/
```

**预期结果**：✅ 正常显示登录页面

#### 访问其他页面
```
http://localhost:3000/erp/user/login
```

**预期结果**：✅ 路由正常跳转

---

### 3. 测试 API 调用

打开浏览器开发者工具（F12），查看 Network 面板：

**预期结果**：
- ✅ 所有 API 请求路径为 `/erp/jshERP-boot/...`
- ✅ 请求状态码为 200
- ✅ 响应数据正常

---

## 配置说明

### 环境配置对比

| 配置项 | 开发环境 | 生产环境 |
|--------|---------|---------|
| 配置文件 | `.env.development` | `.env.production` |
| 公共路径 | `/erp/` | `/erp/` |
| API 基础路径 | `/erp/jshERP-boot` | `/erp/jshERP-boot` |
| 访问地址 | `http://localhost:3000/erp/` | `http://域名/erp/` |
| API 地址 | `http://localhost:3000/erp/jshERP-boot/...` | `http://域名/erp/jshERP-boot/...` |

---

### 代理工作原理

#### 开发环境代理流程

```
浏览器请求
    ↓
http://localhost:3000/erp/jshERP-boot/platformConfig/getPlatform/url
    ↓
Vue DevServer 代理匹配 /erp/jshERP-boot
    ↓
pathRewrite: 去掉 /erp 前缀
    ↓
转发到 http://localhost:9999/jshERP-boot/platformConfig/getPlatform/url
    ↓
后端处理并返回
    ↓
代理返回给浏览器
```

#### 生产环境访问流程

```
浏览器请求
    ↓
http://域名/erp/jshERP-boot/platformConfig/getPlatform/url
    ↓
Nginx 代理转发
    ↓
http://后端服务器:9999/jshERP-boot/platformConfig/getPlatform/url
    ↓
后端处理并返回
```

---

## 相关文件清单

### 修改的文件

1. **jshERP-web/.env.development**（新建）
   - 开发环境配置文件
   - 定义公共路径和 API 基础路径

2. **jshERP-web/vue.config.js**（修改）
   - 添加 `/erp/jshERP-boot` 代理规则
   - 配置路径重写规则

### 保持不变的文件

1. **jshERP-web/.env.production**
   - 生产环境配置保持不变
   - 继续使用 `/erp/` 子路径

2. **jshERP-boot/src/main/resources/application.properties**
   - 后端配置保持不变
   - 上下文路径仍为 `/jshERP-boot`

---

## 常见问题

### Q1：修改配置后仍然 404？
**A**：请确保已经重启前端开发服务器。`.env` 文件和 `vue.config.js` 的修改需要重启才能生效。

### Q2：为什么要保留 `/jshERP-boot` 的代理？
**A**：为了兼容性。如果有其他地方直接使用了 `/jshERP-boot` 路径，仍然可以正常工作。

### Q3：生产环境需要修改吗？
**A**：不需要。生产环境已经配置了 `/erp/` 子路径，保持不变即可。

### Q4：如何切换回根路径部署？
**A**：修改 `.env.development` 和 `.env.production`：
```bash
VUE_APP_PUBLIC_PATH=/
VUE_APP_API_BASE=/jshERP-boot
```

### Q5：为什么开发环境要和生产环境保持一致？
**A**：
- 避免环境差异导致的 Bug
- 便于测试生产环境的实际表现
- 减少部署时的配置问题

---

## 技术要点

### 1. Vue CLI 环境变量
- `.env.development`：开发环境（`npm run serve`）
- `.env.production`：生产环境（`npm run build`）
- 变量必须以 `VUE_APP_` 开头才能在代码中访问

### 2. Vue DevServer 代理
- 只在开发环境生效
- 用于解决跨域问题
- 支持路径重写（pathRewrite）

### 3. publicPath 配置
- 影响所有静态资源的加载路径
- 影响 Vue Router 的 base 路径
- 必须以 `/` 开头和结尾

### 4. 代理匹配优先级
- 更具体的路径优先匹配
- `/erp/jshERP-boot` 优先于 `/jshERP-boot`

---

## 总结

### 问题根源
开发环境缺少 `/erp` 子路径的配置，导致前端代理无法正确转发请求。

### 解决方法
1. 创建 `.env.development` 文件，配置子路径
2. 修改 `vue.config.js`，添加代理规则
3. 重启开发服务器

### 最终效果
- ✅ 开发环境和生产环境路径完全一致
- ✅ 接口访问正常，返回 200
- ✅ 兼容旧的访问方式
- ✅ 生产环境配置保持不变

---

## 附录

### 完整配置文件

#### .env.development
```bash
# 开发环境配置（yarn serve / npm run serve）
# 与生产环境保持一致，使用 /erp/ 子路径
VUE_APP_PUBLIC_PATH=/erp/
VUE_APP_API_BASE=/erp/jshERP-boot
```

#### .env.production
```bash
# 生产构建（yarn build / npm run build）：与线上宿主机 /erp/、容器 nginx default.conf 一致
# 若改为根路径部署，请改为 VUE_APP_PUBLIC_PATH=/ 并同步 VUE_APP_API_BASE 与 deploy/docker/nginx/default-root.conf
VUE_APP_PUBLIC_PATH=/erp/
VUE_APP_API_BASE=/erp/jshERP-boot
```

#### vue.config.js（devServer 部分）
```javascript
devServer: {
    port: 3000,
    proxy: {
        '/jshERP-boot': {
            target: 'http://localhost:9999', // 请求本地 需要jshERP-boot后台项目
            ws: false,
            changeOrigin: true
        },
        '/erp/jshERP-boot': {
            target: 'http://localhost:9999', // 支持 /erp 子路径访问
            ws: false,
            changeOrigin: true,
            pathRewrite: {
                '^/erp': '' // 去掉 /erp 前缀，转发到后端
            }
        }
    }
}
```

---

**文档版本**：v1.0  
**创建日期**：2026-04-18  
**适用项目**：管伊佳ERP（jshERP）  
**作者**：技术团队
