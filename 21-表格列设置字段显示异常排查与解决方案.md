# 表格列设置字段显示异常排查与解决方案

## 1. 结论

当前“列设置”弹窗里字段名称显示不正常，主要不是后端接口问题，也不是 Ant Design Vue 4 表格组件本身直接导致。

核心原因是：项目的列设置弹窗直接读取前端列配置 `defColumns.title` 显示字段名，而当前部分页面的 `defColumns` 已经存在标题缺失、标题错位、标题被校验文案污染等问题。只要 `title` 不正确，列设置里就会同步显示不正确。

同时，列设置的勾选状态和实际展示列还会受浏览器本地缓存影响。缓存只保存 `dataIndex`，不保存 `title`，所以它不是字段名乱码或错名的直接原因，但会导致“修复列配置后，用户侧仍保留旧列选择结果”的问题。

## 2. 当前实现链路

### 2.1 通用列表

通用列表列设置入口在：

- `jshERP-web/src/mixins/JeecgListMixin.js`

关键逻辑：

```js
let columnsStr = storage.get(this.pageName)
this.settingDataIndex = columnsStr ? columnsStr.split(',') : this.defDataIndex
this.columns = this.defColumns.filter(item => {
  return this.settingDataIndex.includes(item.dataIndex)
})
```

列设置弹窗中展示字段名时，大量页面使用：

```vue
<j-ellipsis :value="item.title" :length="10"></j-ellipsis>
```

因此 `item.title` 缺失或错误时，列设置显示必然异常。

### 2.2 单据列表

单据列表列设置入口在：

- `jshERP-web/src/views/bill/mixins/BillListMixin.js`

关键逻辑与通用列表一致，只是缓存 key 使用 `prefixNo`：

```js
let columnsStr = storage.get(this.prefixNo)
this.settingDataIndex = columnsStr ? columnsStr.split(',') : this.defDataIndex
this.columns = this.defColumns.filter(item => {
  return this.settingDataIndex.includes(item.dataIndex)
})
```

### 2.3 商品列表

商品列表没有完全走通用 mixin，而是在：

- `jshERP-web/src/views/material/MaterialList.vue`

使用独立缓存 key：

```js
storage.get('materialColumns')
storage.set('materialColumns', columnsStr)
```

实现方式仍然依赖 `defColumns.title`。

### 2.4 报表列表

多个报表页面直接在页面内维护 `defColumns`、`defDataIndex`、`settingDataIndex`，字段名同样来自 `defColumns.title`。

## 3. 已确认的问题

本次静态扫描范围：

- `jshERP-web/src/views`
- `jshERP-web/src/components`
- `jshERP-web/src/mixins`

扫描结论：

- `defColumns` 相关文件共 32 个。
- 已确认高风险列配置问题 31 处。
- 其中 `action` 缺少 `title` 3 处。
- `rowIndex` 缺少 `title` 15 处。
- `title` 疑似被校验/占位文案污染 5 处。
- `action` 列标题需人工确认 8 处。

## 4. 明确需要修复的列配置

### 4.1 系统选项管理标题污染

文件：

- `jshERP-web/src/views/system/OptionList.vue`

已确认问题：

| 行号 | dataIndex | 当前标题问题 | 建议标题 |
| --- | --- | --- | --- |
| 187 | `enabled` | `请输入组编码!` | `是否启用` |
| 188 | `action` | `请输入显示选项值!` | `操作` |
| 213 | `isDefault` | `请输入组编码!` | `是否默认` |
| 214 | `enabled` | `请输入组编码!` | `是否启用` |
| 215 | `action` | `请输入显示选项值!` | `操作` |

这是最明确的问题。列标题被表单校验文案污染，会直接导致列设置中显示“请输入组编码!”、“请输入显示选项值!”。

### 4.2 单据列表列标题缺失和错位

文件：

- `jshERP-web/src/views/bill/AllocationOutList.vue`
- `jshERP-web/src/views/bill/AssembleList.vue`
- `jshERP-web/src/views/bill/DisassembleList.vue`

已确认问题：

| 文件 | dataIndex | 当前问题 | 建议 |
| --- | --- | --- | --- |
| `AllocationOutList.vue` | `action` | 缺少 `title` | 增加 `title: '操作'` |
| `AllocationOutList.vue` | `userName` | 标题显示为“单据日期” | 改为“操作员” |
| `AllocationOutList.vue` | `remark` | 标题显示为“金额合计” | 改为“备注” |
| `AllocationOutList.vue` | `status` | 标题显示为“备注” | 改为“状态” |
| `AssembleList.vue` | `action` | 缺少 `title` | 增加 `title: '操作'` |
| `AssembleList.vue` | `userName` | 标题显示为“单据日期” | 改为“操作员” |
| `AssembleList.vue` | `remark` | 标题显示为“金额合计” | 改为“备注” |
| `AssembleList.vue` | `status` | 标题显示为“备注” | 改为“状态” |
| `DisassembleList.vue` | `action` | 缺少 `title` | 增加 `title: '操作'` |
| `DisassembleList.vue` | `userName` | 标题显示为“单据日期” | 改为“操作员” |
| `DisassembleList.vue` | `remark` | 标题显示为“金额合计” | 改为“备注” |
| `DisassembleList.vue` | `status` | 标题显示为“备注” | 改为“状态” |

这些问题会导致列设置中操作列空白、操作员显示成单据日期、备注和状态错位。

### 4.3 报表序号列缺少标题

以下报表页面中 `rowIndex` 列没有 `title`：

- `jshERP-web/src/views/report/AccountReport.vue`
- `jshERP-web/src/views/report/AllocationDetail.vue`
- `jshERP-web/src/views/report/BuyInReport.vue`
- `jshERP-web/src/views/report/CustomerAccount.vue`
- `jshERP-web/src/views/report/InDetail.vue`
- `jshERP-web/src/views/report/InMaterialCount.vue`
- `jshERP-web/src/views/report/InOutStockReport.vue`
- `jshERP-web/src/views/report/MaterialStock.vue`
- `jshERP-web/src/views/report/modules/AccountInOutList.vue`
- `jshERP-web/src/views/report/OutDetail.vue`
- `jshERP-web/src/views/report/OutMaterialCount.vue`
- `jshERP-web/src/views/report/RetailOutReport.vue`
- `jshERP-web/src/views/report/SaleOutReport.vue`
- `jshERP-web/src/views/report/StockWarningReport.vue`
- `jshERP-web/src/views/report/VendorAccount.vue`

建议统一增加：

```js
title: '#'
```

或使用：

```js
title: '序号'
```

需要保持页面当前视觉时，优先使用 `#`。

### 4.4 action 列标题需人工确认

以下页面的 `action` 列有业务化标题，不一定错误，但需要确认是否应在列设置里显示这些名称：

| 文件 | 当前标题 |
| --- | --- |
| `jshERP-web/src/views/report/AccountReport.vue` | `账户流水` |
| `jshERP-web/src/views/report/CustomerAccount.vue` | `欠款详情` |
| `jshERP-web/src/views/report/VendorAccount.vue` | `欠款详情` |
| `jshERP-web/src/views/report/InOutStockReport.vue` | `库存详情` |
| `jshERP-web/src/views/report/MaterialStock.vue` | `库存详情` |
| `jshERP-web/src/views/system/modules/RolePushBtnModal.vue` | `按钮列表` |

如果这些列确实是“详情入口”而非普通操作按钮，可以保留当前标题；如果列设置里需要统一显示“操作”，则改为 `title: '操作'`，并把按钮文本保留在单元格内容里。

## 5. 影响范围判断

### 5.1 单据模块

单据模块受影响较明显。当前明确异常集中在调拨出库、组装单、拆卸单 3 个页面，但同类页面都使用 `BillListMixin`，建议统一复查所有单据列表的 `defColumns`。

重点页面：

- 零售出库、零售退货
- 采购订单、采购入库、采购退货、采购申请
- 销售订单、销售出库、销售退货
- 调拨出库
- 组装单、拆卸单
- 其他入库、其他出库

### 5.2 报表模块

报表模块大量使用禁用的 `rowIndex` 复选框，但 `rowIndex` 本身缺少标题。虽然禁用列不一定显示字段名，但仍建议补齐 `title`，避免后续统一组件或导出逻辑读取标题时出现空值。

### 5.3 系统模块

`OptionList.vue` 是当前系统模块中最明确的列标题污染问题。这个页面需要优先修。

### 5.4 商品模块

`MaterialList.vue` 使用独立列设置逻辑，当前未扫描出明确标题污染，但它与通用列表一样依赖 `defColumns.title`，后续应纳入统一列设置规范。

### 5.5 财务模块

财务列表也存在大量 `action` 列，但当前静态扫描没有发现高置信标题污染。建议后续按同一规则做人工复核，重点确认列设置弹窗里是否显示正确中文字段名。

## 6. 根因分析

### 6.1 列标题没有统一校验

当前每个页面手写 `defColumns`。缺少编译期或启动期校验，导致以下问题不会被 `npm run build` 发现：

- `dataIndex` 有值但 `title` 缺失。
- `title` 与 `dataIndex` 语义不匹配。
- `title` 被误写成表单校验文案。
- 操作列、序号列是否进入列设置没有统一规则。

### 6.2 列设置弹窗没有标题兜底

当前弹窗直接显示：

```vue
item.title
```

没有兜底逻辑。如果 `title` 为空，列设置字段名就会空白；如果 `title` 错误，列设置会原样显示错误值。

### 6.3 本地缓存只保存 dataIndex

列设置缓存保存的是逗号分隔的 `dataIndex`：

```js
storage.set(this.pageName, columnsStr)
storage.set(this.prefixNo, columnsStr)
storage.set('materialColumns', columnsStr)
```

这不会直接导致字段名错误，但会造成两个副作用：

1. 旧缓存中保存了已经删除或改名的 `dataIndex` 时，页面显示列集合可能异常。
2. 修复默认列配置后，用户仍可能沿用旧缓存的列选择结果，导致“修了代码但页面显示仍不符合默认列”的误判。

## 7. 解决方案

### 7.1 第一阶段：修复明确错误的列标题

优先修复不需要业务判断的问题：

1. `OptionList.vue` 中 5 个校验文案列标题。
2. `AllocationOutList.vue`、`AssembleList.vue`、`DisassembleList.vue` 中 `action/userName/remark/status` 的标题缺失或错位。
3. 报表页所有 `rowIndex` 增加 `title: '#'` 或 `title: '序号'`。

这一阶段不改列设置机制，只修明显错误的 `defColumns`。

### 7.2 第二阶段：统一列设置标题兜底

建议新增公共方法，例如：

```js
getColumnSettingTitle(column) {
  if (column.title && typeof column.title === 'string') {
    return column.title
  }
  return this.columnTitleMap?.[column.dataIndex] || column.dataIndex || ''
}
```

列设置弹窗改为显示兜底后的标题，避免个别页面遗漏 `title` 时直接空白。

注意：兜底只能防止空白，不应该替代修复 `defColumns.title`。业务字段仍应在列定义中明确配置中文标题。

### 7.3 第三阶段：过滤非法缓存和废弃列

在 `initColumnsSetting()` 中增加合法列过滤：

```js
const validDataIndexSet = new Set(this.defColumns.map(item => item.dataIndex))
this.settingDataIndex = this.settingDataIndex.filter(item => validDataIndexSet.has(item))
```

如果过滤后为空，回退到 `defDataIndex`。

建议对以下缓存 key 做兼容：

- `pageName`
- `prefixNo`
- `materialColumns`

### 7.4 第四阶段：明确哪些列允许进入列设置

建议引入统一字段：

```js
hideInColumnSetting: true
```

推荐规则：

- `rowIndex` 默认不允许用户取消，或者只显示为禁用项。
- `action` 是否允许隐藏由业务决定。
- 纯内部字段不进入列设置。
- 没有 `dataIndex` 的组合列不进入列设置，除非显式配置 `settingKey`。

### 7.5 第五阶段：列配置静态检查

建议增加一个检查脚本，用于在前端构建前或代码审查时发现问题：

检查规则：

- `defColumns` 中每个可配置列必须有 `dataIndex`。
- `defColumns` 中每个可配置列必须有 `title`。
- `title` 不允许包含 `请输入`、`请选择` 等表单校验文案。
- `rowIndex` 必须有标题。
- `action` 缺标题必须报错。

## 8. 推荐实施步骤

### 步骤 1：修复最高风险页面

先修：

- `jshERP-web/src/views/system/OptionList.vue`
- `jshERP-web/src/views/bill/AllocationOutList.vue`
- `jshERP-web/src/views/bill/AssembleList.vue`
- `jshERP-web/src/views/bill/DisassembleList.vue`

验证：

- 打开对应页面列设置。
- 字段名称必须显示正常中文。
- 勾选、取消、保存、刷新后仍正常。

### 步骤 2：修复报表 rowIndex 标题

统一给报表 `rowIndex` 增加标题。

验证：

- 报表列设置弹窗中不出现空白项。
- 禁用的序号列显示合理。
- 报表查询、分页、导出不受影响。

### 步骤 3：确认 action 列业务标题

对以下标题做业务确认：

- `账户流水`
- `欠款详情`
- `库存详情`
- `按钮列表`

确认后决定保留业务标题，还是统一改为 `操作`。

### 步骤 4：加固列设置公共逻辑

在通用列设置入口增加：

- 标题兜底。
- 非法缓存过滤。
- 空缓存回退默认列。

涉及入口：

- `JeecgListMixin.js`
- `BillListMixin.js`
- `MaterialList.vue`
- 报表页面中重复的列设置逻辑，后续建议抽公共 mixin。

### 步骤 5：处理历史缓存

建议提供两种方式：

1. 页面“重置默认列配置”继续清除当前页面缓存。
2. 前端列配置增加版本号，例如 `columnsVersion`，当版本变化时自动忽略旧缓存。

不建议直接让用户手工清浏览器全部缓存，因为会影响登录态和其它偏好设置。

### 步骤 6：全量回归

至少覆盖：

- 单据：零售、采购、销售、仓库、组装、拆卸、调拨、其他出入库。
- 财务：收款、付款、转账、预收款。
- 报表：库存、出入库、采购、销售、零售、账户、客户、供应商。
- 系统：选项、用户、角色、菜单、字典、仓库、账户、收支项目。
- 商品：商品列表、商品属性、商品扩展属性。

每个页面验证：

- 列设置字段名称正常。
- 默认列显示正常。
- 取消某列后表格正确隐藏。
- 刷新页面后列选择仍生效。
- 重置默认列配置后恢复正常。

## 9. 静态检查命令

可用于后续复查：

```bash
rg -n "title:\\s*['\"]请输入|title:\\s*['\"]请选择" jshERP-web/src/views jshERP-web/src/components
```

```bash
rg -n "dataIndex:\\s*['\"]action['\"]|dataIndex:\\s*['\"]rowIndex['\"]" jshERP-web/src/views jshERP-web/src/components
```

```bash
rg -n "item\\.title|item\\.dataIndex|onColChange|settingDataIndex" jshERP-web/src/views jshERP-web/src/components
```

## 10. 验收标准

修复完成后应满足：

1. 所有列设置弹窗中不再出现空白字段名。
2. 所有列设置弹窗中不再出现 `请输入...`、`请选择...` 这类校验文案。
3. 单据列表的操作员、备注、状态等字段标题与实际列一致。
4. 报表序号列有明确标题。
5. 本地旧列设置缓存不会导致页面列集合异常。
6. `npm run build` 通过。
7. 打开核心业务页面后，列设置、查询、分页、导出、刷新后恢复均正常。

## 11. 本轮未修改代码

本次仅完成静态排查并输出解决方案，没有修改前端源码。
