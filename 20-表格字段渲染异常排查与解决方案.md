# 表格字段渲染异常排查与解决方案

## 1. 排查结论

零售出库列表“状态”列显示为原始值 `1`，不是后端数据错误，也不是状态字典缺失。根因是 Ant Design Vue 4 升级后，当前项目把旧的表格命名插槽迁移成了一个全局 `$renderColumnSlot()` 桥接方法，但这个桥接方法读取不到页面内部声明的 `<template #customRenderStatus>` 插槽，最终回退返回原始 `text`。

当前问题不是零售出库单点问题。静态扫描结果：

- `jshERP-web/src` 下共有 62 个文件使用 `$renderColumnSlot(...)`。
- 共有 130 处列渲染调用依赖该桥接方法。
- `customRenderStatus` 涉及 27 个文件，单据、财务、字典等状态列都可能显示为 `0/1/9` 等原始值。
- `action` 涉及 48 个文件，操作列也有同类风险。
- 仍有 58 处 `customRender:function(t,r,index)` 形式的旧 AntD 1.x 入参写法，AntD4 下也需要继续清理。

## 2. 零售出库状态列定位

### 2.1 当前代码结构

`jshERP-web/src/views/bill/RetailOutList.vue` 中，状态列模板是：

```vue
<template #customRenderStatus="{ text: status }">
  <a-tag v-if="status == '0'" color="red">未审核</a-tag>
  <a-tag v-if="status == '1'" color="green">已审核</a-tag>
  <a-tag v-if="status == '9'" color="orange">审核中</a-tag>
</template>
```

列配置是：

```js
{
  title: '状态',
  dataIndex: 'status',
  width: 80,
  align: 'center',
  customRender: (cell) => this.$renderColumnSlot('customRenderStatus', cell)
}
```

全局桥接方法在 `jshERP-web/src/main.js`：

```js
app.config.globalProperties.$renderColumnSlot = function (slotName, cell) {
  const payload = cell && typeof cell === 'object'
    ? cell
    : { text: cell }
  const text = Object.prototype.hasOwnProperty.call(payload, 'text') ? payload.text : cell
  const slot = this.$slots && this.$slots[slotName]
  return slot ? slot({
    text,
    value: text,
    record: payload.record,
    index: payload.index,
    column: payload.column
  }) : text
}
```

### 2.2 为什么会显示 `1`

`$renderColumnSlot()` 中的 `this.$slots` 指的是当前页面组件从父组件接收到的插槽，不是当前页面模板里传给 `<a-table>` 的插槽。

也就是说，`RetailOutList.vue` 内部写的：

```vue
<template #customRenderStatus>
```

是传给子组件 `<a-table>` 的插槽，不会出现在 `RetailOutList.vue` 自身的 `this.$slots` 中。

所以执行链路实际变成：

```text
status 列 customRender
-> this.$renderColumnSlot('customRenderStatus', cell)
-> this.$slots.customRenderStatus 不存在
-> return text
-> 页面显示原始值 1
```

这与截图完全一致。

## 3. Ant Design Vue 4 表格插槽机制

本地依赖源码中可以确认，AntD4 Table 当前推荐使用：

- `v-slot:bodyCell`
- `v-slot:headerCell`

本地 `node_modules/ant-design-vue/es/table/hooks/useColumns.js` 中也保留了 `column.slots` 兼容逻辑，但明确给出提示：

```text
`column.slots` is deprecated. Please use `v-slot:headerCell` `v-slot:bodyCell` instead.
```

因此当前项目不应继续扩展 `$renderColumnSlot` 这种全局桥接方式。它既不能访问当前页面局部 slot，也不是 AntD4 推荐模型。

## 4. 同类问题影响范围

### 4.1 `$renderColumnSlot` 按插槽名统计

| 插槽名 | 文件数 | 影响类型 |
| --- | ---: | --- |
| `action` | 48 | 操作列按钮、查看、编辑、删除、复制、关联操作 |
| `customRenderStatus` | 27 | 单据、财务、字典等状态列 |
| `customRenderFlag` | 12 | 系统模块启用/禁用、状态标记 |
| `numberCustomRender` | 11 | 单据编号、报表编号、可点击详情链接 |
| `customPic` | 3 | 商品图片、库存报表图片 |
| `customRenderEnabled` | 3 | 启用状态、选项状态、租户状态 |
| `customRenderStock` | 3 | 商品库存、库存报表库存状态 |
| `customRenderDebt` | 2 | 采购入库、销售出库欠款字段 |
| `customRenderEnabledFlag` | 2 | 账户、仓库启用状态 |
| 其它单点插槽 | 1 | `customName`、`linkInfo`、`scopeRender`、`customRenderType` 等 |

### 4.2 状态列明确受影响文件

`customRenderStatus` 当前涉及：

- `jshERP-web/src/views/bill/AllocationOutList.vue`
- `jshERP-web/src/views/bill/AssembleList.vue`
- `jshERP-web/src/views/bill/DisassembleList.vue`
- `jshERP-web/src/views/bill/OtherInList.vue`
- `jshERP-web/src/views/bill/OtherOutList.vue`
- `jshERP-web/src/views/bill/PurchaseApplyList.vue`
- `jshERP-web/src/views/bill/PurchaseBackList.vue`
- `jshERP-web/src/views/bill/PurchaseInList.vue`
- `jshERP-web/src/views/bill/PurchaseOrderList.vue`
- `jshERP-web/src/views/bill/RetailBackList.vue`
- `jshERP-web/src/views/bill/RetailOutList.vue`
- `jshERP-web/src/views/bill/SaleBackList.vue`
- `jshERP-web/src/views/bill/SaleOrderList.vue`
- `jshERP-web/src/views/bill/SaleOutList.vue`
- `jshERP-web/src/views/bill/dialog/BatchWaitBillList.vue`
- `jshERP-web/src/views/bill/dialog/HistoryBillList.vue`
- `jshERP-web/src/views/bill/dialog/LinkBillList.vue`
- `jshERP-web/src/views/bill/dialog/WaitBillList.vue`
- `jshERP-web/src/views/financial/AdvanceInList.vue`
- `jshERP-web/src/views/financial/GiroList.vue`
- `jshERP-web/src/views/financial/ItemInList.vue`
- `jshERP-web/src/views/financial/ItemOutList.vue`
- `jshERP-web/src/views/financial/MoneyInList.vue`
- `jshERP-web/src/views/financial/MoneyOutList.vue`
- `jshERP-web/src/views/report/modules/HistoryFinancialList.vue`
- `jshERP-web/src/views/system/DictList.vue`
- `jshERP-web/src/views/system/modules/DictDataListModal.vue`

这些页面的状态列都应按同一类问题处理。

### 4.3 其它字段同源风险

除状态列外，以下字段也存在同类风险：

- 操作列 `action`：如果未被 AntD4 正确渲染，操作按钮可能为空或显示异常。
- 编号列 `numberCustomRender` / `billNoCustomRender`：详情链接、单据编号跳转可能丢失。
- 欠款列 `customRenderDebt`：欠款红色标记可能失效。
- 商品图片 `customPic`：图片预览列可能显示原始 URL 或空值。
- 商品名称 `customName`：商品名的复合展示可能退化。
- 系统状态 `customRenderFlag`、`customRenderEnabled`、`customRenderEnabledFlag`：启用/禁用标签可能显示原始布尔值或数字。
- 字典字段 `customRenderDictType`、`customRenderDictLabel`：字典类型/标签可能显示原始值。
- 插件字段 `linkInfo`：插件链接展示可能退化。

## 5. 旧 customRender 入参问题

AntD4 的 `customRender` 入参已经是对象形式：

```js
customRender: ({ text, record, index, column }) => {}
```

但当前源码中仍有 58 处 `customRender:function(...)` 形式，分布在 45 个文件中。典型旧写法：

```js
customRender:function (t, r, index) {
  return parseInt(index) + 1
}
```

在 AntD4 下，函数第一个参数实际是对象，继续按 `t, r, index` 使用会导致：

- `r` 为 `undefined`
- `index` 为 `undefined`
- 行号变成 `NaN`
- 依赖 `record` 的显示逻辑失效

这类问题与状态列显示异常属于同一轮 AntD4 表格迁移遗留问题，应该一起处理。

## 6. 不建议继续使用当前桥接方式

当前全局桥接：

```js
this.$renderColumnSlot('customRenderStatus', cell)
```

不建议继续扩展，原因：

1. 它读取的是页面组件自己的 `$slots`，不是 `<a-table>` 的 slot 上下文。
2. 它把表格列渲染逻辑放到全局属性中，问题定位困难。
3. 它会在找不到 slot 时静默返回原始值，容易掩盖渲染失败。
4. 它不是 AntD4 推荐写法，后续继续清理 Vue3 生态时还要再次返工。

## 7. 解决方案

### 7.1 推荐方案：统一迁移到 `#bodyCell`

以零售出库为例，推荐将状态、操作等列统一放到 `bodyCell` 中按列分支渲染：

```vue
<a-table
  :columns="columns"
  :dataSource="dataSource"
  ...>
  <template #bodyCell="{ column, text, record, index }">
    <template v-if="column.dataIndex === 'status'">
      <a-tag v-if="text == '0'" color="red">未审核</a-tag>
      <a-tag v-else-if="text == '1'" color="green">已审核</a-tag>
      <a-tag v-else-if="text == '9'" color="orange">审核中</a-tag>
      <span v-else>{{ text }}</span>
    </template>

    <template v-else-if="column.dataIndex === 'action'">
      <!-- 原操作列按钮 -->
    </template>
  </template>
</a-table>
```

对应列配置中删除：

```js
customRender: (cell) => this.$renderColumnSlot('customRenderStatus', cell)
```

如果一个页面有多个表格，应按每个表格各自的 `bodyCell` 处理，避免多个表格共享同名 slot 导致冲突。

### 7.2 可接受的临时方案：恢复 `column.slots`

如果需要快速恢复页面显示，可以临时使用 AntD4 仍兼容但已废弃的写法：

```js
{
  title: '状态',
  dataIndex: 'status',
  slots: { customRender: 'customRenderStatus' }
}
```

前提是删除同一列上的 `customRender`，因为本地 AntD4 源码只会在 `cloneColumn[key] === undefined` 时把 `slots.customRender` 映射过去。

该方案能快速修复显示，但不建议作为最终状态，因为 AntD4 已提示 `column.slots` 废弃。

### 7.3 适合公共状态的方案：直接函数渲染

对于通用状态列，也可以用纯 `customRender` 函数直接返回 VNode：

```js
customRender: ({ text }) => {
  const map = {
    '0': { color: 'red', label: '未审核' },
    '1': { color: 'green', label: '已审核' },
    '9': { color: 'orange', label: '审核中' }
  }
  const item = map[String(text)]
  return item ? h(Tag, { color: item.color }, () => item.label) : text
}
```

这种方式适合状态映射固定、页面交互较少的列。操作列、复杂编号列、图片列更适合 `bodyCell`。

## 8. 建议实施步骤

### 第一步：先修零售出库状态列

目标文件：

- `jshERP-web/src/views/bill/RetailOutList.vue`

处理范围：

- `status` 列
- `action` 操作列
- 展开行 `expandedRowRender` 保持不变

验收：

- 状态 `0` 显示“未审核”红色标签。
- 状态 `1` 显示“已审核”绿色标签。
- 状态 `9` 显示“审核中”橙色标签。
- 操作列按钮仍可点击。

### 第二步：批量修单据主列表状态列

目标文件：

- `AllocationOutList.vue`
- `AssembleList.vue`
- `DisassembleList.vue`
- `OtherInList.vue`
- `OtherOutList.vue`
- `PurchaseApplyList.vue`
- `PurchaseBackList.vue`
- `PurchaseInList.vue`
- `PurchaseOrderList.vue`
- `RetailBackList.vue`
- `RetailOutList.vue`
- `SaleBackList.vue`
- `SaleOrderList.vue`
- `SaleOutList.vue`

处理范围：

- `customRenderStatus`
- `action`
- `customRenderDebt`
- `customRenderPurchaseStatus`

验收：

- 所有单据列表状态列不再显示 `0/1/9`。
- 欠款字段红色标记正常。
- 销售订单采购状态正常。
- 查看、编辑、复制、删除等操作按钮正常。

### 第三步：修单据弹窗和报表编号列

目标文件：

- `bill/dialog/BatchWaitBillList.vue`
- `bill/dialog/HistoryBillList.vue`
- `bill/dialog/LinkBillList.vue`
- `bill/dialog/WaitBillList.vue`
- `financial/dialog/DebtBillList.vue`
- `report/AllocationDetail.vue`
- `report/InDetail.vue`
- `report/OutDetail.vue`
- `report/modules/AccountInOutList.vue`
- `report/modules/DebtAccountList.vue`
- `report/modules/MaterialInOutList.vue`

处理范围：

- `numberCustomRender`
- `billNoCustomRender`
- `customRenderStatus`

验收：

- 编号列仍可点击打开详情。
- 状态标签正常。
- 弹窗选择、确认、跳转逻辑不变。

### 第四步：修财务状态列

目标文件：

- `AdvanceInList.vue`
- `GiroList.vue`
- `ItemInList.vue`
- `ItemOutList.vue`
- `MoneyInList.vue`
- `MoneyOutList.vue`

处理范围：

- `customRenderStatus`
- `action`

验收：

- 财务单据状态不再显示原始数字。
- 新增、编辑、删除、审核类操作正常。

### 第五步：修系统、商品、报表模块字段

重点字段：

- `customRenderFlag`
- `customRenderEnabled`
- `customRenderEnabledFlag`
- `customPic`
- `customName`
- `customRenderStock`
- `customRenderDictType`
- `customRenderDictLabel`
- `linkInfo`
- `scopeRender`

验收：

- 系统模块启用/禁用字段显示标签或文本，不显示原始布尔值。
- 商品图片正常显示。
- 商品库存状态正常。
- 字典类型、字典状态正常。

### 第六步：清理旧 `customRender:function(t,r,index)` 签名

统一改为：

```js
customRender: ({ text, record, index }) => {
  // 原逻辑
}
```

重点关注：

- 行号列
- 操作列
- 依赖 `record` 拼接显示的列
- 报表跳转列

验收：

- 不再出现 `customRender:function(t,r,index)`。
- 不再出现 `customRender:function (t,r,index)`。
- 行号不显示 `NaN`。
- 依赖 `record` 的字段不报错。

### 第七步：删除 `$renderColumnSlot`

当扫描结果确认没有 `$renderColumnSlot` 调用后，再删除：

- `jshERP-web/src/main.js` 中的 `$renderColumnSlot` 全局方法。

验收：

```bash
rg -n -F '$renderColumnSlot' jshERP-web/src
```

应无结果。

## 9. 验证清单

构建验证：

```bash
cd jshERP-web
npm run build
```

页面验证：

- 零售出库：状态列、操作列、展开明细。
- 采购入库：状态列、欠款列、操作列。
- 销售出库：状态列、欠款列、操作列。
- 销售订单：审核状态、采购状态。
- 采购订单：状态列、关联单据显示。
- 财务收付款：状态列、操作列。
- 商品列表：图片、名称、库存、启用状态。
- 系统用户/角色/菜单/字典：启用状态、操作列。
- 报表明细：单据编号可点击跳转。

静态扫描：

```bash
rg -n -F '$renderColumnSlot' jshERP-web/src
rg -n 'customRender\s*:\s*function\s*\(' jshERP-web/src
rg -n 'scopedSlots|slots:\s*\{\s*customRender' jshERP-web/src
```

目标：

- `$renderColumnSlot` 为 0。
- 旧 `customRender:function(...)` 为 0。
- 不新增 `scopedSlots`。
- 不长期保留 `column.slots.customRender`。

## 10. 优先级建议

优先级如下：

1. 先修 `RetailOutList.vue`，验证截图中的状态列问题。
2. 修所有单据主列表的 `customRenderStatus/action`。
3. 修单据弹窗和报表编号列，避免详情跳转丢失。
4. 修财务状态列。
5. 修系统、商品、报表中的布尔状态、图片、字典、链接字段。
6. 清理旧 `customRender:function(t,r,index)`。
7. 最后删除 `$renderColumnSlot` 全局桥接。

